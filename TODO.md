# Nuance 구현 TODO

담당별 구현 체크리스트(작업의 단일 소스). **분업 경계 = 팝업창**: A는 팝업이 뜨기 전까지의 로직(창 선택·캡처·오버레이·모드·텍스트 추출·단어 좌표·클릭 감지), B는 팝업이 뜬 이후의 모든 로직(팝업 내 범위 확정·문맥 구성·발음/사전/질문·결과 렌더·설정 화면)을 담당한다. 설계·인터페이스 계약은 [PLAN.md](PLAN.md) 참고 — §8(2인 분업 / 인터페이스 계약), §10(프로젝트 구조).

권장 순서: **뼈대 → 한 경로 end-to-end 관통 → 소스별 확장**. 첫 관통 경로는 가장 확실한 **PDF/텍스트 직접 추출 → 통합 질문**으로 잡고, 이후 OCR·자막·발음·사전을 붙인다.

## 목차

- [🅰️ 담당 A — 선택 준비 & 추출 (팝업 전)](#a-담당)
  - [앱 · 윈도우 · 모드](#a-app)
  - [단어 감지 · 좌표 매핑 (오버레이)](#a-coord)
  - [추출 판정 · 실행](#a-extract)
- [🅱️ 담당 B — 선택 확정 & 질문 (팝업 후)](#b-담당)
  - [팝업 선택 & 문맥 확정](#b-select)
  - [LLM 어댑터](#b-llm)
  - [질문 기능](#b-feature)
  - [UI · 설정](#b-ui)
  - [브라우저 확장 · 자막 추출 (MV3)](#b-ext)
- [🤝 공동](#공동)
- [🙈 코드는 살려두고 UI에서만 감춘 것](#hidden-ui)
- [⚠️ 미해결 문제](#미해결-문제)

<a id="a-담당"></a>

## 🅰️ 담당 A — 선택 준비 & 추출 (팝업 전)

<a id="a-app"></a>

**앱 · 윈도우 · 모드**
- [x] 창 선택 UI + `desktopCapturer` 창 목록/선택 — **다른 가상 데스크탑(Space)의 창까지 보여주는 확장은 시도했다가 되돌림**(`feat/mac-other-space-window`→`fix/mac-window-list-slow`로 이어졌던 `listWindowsMac`/`macWindow.ts: listAllMacWindows`). 목록이 지저분해지는 등 UX가 오히려 나빠져 원복 — macOS도 다시 Windows와 동일하게 `desktopCapturer`(현재 Space에 보이는 창만) 하나로 목록을 구성한다(`listWindowsElectron`). Space별 탭 구분 시도(비공개 SkyLight API)도 신뢰도 문제로 보류했던 건이라 관련 TODO 항목도 함께 정리함. **자기 자신(Nuance) 창 제외** — win32 경로(`listWindowsWin32`)는 `BrowserWindow.getAllWindows()`의 hwnd로 이미 걸러내고 있었는데, `listWindowsElectron`(macOS/Linux 경로)엔 이 필터가 없어서 목록에 앱 자신의 창(빈 화면)이 함께 떴다 — `win.getMediaSourceId()`로 자기 창들의 desktopCapturer 소스 id를 모아 동일하게 제외하도록 수정(`capture.ts`).
- [x] 선택된 창 테두리 색 표시 (일반=파랑 / 선택=보라) — Windows: 대상 창에 정렬+실시간 추적. **macOS: 구현됨** — `showMacSelectionOverlay`(`windows.ts`)가 CoreGraphics `CGWindowListCopyWindowInfo`(koffi, `selection/macWindow.ts`)로 desktopCapturer window id 의 실제 bounds 를 얻어 테두리를 정렬하고, `MAC_TRACK_INTERVAL_MS`(16ms) 고빈도 폴링으로 이동/리사이즈를 따라간다(Windows 폴백 폴링의 150ms보다 촘촘함). bounds 를 못 구하면(예: 창이 닫힘) 테두리를 숨긴다(`hideMacOverlay`). **추가 권한 프롬프트 없음**(창 geometry 조회는 권한 불필요, 창 목록엔 이미 화면기록 권한 사용). osascript(자동화 권한) 방식은 unsigned dev 앱에서 프롬프트가 안 떠 폐기. → **다른 창이 대상을 덮으면 테두리가 안 보이던 문제 수정(2026-07-29)**: 예전엔 대상 창이 다른 앱 창에 가리면 z-순서 판정(`isMacTargetCovered`)으로 테두리를 일부러 숨겼는데("창 사이에 끼는" 걸 막으려는 의도였음), 사용자 요청으로 반대로 바꿈 — 오버레이 창을 `alwaysOnTop: true`로 만들어(mac은 기존 `floating` 레벨 유지, win32도 동일하게 적용) 다른 창이 덮여도 테두리가 계속 보이게 함. `isMacTargetCovered`/`getOnScreenLayer0Windows`/`intersects`(가림 판정 전용, 더 이상 안 씀)는 죽은 코드라 삭제. → **재수정(2026-07-29, "데스크탑 바꿔도 테두리가 그대로 있다" 피드백)**: 위 수정 때 `alwaysOnTop`과 함께 `setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true })`도 같이 켰었는데, 이건 "같은 데스크탑에서 다른 창에 가려도 보이게"가 아니라 "다른 데스크탑(Space)으로 전환해도 계속 보이게"까지 만드는 설정이라 대상 창이 없는 화면에도 테두리가 남는 부작용이 있었다. `setVisibleOnAllWorkspaces` 호출을 제거(기본값 false로 복귀)하고 `alwaysOnTop(true, 'floating')`만 남김 — 같은 Space 안에서 다른 창에 가려도 보이는 원래 목적은 유지하면서, 데스크탑을 바꾸면 그 Space 에선 더 이상 안 보이게 됨. → **2차 재수정(2026-07-29, "포커싱이 다른 창일 때 테두리가 그 창 앞으로 나온다", "오버레이 테두리는 선택된 창 바로 위에만 붙어있어야 해" 피드백)**: `alwaysOnTop`을 상시 켜둔 게 애초에 과했다 — 대상이 아닌 다른(포커싱된) 창 위에도 테두리가 항상 떠 있게 됐다. win32는 원래 있던 `syncOverlayZOrder`(대상 창 바로 위 z-order 한 칸에 꽂기)로 되돌리는 게 정석이라 base 생성자 옵션의 `alwaysOnTop: true`를 제거(→ win32는 이제 다시 대상이 다른 창에 가리면 테두리도 같이 가려짐, 처음 설계 그대로). mac은 네이티브로 "이 창 바로 위"에 꽂는 API가 마땅치 않아, 대신 매 16ms 폴링마다 대상이 지금 화면 맨 앞(=사실상 포커싱된 창)인지 확인해(`macWindow.ts` 신규 `isMacWindowFrontmost` — `CGWindowListCopyWindowInfo(OnScreenOnly)`가 z-순서로 주는 성질 이용, `kCGWindowLayer`가 0인 첫 항목이 대상인지 확인) 맞을 때만 `setAlwaysOnTop(true, 'floating')`, 아니면 `setAlwaysOnTop(false)`로 근사(`showMacSelectionOverlay`, 값이 바뀔 때만 호출). 오버레이 자신이 이 z-순서 목록에서 스스로를 대상으로 오인하지 않도록, 오버레이 창에만 안 보이는 표식 제목(`OVERLAY_MARKER_TITLE`, `frame:false`라 화면엔 안 보임)을 붙이고 `isMacWindowFrontmost`가 그 제목을 만나면 건너뛴다 — pid 로는 메인/설정/팝업 등 같은 프로세스의 다른 창과 구분이 안 돼 제목으로 구분해야 했다. → **3차 재수정(2026-07-29, "선택창 포커스가 해제되면 테두리가 안 보이게 됐다" 피드백)**: 2차 재수정의 `alwaysOnTop` on/off 토글은, 꺼졌을 때 오버레이가 "일반 레벨의 어디쯤"으로 가라앉을 뿐이라 대상 창이 멀쩡히 보이는데도(아무것도 안 덮었는데도) 테두리만 안 보이는 문제가 있었다. win32의 `syncOverlayZOrder`처럼 매 틱 대상 창 바로 위 z-order 한 칸에 직접 꽂는 방식으로 전환 — 공개 API `-[NSWindow orderWindow:relativeTo:]`(`macWindow.ts: orderOverlayAboveWindow`, Electron `getNativeWindowHandle()`이 주는 NSView*에서 `-window` 접근자로 NSWindow*를 얻어 호출)를 objc_msgSend로 직접 호출한다 — koffi는 포인터를 BigInt로 표현하므로 반환값을 그대로 다음 호출의 인자로 넘길 수 있다. alwaysOnTop 토글 없이도 다른 창이 대상을 덮으면 테두리도 자연히 같이 가려지고, 아무것도 안 덮으면 항상 대상 바로 위에 남는다. 이 네이티브 호출이 실패하는 경우에만 안전망으로 `alwaysOnTop(true, 'floating')`을 켠다. 이전 시도(`isMacWindowFrontmost`/`OVERLAY_MARKER_TITLE`)는 더 이상 안 써서 삭제. → **4차 재수정(2026-07-29, "전체화면일 땐 테두리 안 나왔으면 좋겠다" 요청)**: win32(`applyOverlayBounds`)는 대상 창이 모니터를 진짜로 꽉 채우면(F11류, 유튜브/넷플릭스 영상 전체화면 포함) 오버레이를 숨기는 로직이 이미 있었는데 mac(`showMacOverlayAt`)에는 없었다 — 동일한 `coversWholeDisplay` 판정을 mac 쪽에도 추가해 전체화면일 때 오버레이를 숨기도록 통일. → **win32/mac 격차 코드 감사로 발견한 버그 2건 수정(2026-07-29, 사용자 요청 "윈도우 맥에 차이 있는 부분 검사해봐")**: (1) 4차 재수정 직후, mac 폴링이 `showMacOverlayAt`으로 전체화면 판정 숨김을 한 바로 다음 줄에서 `overlayVisible` 확인 없이 `orderOverlayAboveWindow`(`-orderWindow:relativeTo:`)를 계속 불렀다 — mac에서 이 API는 win32의 `SetWindowPos`와 달리 순수 z-order 전용이 아니라 "보이기/순서 배치" 그 자체라, 방금 숨긴 창을 이 호출이 다시 나타나게 할 수 있어 방금 고친 전체화면 숨김을 무력화할 뻔했다 — win32의 `syncOverlayZOrder`가 이미 갖고 있던 것과 동일한 `overlayVisible` 가드를 추가. (2) win32(`getWindowScreenRect`)는 대상 창이 최소화되면 `IsIconic`으로 감지해 오버레이를 숨기는데, mac은 `getMacWindowBounds`가 최소화돼도 (닫힘과 구분하려고 의도적으로) 마지막 bounds를 그대로 줘서 최소화 중에도 옛 위치에 테두리가 계속 떠 있었다 — `macWindow.ts`에 `isMacWindowOnScreen`(`kCGWindowListOptionOnScreenOnly`를 같이 줘서 조회, 최소화된 창은 결과가 비어 옴)을 추가해 최소화 중엔 오버레이만 숨기고(닫힘 판정용 `missingBoundsStreak`는 건드리지 않음) win32와 동작을 맞췄다. → **5차 재수정(2026-07-29, "데스크탑 전환 시 오버레이가 따라온다" 재발 제보)**: `isMacWindowOnScreen`을 처음엔 `kCGWindowListOptionOnScreenOnly`와 `kCGWindowListOptionIncludingWindow`(특정 windowId 하나만 조회하는 옵션)를 비트 OR로 합쳐서 썼는데, `kCGWindowListOptionIncludingWindow`는 원래 "그 windowId 부터 시작하는 나머지 목록"을 뜻하는 옵션(relativeToWindow 위치 지정용)이라 onScreenOnly와 합치면 "이 창 하나만"이 아니라 "이 창 위치부터 이어지는 모든 on-screen 창들"이 반환됐을 수 있다 — 대상이 안 보이는 상태(다른 데스크탑에 있음)여도 다른 on-screen 창들(Finder 등) 때문에 결과가 거의 항상 비어있지 않아 "보임"으로 오판했을 가능성이 높다. 이미 검증된 `listMacWindowOwnerNames`와 동일하게 `kCGWindowListOptionOnScreenOnly` 단독으로 전체 on-screen 목록을 받아 그 안에 windowId가 있는지 직접 찾는 방식으로 바꿔 이 모호함을 없앴다.
  - [x] **(2026-07-29) 대상 창이 진짜 전체화면(F11 등, taskbar 까지 덮음)일 때 taskbar 자동 숨김이 안 되던 문제 수정 — Windows 전용**(win32 z-order 트릭 특유의 원인이라 macOS 는 해당 없음, 별도 확인 안 함). 원인: `syncOverlayZOrder`(`windows.ts`)가 오버레이를 대상 창 바로 위 z-order 에 계속 꽂아두는데, Windows 는 포그라운드 전체화면 창 위에 다른 창이(투명·클릭스루라도) z-order 상 겹쳐 있으면 taskbar 자동 숨김 등 "전체화면 최적화"를 발동하지 않는다(게임 오버레이 앱이 같은 최적화를 깨는 것과 동일 원인) — 실사용 중 "선택된 창에서만 F11 눌러도 taskbar 안 사라짐"으로 확인. 대상 창이 모니터를 진짜로(taskbar 포함) 꽉 채우면(`applyOverlayBounds`: `coversWholeDisplay`) 오버레이 창 자체를 숨기도록 수정(`overlayWindow.hide()`) — 전체화면을 벗어나면 다음 위치/크기 갱신 때 자동 복원됨. **1차 수정 때 `!isMaximized` 를 조건에 넣었다가 실패**(실사용 진단 로그로 확인, 2026-07-29): 크롬 F11 은 창 크기를 모니터 전체로 키우면서도 `IsZoomed`(win32Capture.ts: isWindowMaximized) 를 그대로 true 로 남긴다 — `!isMaximized` 조건이 이 케이스를 전부 걸러내 hide 가 전혀 안 됐음. `isMaximized` 값과 무관하게 "모니터를 실제로 덮었는지"만으로 판단하도록 수정해 해결(단순 최대화로 taskbar 가 그대로 보이는 케이스는 `coversWholeDisplay` 가 애초에 false 라 자연히 구분됨, `isMaximized` 는 그 케이스의 workArea 스냅에만 그대로 사용). **트레이드오프(사용자 결정)**: 오버레이가 숨겨지는 동안은 테두리뿐 아니라 hover/클릭 기반 텍스트 선택도 같이 비활성화된다 — 자막 모드(유튜브/넷플릭스)는 확장이 페이지 안에 직접 하이라이트를 그려서(`extension/src/highlight.ts`) 이 오버레이와 무관하니 지장 없지만, 그 외 OCR 기반 선택 모드는 전체화면인 동안 잠깐 선택이 안 되는 쪽을 감수하기로 함.
  - [x] **(2026-07-29) 오버레이 테두리 두께를 OS 별로 분리 — 같은 값(3px)이라도 Windows 쪽이 유독 얇아 보인다는 사용자 피드백**(`styles.css`). `--overlay-radius`와 같은 패턴으로 `--overlay-border-width` 변수를 추가해 `.overlay-root`의 outline과 `.region-border-topcoat`의 border가 항상 같은 값을 공유하게 함 — 기본값(Windows/Linux)은 기존의 1.5배(3px→4.5px), `.is-mac`(Overlay.tsx: IS_MAC)일 때만 기존 3px 그대로 유지. 단순 최대화(workArea) 상태에서 DPI 배율(125%) 반올림으로 아랫 테두리가 잘려 보이는 별개 문제(위 항목과 무관, 미해결)는 나중에 별도로 다루기로 함.
  - [x] **(2026-07-29) mac 오버레이 테두리 모서리 반지름을 14px→10px로 정정** — 14px는 "안쪽 반지름이 뚜렷이 보이게"라는 내부 대비 목적으로 고른 값일 뿐 실제 macOS 창 모서리 곡률과는 무관했다는 사용자 지적으로 재조사. Big Sur(2020)에서 확정돼 Monterey/Ventura/Sonoma까지 유지된 표준 타이틀 창 모서리 반지름은 10pt(Mozilla Bugzilla #1670150에서 재확인된 값 — Control Center 같은 다른 패널은 16px로 다르니 혼동 주의)로 이 앱의 대상 macOS 버전대와 일치해 10px로 정정(`styles.css` `--overlay-radius`). **한계**: macOS는 CSS `border-radius`(순수 원호)가 아니라 "continuous"(스퀴클에 가까운) 곡선을 쓰는데, Chromium/Electron 31은 이 곡선을 그리는 CSS `corner-shape`를 아직 지원 안 해(2024년 W3C 초안 단계) 완벽히 동일하진 않다 — 10px 원호가 실제 곡률에 가장 가까운 근사치. 최신 macOS Tahoe(26)는 창 종류별(툴바 有無)로 반지름 자체가 달라지도록 재설계됐으나 이 앱이 대상으로 하는 버전대(Sonoma 등)엔 해당 없음.
- [x] 백그라운드 실행 + 트레이 아이콘 (창 선택 해제 / 창 선택 전환 / 설정 — 2026-07-29 요청으로 "선택 해제"가 "전환"보다 앞에 오도록 순서 변경) → **선택하던 창이 닫히면 자동 선택 해제(2026-07-29)**: 선택 중이던 창을 닫아도 선택 상태(오버레이·트레이 메뉴)가 그대로 남아있던 문제 — 최소화와 "진짜로 닫힘"을 구분해야 해서(최소화 시엔 선택을 유지해야 함), win32는 `IsWindow`(핸들 생존 여부, `win32Capture.ts` `isWin32WindowAlive` — 기존 `getWindowScreenRect`의 null은 최소화·닫힘 둘 다에서 나와 구분이 안 됐음), mac은 `getMacWindowBounds`가 `kCGWindowListOptionIncludingWindow`만 써서(onScreenOnly 아님) 최소화된 창도 잡히므로 null이 반복되면(연속 15틱, 약 240ms) 닫힘으로 확정하는 방식으로 각각 구분(`windows.ts` 오버레이 추적 폴링에 통합). `onTargetWindowGone` 콜백 등록 방식으로 `windows.ts`는 선택 상태를 몰라도 되게 하고, `tray.ts`가 기존 수동 "선택 해제"(`deselectWindow`)를 그대로 재사용해 연결. → **전체화면 진입/해제 시 오탐으로 선택이 풀리던 버그 수정(2026-07-29)**: 대상 창을 전체화면으로 전환/해제하면 전환 애니메이션 도중 창이 일시적으로 조회 실패하는 경우가 있는데(mac은 별도 Space로 옮겨가며, 애니메이션이 보통 0.3~0.5초), mac 쪽 임계값이 240ms(16ms×15틱)로 너무 짧아 진짜로 닫힌 것으로 오판했다. mac은 16ms×90≈1.44초로 늘리고, win32(`isWin32WindowAlive`)는 원래 단 한 번의 실패로 바로 확정하던 걸 mac과 동일한 연속-실패 방식(150ms×10≈1.5초)으로 바꿔 방어적으로 통일.
- [x] 투명·클릭스루 오버레이 윈도우 (선택 창에 정렬 · 이동/리사이즈 추적) — Windows: WinEventHook + 폴백 폴링(150ms, `TRACK_FALLBACK_INTERVAL_MS`). macOS: CGWindowList bounds 폴링(`MAC_TRACK_INTERVAL_MS`=16ms)으로 정렬+이동/리사이즈 추적 — win32 는 즉시 반응 훅이 있지만 mac 은 폴링만인데, 폴링 주기 자체는 Windows 폴백(150ms)보다 오히려 촘촘함(16ms). z-order 재적용(`syncOverlayZOrder`)은 원래 포그라운드 전환 이벤트에서만 돌았는데, 메모장 탭 전환처럼 같은 창 안에서 내부적으로 다시 그려지는 경우(포그라운드 이벤트가 안 뜸) 오버레이가 뒤로 밀리는 문제가 있어 150ms 폴백 폴링에도 같이 넣음(`windows.ts: trackSelectionOverlay`).
- [x] 창 선택 시 대상 창 맨 앞으로 — Windows: `bringWindowToForeground`(win32Capture). macOS: 창의 owner PID(CGWindowList)로 `NSRunningApplication.activateWithOptions:`(objc, koffi, `selection/macWindow.ts`). raise 가 실패해도 테두리/추적은 정상 동작(try/catch 로 degrade). CoreGraphics/objc 네이티브 바인딩은 실기기(맥 arm64)에서 bounds·PID 조회·클래스 로드까지 검증됨.
- [x] 모드 전환 전역 단축키(기본 Alt+Q) + `MODE_CHANGED` 통지

<a id="a-coord"></a>

**단어 감지 · 좌표 매핑 (오버레이)**
- [x] 단어 bbox 좌표 확보 → 커서 좌표 ↔ 단어 매핑 — **Windows 전용, macOS 미구현**(캡처가 `captureFocusedWindow`/win32Capture 기반이라 macOS에선 애초에 OCR 자체가 안 돎; bbox 보정 중 원점 보정도 `getCaptureOriginOffset`으로 win32 전용 API 사용. 배율 보정만은 Electron `screen` API 기반이라 플랫폼 무관). 매핑 로직(`shared/wordMapping.ts: findWordAtPoint`)·오버레이·OCR 파이프라인이 실 데이터로 완전히 연결됨. 선택 모드 진입 시 캐시된 OCR 결과(`extractionCache.ts`)의 단어 bbox를 메인→오버레이로 IPC 통지(`windows.ts: sendOverlayWords`, `preload: onExtractionWords`)해서 `Overlay.tsx`가 **실제 단어 위치**로 hover/클릭 판정을 한다(`MOCK_WORDS` 자리표시자는 제거됨). bbox 정확도를 위해 두 가지 보정을 적용: (1) 물리 픽셀(캡처·OCR) → DIP(오버레이 렌더링) 배율 보정(`windows.ts: getPhysicalToDipScale`) — 디스플레이 배율 100% 아닐 때 어긋나는 문제, (2) 캡처 좌표계(`GetWindowRect`, 안 보이는 리사이즈 테두리 포함) ↔ 오버레이 좌표계(`DWMWA_EXTENDED_FRAME_BOUNDS`, 실제 보이는 프레임) 원점 차이 보정(`win32Capture.ts: getCaptureOriginOffset`). 단어 박스 높이는 단어 자체 bbox 대신 그 단어가 속한 줄(line) bbox 를 써서 같은 줄 단어들의 높이를 통일함(`ocr.ts`). 화면에 그리는 박스는 hover 판정용 bbox 와 별개로 좌우에 각각 2px 시각적 여백을 둬서 글자 획(L/T/I 등)과 테두리가 안 겹치게 함(`Overlay.tsx: WORD_BOX_PADDING`). **(2026-07-29 변경) 단어 단위 → 줄 단위 hover/선택으로 전환** — 사용자 결정에 따라, 커서 아래 단어가 속한 줄(세로쓰기 한 열/가로쓰기 한 줄) 전체를 hover 박스로 보여주고 클릭 시 그 줄 전체가 팝업 초기 선택 범위가 되도록 변경. `Word`에 같은 줄 단어들을 묶는 `lineId`를 추가(NDLOCR/PaddleOCR/Tesseract 세 인식 경로 모두 줄 단위 그룹핑 시 부여)하고, `shared/wordMapping.ts: findLineWordsAtPoint`(줄 전체 단어 반환)+`unionBbox`(그 줄들의 bbox 합집합, hover 박스에 사용)를 추가. 클릭 시 anchor 계산도 `selection/index.ts: findLineSpan`(같은 lineId 를 가진 연속 구간 전체의 오프셋)으로 확장 — `lineId` 가 없는 경로(예: 자막 direct 추출)는 기존 단어 단위로 자동 폴백. 팝업 쪽(anchor→초기 선택)은 무변경 — `computeContextRange` 가 원래 anchor 길이에 무관하게 항상 전체를 포함하도록 설계돼 있어서 그대로 작동. 문장부호(형태소 분석기가 "단어" 아닌 것으로 걸러 bbox 를 안 주는 gap 문자)로 줄이 끝나면 박스 테두리가 살짝 겹쳐 보이는 문제가 있어 `WORD_BOX_PADDING` 을 상/하/좌/우 전부에 적용하도록 조정함. **(2026-07-29 재요청으로 범위 축소) 줄 단위는 세로쓰기 일본어(NDLOCR-Lite, `ocrNdlocr.ts`)에만 남기고 나머지는 단어 단위로 되돌림** — 가로쓰기(영어 Tesseract/PaddleOCR, 중국어 PaddleOCR·Tesseract CJK 분기)는 `lineId` 부여를 제거했다(`ocr.ts`, `ocrPaddle.ts`) — `wordMapping.ts` `findLineWordsAtPoint` 가 `lineId` 없으면 이미 단어 단위(`[hit]`)로 폴백하도록 설계돼 있어 그대로 활용. 단어 경계의 문장부호 제외는 원래부터 `splitWordBySymbols`(Tesseract 트레일링 문장부호·대시 제거)가 담당하고 있어 별도 수정 불필요 — 유튜브/넷플릭스 자막 확장(hover 박스)에도 동일 규칙(`WORD_ATOM_RE`, `extension/src/domWords.ts`)을 새로 추가해 팝업의 영문 단어 판정(`popup/selection.ts` `LATIN_ATOM_RE`)과 맞춤.
  - [x] **(2026-07-29) 개발 전용 디버그 — OCR 추출 블록 시각화, Windows/macOS 공통.** 다단 레이아웃에서 왜 특정 블록이 유난히 크거나 느린지 개발 중 눈으로 바로 확인하려고, `ocr.ts`(`runOcr`)가 실제로 인식에 넘긴 블록/열 경계(`getLastExtractionBlocks`)를 `extractionCache.ts`가 `import.meta.env.DEV`일 때만 읽어 words 와 동일한 물리 픽셀→DIP 보정을 거쳐 오버레이로 통지한다(신규 IPC `DEBUG_BLOCKS`, `windows.ts: sendDebugBlocks`, `preload: onDebugBlocks`). `Overlay.tsx`가 노란 점선 반투명 사각형(`.debug-block`, `pointer-events: none`)으로 그린다. 순수 JS/React + 이미 크로스플랫폼인 `alignWordsToOverlay` 재사용이라 OS 분기 자체가 없음 — 프로덕션 빌드에서는 메인이 이벤트를 아예 안 보내 자동으로 비활성화됨.
- [x] hover 시 커서 모양 변경 — **데스크톱 커서 모양만 구현**, 실제 단어 bbox 기준으로 동작(`windows.ts: setOverlayInteractive`가 실제 텍스트 위에서만 클릭스루 해제 → CSS `cursor: pointer` 반영 + 보라 박스 표시). ~~(확장) 단어 사각형 하이라이트~~는 PLAN.md상 브라우저 확장(extension/) 쪽 기능인데, 확장이 아직 전혀 구현 안 된 상태(native messaging 등 기반 자체가 없음)라 의도적으로 건너뜀 — 확장 작업 시작할 때 별도로 처리 필요.
- [x] 단어 클릭 감지 → 팝업 트리거 (팝업 직전까지가 A 경계) — `Overlay.tsx`가 선택 모드 동안 오버레이를 인터랙티브 상태로 두고(현재는 텍스트 위에서만, 위 항목 참고) 클릭 시 `extractSelection(point)` 호출, `ipc.ts`의 `SELECTION_EXTRACTED` 핸들러가 결과로 바로 `createPopupWindow()`를 호출해 팝업을 연다.
- [x] 산출: 근방 추출 텍스트 + 단어 좌표 + 클릭 기준점을 B로 전달 (최종 선택 확정은 B가 팝업에서 수행) — `runSelectionPipeline`(`selection/index.ts`)이 캐시된 추출 결과에서 클릭 좌표에 해당하는 단어를 찾아 `ExtractedSelection`(`text`+`anchor`+`words`+`source`+`extraction`)을 만들어 B(`PopupScreen.tsx`)로 전달. 단, 앞뒤 문맥(`text`) 자체는 페이지 전체가 아니라 OCR/캡처 단위(현재 화면에 보이는 범위)로 한정됨 — 스크롤되어 화면 밖에 있는 텍스트는 애초에 캡처되지 않아 문맥에 포함 안 됨.

<a id="a-extract"></a>

**추출 판정 · 실행**
- [x] ~~OCR 사용 여부 판정~~ → **구현은 됐지만 지금 파이프라인에서는 미사용으로 보류**(아래 이유). `decideOcr.ts`의 접근성 텍스트 읽기(`accessibility.ts: readWindowText`)는 **Windows 전용, macOS 미구현** — 함수 자체에 `process.platform !== 'win32'` 가드가 있어 macOS에서는 항상 null 반환(항상 OCR로 폴백하는 셈). `decideOcr.ts`(`decideExtraction`)가 접근성 텍스트(표준 Edit/RichEdit 컨트롤)로 direct vs OCR을 판정하고 `extractDirect.ts`가 direct 추출을 구현해뒀는데, direct 추출은 화면 좌표(bbox)를 만들 수 없어서 "클릭한 단어 기준 앞뒤 범위만 팝업에 표시"하는 지금 UX(좌표 필수)와 근본적으로 안 맞는다 — 좌표 때문에 결국 항상 OCR을 돌려야 해서 direct 판정 자체가 무의미해짐. `extractionCache.ts`는 이 둘을 호출하지 않고 항상 OCR만 쓴다. 나중에 "OCR 좌표 + 접근성 텍스트로 내용만 교체"하는 하이브리드로 갈 수도 있지만 두 추출 결과를 매칭시켜야 해서 별도 작업 필요.
- [x] **(2026-07-29) 유튜브/넷플릭스 브라우저를 선택해도 가끔 OCR "영역 지정" 배너가 뜨는 레이스 수정** — 선택 모드 진입 직후엔 확장의 첫 `activeTab` 보고(WS 핸드셰이크)가 아직 도착하기 전일 수 있는데, 이 짧은 순간에 `decideExtraction()`(`decideOcr.ts`)이 판정하면 `getBrowserSource()`가 null이라 "브라우저 아님"으로 오판해 OCR 경로로 빠지고 배너가 잠깐 떴다 사라졌다(사용자 피드백 — "가끔씩 레이스 컨디션 때문에 그런 거 같다"). 선택된 창이 크롬인 게 이미 확실하면(`selectedWindowIsChrome`) `getBrowserSource()`가 비어 있어도 바로 포기하지 않고, 확장의 다음 `activeTab` 보고(`activeTabTracker`의 `change` 이벤트)를 최대 1.2초까지 기다렸다 재확인하도록 `waitForBrowserSource` 추가. 확장 자체가 연결 안 돼 있으면(`extensionBridge.isConnected()`) 영원히 안 올 보고를 기다리는 셈이라 매번 크롬 창 선택마다 불필요한 지연이 생기므로, 연결 여부부터 확인해 미연결이면 기다리지 않고 바로 포기(기존 동작 그대로)한다.
- [x] 판정 캐싱: 모드 진입 시 1회 — `extractionCache.ts`. **캐싱 로직 자체(단일 슬롯, 무효화 시점 등)는 플랫폼 무관**하지만, 실제로 캐시를 채우는 `captureFocusedWindow()`(`capture.ts`)가 Windows 전용이라(비-win32에서 예외 throw) **지금은 사실상 Windows 전용으로만 동작**함 — macOS 캡처 구현되면 이 캐싱 로직은 그대로 재사용 가능. 클릭마다 캡처+OCR을 새로 돌리면 매번 1-3초씩 걸려서, 선택 모드 진입(`shortcut.ts: toggleMode`) 시 미리 캡처+OCR 해 캐시해두고 클릭 시엔 캐시를 즉시 사용하도록 변경. 선택 모드를 나갔다 다시 들어올 때마다 항상 새로 캡처+OCR 한다(그 사이 스크롤 등으로 내용이 바뀌었을 수 있어서 "재진입 = 최신화"로 결정). 창 재선택/선택 해제 시 캐시 무효화(`ipc.ts`/`tray.ts`). ~~URL 키~~ 방식이 아니라 "현재 선택된 창 1개"만 캐시하는 단일 슬롯 구조로 구현(URL 기반 캐싱은 브라우저 확장 경로가 생기면 별도 추가 필요). 캐시가 준비되면 오버레이로도 단어 bbox를 통지(`windows.ts: sendOverlayWords`)해서 hover/클릭이 실제 텍스트 위에서만 되게 함(`Overlay.tsx`). 선택 모드 진입 직후부터(클릭 시가 아니라) 캐시가 준비될 때까지 오버레이 상단에 스피너+"텍스트 추출 중…" 배너를 표시(`Overlay.tsx: extracting` 상태, **플랫폼 무관** — 순수 렌더러 UI). 실패 시에도 빈 배열을 통지해 배너가 안 멈추게 처리.
- [x] **(2026-07-29) 직전 회차 추출 결과 캐시(회차 히스토리) — Windows/macOS 공통(순수 JS 로직, OS 분기 없음)**. `extractionCache.ts`: 매 회차 추출(`refreshExtractionCache`)이 성공하면 그 직전까지 `cached`였던 결과를 `previousExtraction`으로 한 칸 밀어두는 방식으로 **항상 "직전 1회차"만** 보관(예: 3번째 추출이 끝나면 1번째 기록은 버려지고 2번째 기록으로 교체) — 별도 배열/큐 없이 슬롯 하나만 교체. 저장 형식은 팝업 본문에 실제 쓰이는 것과 동일한 `CachedExtraction.text`(OCR 원문, 별도 가공 없음). 선택된 창을 전환/해제할 때만(`ipc.ts: SELECT_WINDOW`, `tray.ts: deselectWindow` — 대상 창이 닫혀 자동 해제되는 경우도 `deselectWindow` 경유라 포함됨) `clearExtractionHistory()`로 비운다 — 창 리사이즈·영역 재선택처럼 "같은 창 안에서" 재추출되는 경우(`invalidateExtractionCache()`만 호출되는 지점들)는 회차 히스토리를 건드리지 않아 문맥이 계속 이어짐.
  - [x] **(2026-07-29) 클릭 시 직전 회차 문맥 병합** — `selection/index.ts: runSelectionPipeline`이 클릭 좌표로 anchor(줄 범위)를 구한 뒤 `extractionCache.ts: mergeWithPreviousContext()`를 호출: anchor가 속한 줄을 `previousExtraction.text`에서 정확히 한 번만 찾을 수 있으면(모호하거나 못 찾으면 안전하게 병합 포기) 그 지점을 기준으로 앞/뒤 중 이전 회차 쪽이 더 긴 방향만 그 텍스트로 교체해 `ExtractedSelection.text`/`anchor`에 반영. 이 하나의 병합이 팝업 표시 문맥(줄 단위, `popup/selection.ts: buildDisplayText`)과 LLM 문맥(바이트 단위, `question/llm/adapter.ts: buildContextBlock`) 모두에 자동으로 반영됨 — 둘 다 결국 `ExtractedSelection.text`/`anchor`에서 앞뒤로 잘라 쓰는 구조라서 별도 연동 불필요.
- [x] OCR 대상 영역 지정 — 창 전체를 OCR 하지 않고 사용자가 드래그로 지정한 영역만 인식하도록 변경(`regionSelection.ts` + `ocr.ts: runOcr` 의 Tesseract `rectangle` 옵션). 이걸로 "메뉴바·상태표시줄이 같이 인식되는" 문제를 위치 휴리스틱(`removeNoise`) 없이도 근본적으로 피할 수 있음(사용자가 처음부터 본문만 감싸면 됨). 창당 영역은 한 번만 지정하고 재사용(`shortcut.ts: toggleMode` — 영역 있으면 바로 OCR, 없으면 `REGION_SELECTION_NEEDED` 로 오버레이에 드래그 요청). 드래그 중엔 캡처 도구처럼 화면 전체를 어둡게 하고 드래그 사각형 안만 원래 밝기로 보이게 함(`Overlay.tsx: .region-dim`, box-shadow 스포트라이트 트릭). 이 어두운 레이어가 오버레이의 모드 테두리(outline)까지 덮어버려서, 같은 색 테두리를 DOM 순서상 맨 뒤(=항상 맨 위)에 `border`로 한 번 더 그려 덮어씌움(`Overlay.tsx: .region-border-topcoat`) — 인셋을 미세 조정해 맞추는 방식은 서브픽셀 반올림 오차로 흰 틈/겹침이 생겨 폐기하고 이 방식으로 정착. 오버레이 창 자체는 `resizable: false`/`movable: false`로 고정(영역 드래그 중 오버레이가 완전 인터랙티브 상태가 되는데, 이때 대상 창 가장자리를 드래그하면 오버레이 자신이 OS 리사이즈로 잡혀 대상 창과 따로 노는 문제가 있었음). 드래그 완료 직후 브라우저가 자동으로 발생시키는 click 이벤트를 단어 클릭으로 오인해 팝업이 뜨는 문제가 있어 ref 로 한 번 소비하도록 처리(`Overlay.tsx: justSubmittedRegionRef`). 창 크기가 바뀌면 이전 영역 좌표가 안 맞으므로 자동 무효화(영역 + 캐시된 단어를 비워 박스가 더 이상 안 뜨게 함) — **선택 모드 자체는 유지**하고, 오버레이 배너로 안내하면서 바로 영역 재선택 드래그 모드로 자동 진입(`windows.ts: onWindowResized`, 크기 변화만 감지해 위치 이동과 구분; `shortcut.ts` — 별도 트레이 메뉴 없이 자동 트리거). 단어 hover/클릭은 영역 재선택 중엔 비활성화됨. 화면 내용이 바뀌는 걸 감지해 영역 안에서만 자동 재추출하는 기능(바로 아래 항목)의 "어디를 감시할지" 기준이 됨. → **(2026-07-29) 드래그 대기 배너가 떠 있을 때 Esc 로 선택 모드 자체를 빠져나가게 추가**: 오버레이 창은 `focusable: false`라 렌더러가 keydown 을 직접 받을 수 없어(`windows.ts: ensureOverlayWindow`), 배너가 뜨는 두 진입점(`shortcut.ts: acquireRegionManually`/`acquireRegionAutomaticallyOrAskDrag`의 드래그 폴백)을 감싼 `requestRegionSelection()`이 그 순간에만 전역 `globalShortcut.register('Escape', resetToNormalMode)`를 임시로 걸고, 드래그 완료(`ipc.ts: SUBMIT_REGION`)·모드 이탈(`toggleMode`/`resetToNormalMode`) 시 `clearRegionEscapeShortcut()`으로 바로 해제한다 — 사용자가 설정하는 영구 단축키가 아니라 배너가 떠 있는 좁은 구간에서만 등록됐다 풀리는 임시 훅이라 평소 다른 앱의 Esc 사용과 충돌하지 않는다.
- [x] **(2026-07-29) 텍스트 영역 자동 탐지 설정 토글 — Windows/macOS 공통**(순수 JS/Electron 로직, OS 분기 없음 — 기존에도 크로스플랫폼이던 `autoDetectRegion()` DocLayout 호출 자체를 그대로 재사용). 설정 화면에 `autoDetectRegion`(기본 OFF) 토글 추가(`SettingsScreen.tsx`, `AppSettings`/`settingsStore.ts`). **OFF(기본값)**: 선택 모드 진입/리사이즈 시 항상 드래그 수동 선택으로 직행(`shortcut.ts: acquireRegionManually`), 화면 내용 변화 감지(`changeWatcher.ts`)도 영역 재탐지 없이 고정 영역 안에서만 재추출. **ON**: 기존처럼 DocLayout 자동 탐지를 먼저 시도하고 실패하면 드래그로 폴백(`acquireRegionAutomaticallyOrAskDrag`), 영역이 없을 때만 분기하므로 한 번 잡힌 영역(수동이든 자동이든)은 모드를 나갔다 다시 들어와도 재사용됨(리사이즈/재선택으로 무효화되기 전까지) — 재탐지가 필요한 시점(영역 없음)엔 항상 이 시점의 설정값이 우선한다. 자동 탐지 결과가 마음에 안 들면 선택 모드에서만 노출되는 트레이 메뉴 "영역 수동 선택"(`tray.ts`, `shortcut.ts: requestManualRegionSelection`)으로 강제로 드래그 선택으로 덮어쓸 수 있음 — 이 경우 설정은 ON으로 남아있어도 그 사이클은 수동 취급된다(`regionSelection.ts`: region 출처 `auto`/`manual` 추적, `getRegionSource`). 드래그 대기 화면은 십자가 커서(`.region-select-cursor`)와 통일된 안내 문구("선택된 영역에 한하여 화면 변화를 감지하고 텍스트를 추출합니다.")를 모든 진입 경로(모드 진입/리사이즈/트레이 수동 선택)에서 공통으로 보여줌(`Overlay.tsx`). 기존에 개발 전용이던 OCR 블록 시각화(`DEBUG_BLOCKS`)를 자동 탐지 결과 확인용 실사용 기능으로 일반화 — region 출처가 `auto`일 때만 오버레이에 반투명 사각형으로 표시되고, 모드 전환 단축키로 나가면 사라짐(`extractionCache.ts`, 개발 모드에서는 기존처럼 항상 표시). 추출 중 배너를 2단계로 분리 — "언어 감지 & 텍스트 영역 탐지 중…"(모드 진입/리사이즈/재추출 시작 시) → "텍스트 추출 중…"(언어 감지 완료 후 실제 OCR 시작 시, 신규 IPC `EXTRACTION_OCR_STARTED`).
  - [x] ~~**(2026-07-29) 자동 탐지 블록이 떠 있을 때 모드 전환 단축키 첫 누름은 블록만 지우게 수정** — 사용자 피드백: 블록을 보다가 무심코 단축키를 누르면 블록도 사라지고 모드도 같이 바뀌어버려 불편함. `windows.ts`에 `debugBlocksVisible` 플래그 추가(`sendDebugBlocks`가 갱신) + `consumeDebugBlocksVisible()`(블록이 떠 있으면 지우고 true 반환) 추가, `shortcut.ts: toggleMode()` 맨 앞에서 이걸 체크해 블록이 있었으면 그것만 지우고 리턴(모드 그대로), 없으면 평소처럼 바로 토글.~~ → **규칙 제거(2026-07-30, 사용자 결정 — "모드 전환 버튼은 무조건 모드 전환만")**: 개발 모드에선 매 추출마다 블록이 항상 다시 떠서(`extractionCache.ts` `import.meta.env.DEV` 분기) + changeWatcher 재추출마다 재전송돼서, 화면이 자주 바뀌는 콘텐츠에선 단축키를 누를 때마다 "블록 지우기"로만 소비돼 사실상 선택 모드에서 못 빠져나오는 문제("모드 전환하면 영역만 해제되고 테두리는 보라색 유지")가 실사용에서 확인됨 — `consumeDebugBlocksVisible`/`debugBlocksVisible` 삭제, 단축키는 항상 즉시 모드 전환.
- [x] 영역 경계에서 잘린 단어 제외 — 영역 지정에 의존(이제 Windows/macOS 공통). `ocr.ts`: 영역 안에서 텍스트 일부만 보이는 단어가 여전히 hover/클릭 가능한 문제를 세 가지 서로 다른 판정으로 잡는다. 1) `isWordClippedByRegion` — 단어 bbox 가 지정 영역 경계(Tesseract `rectangle` 크롭 경계)에 거의 붙어있으면 제외. 사용자가 영역을 여백 없이 딱 맞춰 잡은 경우엔 잘 잡지만, 영역을 넉넉하게(여백 포함) 잡으면 실제 잘림이 화면 안쪽(예: 원본 앱의 스크롤 패널 경계)에서 일어나 이 방식만으론 못 잡는 사례가 있어 아래 두 방식을 추가. 2) `looksTruncated` — 인식 신뢰도(Tesseract `confidence`) 기반, 단어 평균이 `MIN_WORD_CONFIDENCE`(90) 미만이거나 글자 하나라도 `MIN_SYMBOL_CONFIDENCE`(75) 미만이면 제외(위치 무관, 실사용 확인하며 임계값을 55→75→90 으로 계속 올림 — 정상 단어 오탐 없이 잘린 단어를 더 잡아내는 방향으로 튜닝 중). 3) `findEdgeClippedLines` — 영역의 맨 위/아래 줄만 따로 확인해, 그 줄의 bbox 높이가 나머지 줄 높이 중앙값의 `EDGE_LINE_HEIGHT_RATIO`(0.95) 미만이면(경계가 줄 중간을 가로질러 위/아래 절반만 보임) 그 줄 전체를 통째로 제외. 비교 기준이 될 다른 줄이 최소 2개는 있어야 해서 전체 줄 수가 3개 미만이면 이 판정은 건너뜀. 세 판정 모두 오탐(정상 단어가 같이 빠짐) 위험을 감수하고 "잘린 텍스트는 아예 안 보이게"를 우선하는 방향으로 잡음 — 실사용하며 임계값 조정 중.
- [x] 영역 내용 변화 감지 → 자동 재추출 — 영역 지정에 동반 제약(이제 Windows/macOS 공통). `changeWatcher.ts`: 선택 모드에서 영역이 확정돼 있는 동안 500ms 마다 영역만 크롭 캡처해 직전 캡처와 픽셀(RGBA 중 R 채널) 비교, 다른 비율이 임계값(2%)을 넘으면 "변화"로 판정. 스크롤처럼 계속 움직이는 동안엔 재추출을 걸지 않기 위해, 변화가 감지될 때마다 800ms 대기 타이머를 리셋하고 그 시간 동안 추가 변화가 없어야 실제로 재추출한다(디바운스). 창 크기 변경 이벤트는 명시적으로 다루지 않음 — 그건 `onWindowResized`가 이미 영역을 무효화하고 재선택을 요구하는 별도 경로라 겹치지 않게 분리했고, 리사이즈 시 이 워처도 함께 멈춘다(`shortcut.ts: onWindowResized`). 재추출은 `extractionCache.ts: refreshExtractionCache()`를 그대로 재사용 — 이미 "최신 호출의 inFlight promise만 캐시에 반영" 구조라 재추출 도중 또 변화가 감지돼도 이전 추출 결과는 자연히 버려지고 최신 것만 반영됨(별도 취소 로직 불필요). 변화가 인식되는 즉시 기존 단어 박스를 비우고(`sendOverlayWords([])`), 디바운스가 끝나 실제 재추출이 시작되면 초기 진입 때와 같은 "텍스트 추출 중…" 배너를 띄운다(`EXTRACTION_STARTED` 채널, `Overlay.tsx`) — 완료되면 `EXTRACTION_WORDS`가 배너를 끄고 새 박스로 채운다. 시작/정지는 영역이 확정되는 시점(`shortcut.ts: toggleMode`의 기존 영역 재사용 분기, `ipc.ts: SUBMIT_REGION`)과 영역이 사라지는 시점(모드 이탈, 리사이즈, 창 재선택)에 맞춰 호출.
  - [x] **(2026-07-29) 스크롤/클릭/키보드 입력으로만 재추출 트리거 — Windows 전용, macOS 미구현(기존 동작 그대로 유지됨).** 픽셀 diff 만으로는 브라우저 hover 효과(밑줄, 툴팁 등)에도 재추출이 걸릴 만큼 민감했다(사용자 보고) — `inputHook.ts`(신규)가 win32 저수준 전역 후크(`SetWindowsHookEx`, `WH_KEYBOARD_LL`/`WH_MOUSE_LL`)로 마우스 클릭(좌/우/휠클릭)·스크롤 휠·키 입력(keydown)이 마지막으로 있었던 시각을 기록하고(마우스 **이동**은 명시적으로 제외 — hover 의 원인이라 뺐다), `changeWatcher.ts`가 diff 감지 시 그 시각이 `INPUT_FRESHNESS_MS`(1500ms) 이내여야만 진짜 변화로 인정한다. **크로스플랫폼 처리**: `win32Capture.ts` 와 동일한 패턴(`process.platform === 'win32'` 가드 뒤 동적 `import()`)으로 macOS에서는 이 파일 자체가 안 불려서 koffi 의 win32 DLL 로드 자체가 안 일어난다 — `changeWatcher.ts`의 게이트 함수(`getLastInputTime`)는 win32 가 아니면 항상 "방금 입력 있었음"을 반환해 필터링 없이 기존 픽셀 diff 단독 판정으로 그대로 동작한다(새 기능이 없을 뿐 기존 동작 저하 없음). macOS 대응은 CGEventTap(손쉬운 사용 권한 필요) 별도 구현 필요(미착수). 저수준 후크는 콜백이 무거우면 Windows 가 강제로 훅을 해제하므로 타임스탬프 기록만 하는 최소 작업만 수행하도록 주의해서 구현함. → **macOS 대응 완료(2026-07-29, 코드 감사 후속 — "이거 맥도 똑같이 되게 고쳐줘")**: CGEventTap(손쉬운 사용 권한 필요) 대신 훨씬 가벼운 공개 API `CGEventSourceSecondsSinceLastEventType`을 쓰는 `macInput.ts`(신규)로 구현 — 이벤트를 실제로 가로채는 게 아니라 "이 타입 이벤트가 마지막으로 언제 있었는지"를 그때그때 질의만 하는 방식이라 권한도 이벤트 탭 설치도 필요 없다(스크린세이버 등이 흔히 쓰는 표준 유휴시간 API). 좌/우/중간 클릭·스크롤·키다운 각각의 "마지막으로부터 경과 초"를 물어 가장 최근값을 채택. `changeWatcher.ts`의 `getLastInputTime`이 win32/macOS 를 각각 `inputHook.ts`/`macInput.ts`로 분기하도록 수정.
- [x] ~~창 재선택 시 선택 모드 자동 해제 — `shortcut.ts: resetToNormalMode()`, `ipc.ts`의 `SELECT_WINDOW` 핸들러에서 호출. **플랫폼 무관**(네이티브 API 없이 모드 상태만 토글) — Windows/macOS 둘 다 동작.~~
- [x] **DOM 텍스트(일반 웹페이지) 직접 추출 — 담당: milleion.** 브라우저 확장(`extension/`)에서 유튜브/넷플릭스 자막과 같은 자리(A 영역, PLAN.md §5.1·§9 "DOM 텍스트(일반 웹페이지)는 A 영역으로 후속 예정")에 신규 모듈로 구현 예정. **설계 방향(2026-07-29 논의로 확정)** — 사이트별 URL 등록·셀렉터 목록 방식이 아니라 **범용 본문 탐지 알고리즘**(Mozilla Readability.js와 같은 발상: 컨테이너 후보를 텍스트 길이·문단 수·링크 밀도로 스코어링해 최고점 블록을 본문으로 판정)을 확장에 하나만 구현해 URL 등록 없이 임의의 텍스트 위주 사이트에 적용한다. FictionPress/RoyalRoad/소설가가 되자/qidian(웹소설)과 読売新聞/CNN/人民网(뉴스) 총 7개 사이트를 실측한 결과, 대부분 "본문 블록 하나에 `<p>` 밀집 + 주변 UI 대비 압도적 텍스트 밀도"라는 공통 구조를 가져 이 방식이 통할 근거를 확인했다(단, 실제 DOM 구조 확인은 curl 기준이라 fictionpress.com·qidian.com은 Cloudflare 챌린지/봇 감지로 서버사이드 fetch가 막혀 있었음 — 확장은 사용자의 실제 브라우저 세션 안에서 렌더링 완료 후 DOM을 읽으므로 이 문제 자체가 없을 가능성이 높지만, 실제 브라우저 로드 상태로 재확인 필요).
  - 알고리즘이 반드시 처리해야 하는 함정(실측으로 확인됨): (1) **보이지 않는 가짜 텍스트** — RoyalRoad는 `display:none` CSS로 숨긴 `<span>`(저작권 경고 문구)을 본문 문단 중간에 anti-scraping 함정으로 심어둠 → 텍스트 노드 단위로 `getComputedStyle(el).display !== 'none'` 체크 필수(단순 태그 구조 스코어링만으론 안 걸러짐). (2) **후리가나** — `<rt>/<rp>` 제외, 기존 `extension/src/domWords.ts`의 `isFuriganaText`/`elementTextExcludingFurigana` 패턴 그대로 재사용 가능. (3) **캡션/광고 혼입** — `<figcaption>`, 광고 class 패턴 등은 본문 스코어링에서 배제.
  - **OCR 판정 연결**: 후보 블록의 총 텍스트 길이가 임계값 이상이면 direct 추출로 판정, 못 찾으면 기존 OCR 판정 경로(§4.1)로 자연 폴백.
  - **폰트/글리프 난독화 감지(起点 VIP 챕터 등)는 나머지 구현 완료 후 별도로 추가** — 사후 검증 레이어라 앞단(본문 탐지·추출)과 독립적으로 나중에 끼워 넣어도 기존 로직 변경이 필요 없음(판정 함수 하나 추가 + OCR 폴백 분기만 얹으면 됨). 순환 논리 주의: "추출된 텍스트로 언어 탐지 후 예상 언어와 비교"는 예상 언어를 추출 결과 자체에서 얻어야 해서 순환이 생기므로 안 됨 — 대신 (a) PUA(Private Use Area, U+E000~U+F8FF) 코드포인트 비율(언어 무관 독립 신호), (b) 페이지가 스스로 선언하는 `<html lang="...">`(추출 결과와 무관하게 사전에 얻을 수 있는 진짜 "예상 언어") 두 가지를 판정 근거로 쓴다.
  - **(2026-07-30) 알려진 한계 — `<p>` 태그 의존이라 컴포넌트 기반 SPA에서는 구조적으로 안 통할 수 있음, 나중에 다룰 것.** `findMainContent()`/`visibleParagraphs()`가 후보 컨테이너를 볼 때 `<p>` 태그 3개 이상(`MIN_PARAGRAPHS_FOR_CANDIDATE`)만 후보로 삼는데, 유튜브 홈처럼 Polymer/Lit 등 웹 컴포넌트 기반 SPA는 영상 제목·설명 등을 `<p>`가 아니라 `<yt-formatted-string>`/`<span>`/`<a>` 같은 커스텀·비-`<p>` 엘리먼트로 렌더링한다(사용자 확인 질문: "유튜브 홈 여기 걸려?" — 확인 결과 안 걸림, `MIN_WEB_TEXT_LENGTH` 문턱이 아니라 그보다 앞단에서 `<p>` 자체가 없어 `findMainContent()`가 `null`을 반환해 걸러짐). 지금까지 실측한 사이트(뉴스·웹소설)는 전부 전통적 CMS라 `<p>`를 쓰고 있어 이 한계가 안 드러났을 뿐 — 이런 SPA에 실제로 읽을 만한 긴 텍스트(영상 설명란·댓글 등)가 있어도 지금 알고리즘은 못 찾고 무조건 OCR로 간다. 당장은 손 안 대고 실사용하다 필요해지면 `<p>` 외에 텍스트 밀집 `<div>`/`<span>` 블록도 후보로 보는 등으로 확장하기로 함(사용자 결정, 2026-07-30 — "일단 문서에만 적어두고 나중에").
  - **(2026-07-30) `MIN_WEB_TEXT_LENGTH=300` 실증 안 된 추정치임을 재확인.** 지금까지 실측한 모든 사이트가 문단 하나만 해도 300자를 훌쩍 넘어 이 임계값이 실제로 판정을 가른 적이 없다 — 순전히 추정치인 채로 미검증. `findMainContent()`가 문단 3개 이상 구조 게이트를 이미 갖고 있어서(위 항목), 임계값이 낮아도 쓰레기 텍스트가 바로 통과할 위험은 어느 정도 막혀 있음 — 오히려 너무 높게 잡아서 짧지만 진짜인 본문(한 문단짜리 기사 등)을 OCR로 잘못 떨어뜨릴 위험 쪽이 더 걱정된다고 판단(200 정도로 낮추는 것도 안전할 것으로 추정). 실사용하며 "OCR로 잘못 떨어지는 짧은 본문 페이지"가 실제로 관찰되면 그때 조정하기로 함.
  - **(2026-07-30) 자동판정 결과를 수동으로 OCR로 강제 전환하는 기능 추가.** 자막/웹 direct 추출이 자동으로 됐지만 결과가 마음에 안 들 때(예: 위 `<p>` 한계·임계값 오판 등으로 이상하게 추출됐을 때) 사용자가 수동 개입할 방법이 그동안 없었다(사용자 확인 질문으로 발견: "300자 넘으면 무조건 텍스트 추출만 가능하고 수동으로 OCR 돌릴 수는 없지?" — 맞음, 없었음). 트레이 메뉴 "OCR로 전환"(기본 `Alt+4`/macOS `Option+4`, 기존 "영역 수동 선택"(`Alt+3`)과 동일한 namedShortcut 레지스트리·설정 화면 UI 패턴 재사용)을 추가 — `shortcut.ts: requestForceOcr()`가 자막·웹 두 direct 추출 경로 전부에서 즉시 OCR로 전환한다(사용자 요청: "텍스트를 추출하는 모든 경로에서"). `forceOcrUrl`로 범위를 추적해 **같은 페이지(URL)에서만 유지**되고, 다른 페이지로 이동하면 다음 재판정에서 자동 해제, 선택 모드를 나갔다 다시 들어가면 처음부터 다시 자동판정한다(사용자 결정). 트레이 메뉴 항목 자체는 실제로 자막/웹 모드가 활성 중일 때만 노출(이미 OCR이면 뜻이 없으므로).
  - **(2026-07-30) 구현 완료.** 유튜브/넷플릭스 자막 경로(`subtitleSource.ts`+`extension/src/highlight.ts` — 확장이 페이지 안에서 hover/클릭을 직접 처리하고, 좌표 없이 클릭된 텍스트만 앱에 보내면 앱이 `ExtractedSelection`을 직접 조립해 팝업을 여는 구조)를 그대로 복제했다 — `runSelectionPipeline`/`extractionCache`/`wordMapping`(좌표 기반 OCR 캡처 전제) 은 전혀 손대지 않음.
    - **확장(`extension/src/`)**: `webArticle.ts`(신규) — 사이트별 셀렉터 없이 텍스트 밀도 스코어링(`article`/`main`/`div`/`section` 후보 중 문단 텍스트 길이 − 링크 밀도 페널티가 최고인 컨테이너 채택)으로 본문을 찾고, RoyalRoad 실측(2026-07-29)에서 확인된 `display:none` 함정 문단을 문단 내부 텍스트 노드 단위로 걸러낸다(`isHiddenWithinRoot`) + 후리가나(`<rt>/<rp>`) 제외. `articleHighlight.ts`(신규) — `highlight.ts`(자막)와 달리 문단이 수백 개일 수 있어 `document.elementFromPoint()`로 문단만 싸게 찾고 그 문단 하나에 대해서만 `wordsInElement()`(domWords.ts, 원래도 범용 유틸이라 그대로 재사용)를 지연 계산+캐시한다. `content.ts`는 캡처 시작(`setPageCapture`) 시 1회만 탐지+조립하고(자막처럼 매 프레임 재추출 안 함) 결과 길이를 `pageReady`로 항상 보고, 클릭은 `pageClick`(본문 전체 텍스트+클릭 지점 절대 오프셋 — 확장이 조립 시점에 이미 오프셋을 알고 있어 `subtitleSource.ts`의 `anchorInTranscript` 같은 서버 측 텍스트 재검색이 불필요)으로 전송. `background.ts`는 자막 캡처와 독립된 상태(`pageCaptureDesired`/`pageCapturedTabId`)로 on/off 추적.
    - **공유 프로토콜(`src/shared/extension.ts`)**: `ExtToApp`에 `pageReady`/`pageClick`, `AppToExt`에 `setPageCapture` 추가. `MIN_WEB_TEXT_LENGTH`(=300, 텍스트 위주 사이트 판정 임계값)도 여기 둠 — 확장(웹워커, DOM 의존)과 메인(Node)이 같은 값을 봐야 하는데 확장 전용 파일에서 메인이 직접 import할 수 없어서(DOM API 참조) 공유 프로토콜 파일로 이동.
    - **메인 프로세스**: `bridge.ts`에 `pageReady`/`pageClick` 이벤트·`setPageCapture()` 추가(재접속 시 desired 상태 재통지도 자막과 동일 패턴). 신규 `selection/webSource.ts`(`subtitleSource.ts` 그대로 본뜸) — `startWebMode(onInsufficientText)`가 확장에 캡처를 요청하고 `pageReady` 응답을 최대 1.2초(`decideOcr.ts`의 `waitForBrowserSource`와 동일한 "요청 후 타임아웃까지 대기" idiom, 새 WS 요청/응답 프로토콜을 새로 만들지 않음) 기다려 텍스트가 충분하면 `pageClick` 리스너를 켜고, 부족/타임아웃이면 콜백으로 기존 OCR 흐름에 위임한다. 재진입 시 낡은 대기가 새 판정을 덮어쓰지 않도록 `shortcut.ts`의 `decisionEpoch`와 동일한 세대(epoch) 가드를 둠.
    - **(2026-07-30 후속) 웹 문단 hover에 CJK 형태소 분석 결과 연결.** 위 구현 직후엔 일반 웹페이지 hover 박스가 일본어/중국어에서도 글자 단위로만 잡혔다(자막 경로는 이미 형태소 단위였음) — `wordSegments.ts`(신규)로 hover 하이라이트(`highlight.ts`, 자막)와 웹 문단(`articleHighlight.ts`)이 형태소 분석 결과 캐시를 공유하도록 공통화했다. 문단에 hover가 들어가면 `content.ts: requestParagraphSegments()`가 `pageParagraphText` 메시지로 분석을 요청하고, `bridge.ts`는 자막용 `requestWordSegments()`를 그대로 재사용(dedup·CJK 판정 로직 신규 작성 없음 — `detectCjkVariant`로 ja/zh만 게이팅되는 것도 자막과 동일, tier2로 확장된 건 아님). 응답은 기존 `wordSegments` 메시지로 오고, hover/클릭 양쪽에서 같은 세그먼트를 하나의 단어로 묶어 보여준다(클릭은 항상 그 순간 라이브 문단 텍스트 기준으로 재계산).
    - **판정 연결**: `decideOcr.ts`가 비미디어 브라우저 페이지를 더 이상 곧장 `mode:'ocr'`로 폴백하지 않고 `mode:'web'`로 낙관적으로 판정(실제 충분 여부는 `startWebMode`가 확인). `shortcut.ts: applyExtractionDecision`에 `subtitle` 분기와 병렬로 `web` 분기를 추가하고, 텍스트 부족 시 재사용할 OCR 진입 로직을 `startOcrFallback()`으로 추출해 기존 OCR/direct 분기와 공유. 자막 모드가 다른 모드로 전환될 때 선택 모드 자체를 끄던 기존 정책(2026-07-29 결정)을 웹 모드에도 동일 적용, `exitSelectMode`/`resetToNormalMode`/리사이즈 처리 등 자막 모드를 특별 취급하던 지점 전부에 웹 모드도 대칭으로 추가.
    - **타입체크(`npm run typecheck`)·확장 빌드(`npm run build:ext`)·전체 빌드(`npm run build`) 통과 확인.** 실제 브라우저(fictionpress/qidian 포함 실측 사이트 전체) 수동 QA는 미실시 — 다음 단계로 남음(특히 qidian 폰트 난독화 실측은 위 "폰트/글리프 난독화 감지" 항목 착수 여부 판단 근거로 필요).
    - **(2026-07-30) 실사용 QA(RoyalRoad ch.33)로 발견한 버그 5건 수정.**
      1. hover 박스 스크롤 잔류 — 스크롤/리사이즈 시 단어 캐시만 비우고 박스는 안 숨겼던 게 원인. 팝업을 닫은 뒤 마우스를 안 움직이고 스크롤만 하면(휠/트랙패드) 다음 mousemove가 안 와 박스가 옛 위치에 그대로 남았다 — `onViewportChange`가 `hideBox()`도 같이 호출하도록 수정(`articleHighlight.ts`).
      2. **클릭 단어 ≠ 팝업 선택 단어** — 클릭 앵커를 캡처 시작 시점 스냅샷(`fullText`/문단 오프셋) 기준으로 계산했는데, 뉴스·웹소설처럼 클릭까지 시간이 걸리는 동안 페이지가 바뀌면(광고 지연 로드 등) 스냅샷과 클릭 시점 라이브 DOM의 문단 경계가 어긋나 hover 박스는 맞는 단어를 가리키는데 팝업엔 다른 단어가 선택됐다. `resolveClick()`이 클릭 시점에 컨테이너를 다시 통째로 재추출해 텍스트·오프셋을 항상 같은 순간의 라이브 DOM에서 함께 계산하도록 변경(hover 표시용 캐시는 성능을 위해 유지, 클릭 결과 전송에는 더 이상 안 씀).
      3. **`<br>`로만 나뉜 텍스트가 구분자 없이 붙어 나옴**(예: RoyalRoad 챕터 제목 `<strong>Chapter 033<br></strong><strong>Gateways</strong>` → "Chapter 033Gateways") — `visibleParagraphText`가 텍스트 노드만 순회해 `<br>`을 그냥 건너뛰던 게 원인. `SHOW_ELEMENT`도 같이 받아 `<br>`을 만나면 `'\n'`을 끼워 넣도록 수정(`webArticle.ts`).
      4. **일본어/중국어 웹 문단 hover가 형태소 분석 없이 항상 글자 단위** — 세 가지 원인이 겹쳐 있었다. (a) 애초에 자막 경로(`highlight.ts`)만 형태소 분석 결과를 받고 있었어서, `wordSegments.ts`(신규, 자막·웹 문단이 공유하는 세그먼트 캐시)로 뽑아내 `articleHighlight.ts`도 hover 진입 시 CJK 문단이면 `pageParagraphText` 메시지로 분석을 요청하도록 배선(응답은 기존 `wordSegments` 메시지 재사용, bridge.ts의 자막용 `requestWordSegments()`를 그대로 재사용해 새 분석 로직 없음). (b) `background.ts`가 `pageParagraphText` 메시지를 WS로 중계하는 케이스를 빠뜨려서 요청 자체가 앱까지 도달을 못 했음 — 케이스 추가로 해결. (c) (b)를 고친 뒤에도 여전히 안 됐다 — 앱이 보낸 `wordSegments` 응답을 탭에 전달하는 `relayWordSegments()`가 **자막 전용** `capturedTabId`만 확인하고 있었는데, 자막 캡처와 독립적으로 추적되는 웹 페이지 캡처(`pageCapturedTabId`)에서는 `capturedTabId`가 항상 `null`이라 응답이 조기 반환으로 유실됐다 — `capturedTabId ?? pageCapturedTabId`로 수정.
      5. **팝업 문맥 표시가 문장 경계 때문에 과도하게 확장** — "N줄 전/후" 측정 위치가 여러 줄에 걸친 긴 문장(웹소설 산문에 흔함) 중간에 걸리면, 기존 `sentenceStart()`가 그 문장의 시작까지 뒤로 확장해 2줄 요청이 5~6줄로 불어났다(RoyalRoad 실측: "There was no way...exorbitant."라는 긴 문장 중간에 경계가 걸려 그 문장 전체가 앞에 붙어 나옴 — 진단 로그로 원인 확정). 새 `skipPartialSentenceForward()`(`@shared/context.ts`)를 도입해 "뒤로 확장해 문장을 통째로 포함" 대신 "그 문장을 통째로 버리고 다음 문장 시작까지 앞으로 건너뛰기"로 바꿔 창이 커지는 대신 줄어드는 쪽을 택함. 처음엔 `source.kind==='web'`에서만 적용했으나 "N줄 전/후는 소스 무관 공용 규칙인데 왜 웹만 다르게 동작하냐"는 지적(2026-07-30)으로 전체 소스(OCR/자막/웹) 공통 적용으로 수정 — 이미 문장 시작이면 그대로 반환하는 함수라 줄이 문장에 가까운 OCR/자막에는 사실상 no-op.
    - **(2026-07-30) direct 추출(자막/웹) → OCR 필요 페이지 전환 시 자동 선택 해제 복원.** `decideOcr.ts`에 `mode:'web'`을 추가하면서, 브라우저에 있는 한 판정이 항상 `subtitle` 아니면 `web`이 되어 "미디어→비미디어 전환 시 선택 모드 자동 해제"라는 기존 정책(2026-07-29, 유튜브 영상→홈 이동 등)이 그 분기에 더 이상 안 걸리게 됐다 — 조용히 OCR 영역 지정으로 넘어간다(사용자 지적: "자동으로 해제되는 로직 맞냐"). `shortcut.ts: applyExtractionDecision`이 최상단에서 `wasDirectExtraction`(호출 시점에 자막/웹 모드였는지)을 미리 포착해, `web` 분기의 텍스트 부족 콜백과 그 외 분기 양쪽에서 공유하도록 수정 — direct 추출 상태였다가 OCR이 필요한 곳으로 넘어가면 영역 지정 없이 선택 모드를 끄고, 처음부터(fresh 진입) OCR이 필요한 페이지였을 때는 그대로 정상 영역 지정 흐름으로 진입한다.
    - **(2026-07-30) 위 자동 해제가 "같은 web 분류 안" 페이지 이동에서는 전혀 발동 안 하던 문제 추가 수정.** `activeTab.ts`의 `classKey`가 `kind+isMedia`만 봐서, `isMedia:false`인 페이지끼리(웹소설 A→B챕터, 유튜브 홈→구독피드 등) URL이 바뀌어도 같은 분류로 취급돼 `activeTabTracker`의 재판정 트리거(`change` 이벤트) 자체가 안 걸렸다 — 웹 모드가 한 번 켜지면 이후 페이지 이동에서 텍스트 부족 여부를 다시 확인할 길이 없었다(사용자 지적: "텍스트 많은 곳에서 적은 곳으로 이동해도 자동 해제되는 거 맞지?"). `isMedia:false`면 URL까지 키에 포함하도록 수정(media는 기존처럼 URL 무시 유지 — 영상 내부 전환은 확장이 자체 처리). 이것만으론 부족해서 `shortcut.ts`도 같이 수정 — `web` 분기가 이미 활성화된 상태에서 `startWebMode()`를 다시 부르면 내부 `if (active) return` 가드로 조용히 무시돼 새 페이지의 `pageReady`를 다시 안 기다렸다 — `stopWebMode()`를 먼저 호출해 매번 새 페이지 기준으로 재시작하도록 수정.
    - **(2026-07-30) ~~미해결~~ → 해결 — 호버박스가 가리킨 단어와 팝업에 실제 선택되는 단어가 가끔 어긋남.** 위 "클릭 단어 ≠ 팝업 선택 단어"(항목 2, `resolveClick` 라이브 재추출) 수정 이후에도 재발했고 전 언어에서 발생 — 확장 쪽(articleHighlight.ts) 원인 후보 3가지를 세워 진단 로그까지 넣었으나, **진짜 원인은 확장이 아니라 소스 무관 공용 팝업 코드에 있었다**: `253558c`(jossi-jossi, OCR 경로 조사로 발견·수정)의 `lastSpanRef` 표시 좌표 버그 — 팝업이 근사 문맥 창으로 먼저 그렸다가 DOM 측정 창으로 교체될 때 표시 창 시작점(`windowStart`)이 이동하는데, 선택 유지 로직이 표시 좌표를 그대로 기억해 창 이동량만큼 밀린 위치의 엉뚱한 단어를 재선택했다(원문 절대 좌표로 기억하도록 수정, `displayOffsetToAbsolute`). `PopupScreen.tsx`/`popup/selection.ts`는 OCR/자막/웹이 전부 공유하는 코드라 **웹에도 그대로 적용됨** — 오히려 웹은 근사 창이 문단 단위라 측정 창과 가장 크게 어긋나 제일 심하게 겪던 케이스였고, "호버박스 단어가 팝업 문맥 중앙에는 있는데 선택만 다르다"는 관찰과 정확히 부합(창은 anchor 중심으로 잡히니 위치는 맞고 선택만 밀림). 사용자 실사용 확인으로 해결 검증(2026-07-30). 확장 쪽 진단 로그 3곳은 제거. **주의**: 앞서 항목 2에서 "캡처 스냅샷 vs 라이브 DOM 어긋남"으로 원인을 짚었던 건 오진이었을 가능성이 높다 — 다만 그때 도입한 `resolveClick` 라이브 재계산 자체는 실재하는 별개 위험(캡처 후 광고 지연 로드 등으로 문단 경계 이동)을 막는 방어로서 여전히 유효하고, 현재 클릭 앵커 계산의 핵심 경로이므로 유지한다.
    - **(2026-07-30) 웹 모드 팝업이 닫힐 때 브라우저로 포커스가 안 돌아가던 문제 수정.** 자막(subtitle) 경로는 `subtitleSource.ts`가 팝업 `'closed'` 이벤트에 `extensionBridge.focusTab()`을 걸어뒀는데, 웹 경로(`webSource.ts`)엔 이 처리가 아예 없어서 Esc로 팝업을 닫아도 포커스가 어디로 갈지 OS 기본 동작에 맡겨진 상태였다(신뢰 불가 — 특히 macOS는 다음 앱을 임의로 고를 수 있음, 사용자 질문으로 발견: "팝업창이 닫혔을 때 포커싱이 원래 앱으로 돌아가지 않는게 디폴트고 유튭넷플만 예외인거야?"). `onPageClick`도 자막과 동일하게 `focusTab()`을 걸도록 추가(팝업 재사용 시 리스너 중복 등록 방지 가드 포함). 고치는 과정에서 `background.ts: focusCapturedTab()`도 `relayWordSegments`와 같은 패턴의 버그가 있는 걸 추가로 발견 — 자막 전용 `capturedTabId`만 봐서 웹 모드에서는 요청 자체가 조용히 무시되고 있었다(`capturedTabId ?? pageCapturedTabId`로 수정).
    - **(2026-07-30) 뉴스 기사(인민망) 실사용 제보로 발견한 버그 4건 수정.**
      1. **hover 박스가 단어가 아니라 문단 두 줄을 통째로 덮음**(중국어 기사, "当地政府" 호버 시 재현). 근본 원인은 `SubWord`가 `{text, rect}`만 갖고 좌표 정보가 없어서, `articleHighlight.ts`/`highlight.ts`가 매번 `paragraphText.indexOf(word.text, searchFrom)`로 오프셋을 역산하던 것 — 중국어 문장은 반복 글자(的/也/当地/居民 등)가 매우 흔한데, 화면 줄바꿈 경계 등에서 글자 하나의 rect 측정이 실패해 `words` 배열에서 조용히 빠지면 그 뒤로 모든 역산 오프셋이 앞쪽 중복 글자로 미끄러져 문단 뒷부분 전체가 같은(잘못된) 세그먼트로 묶여버렸다. **구조적으로 고침** — `SubWord`에 `start`/`end`(줄/문단 전체 텍스트 기준 절대 오프셋)를 추가하고, `domWords.ts`에 텍스트 조립과 단어 추출을 한 번의 TreeWalker 순회로 함께 하는 `extractWordsAndText()`를 신설해 text와 words[].start/end가 항상 같은 좌표계에서 나오도록 통일 — `paragraphOffsets()`/`wordAtPoint()`의 indexOf 역산 로직 자체를 제거했다(이 버그 유형이 구조적으로 재발 불가능해짐). `wordsInElement`/`elementTextExcludingFurigana`(자막)와 `visibleParagraphText`/`wordsInParagraph`(웹 문단)도 전부 이 함수 하나로 통합.
      2. **팝업 문맥 맨 위에 정체불명의 빈 줄이 뜸**(같은 기사, 첫 문단에 영상 임베드용 `<script>` 태그가 들어있던 케이스) — **부분 원인**(아래 4번이 진짜 주 원인). `webArticle.ts`의 `visibleParagraphText`가 `<script>`/`<style>` 태그를 걸러내지 않아 JS 소스 코드(줄바꿈·들여쓰기 포함)가 본문 텍스트로 그대로 흡수됐다. `extractWordsAndText`(위 1번과 공유)에서 `SCRIPT`/`STYLE`/`NOSCRIPT`/`TEMPLATE` 태그를 `TreeWalker`의 `FILTER_REJECT`로 걸러내도록 수정.
      3. **(위 1번 수정 후에도 재현) hover 박스가 두 줄에 걸친 단어에서는 여전히 두 줄 전체를 덮음 — 진짜 주 원인 확인, 사용자 재현으로 발견.** 1번에서 고친 오프셋 역산 버그는 실재했지만, "两줄에 걸치면 그 두 줄이 통째로 선택된다"는 재현 조건은 더 근본적인 별개 원인이었다: `groupWordAt()`/`wordAtPoint()`가 세그먼트에 속한 글자들의 rect를 `unionRects()` 하나로만 묶는데, 그 글자들이 화면 줄바꿈에 걸치면(예: "当地政府"의 앞부분은 줄1 오른쪽 끝, 뒷부분은 줄2 왼쪽 끝) 최소/최대 x가 양쪽 줄의 여백까지 벌어져 실제 글자 폭과 무관하게 두 줄 전체 너비를 덮는 bounding box가 나온다 — 이 문제는 오프셋이 완벽히 정확해도 rect가 두 줄에 걸치기만 하면 항상 재현되는 결정론적 지오메트리 문제였다. `wordMapping.ts`에 `groupRectsByLine()`(rect들을 y 겹침 기준으로 줄별 클러스터링 후 줄마다 따로 union)을 추가하고, hover 박스도 단일 박스(`box`)가 아니라 줄 개수만큼의 박스 풀(`boxes[]`, `showBoxesAt(rects[])`)로 렌더링하도록 `articleHighlight.ts`/`highlight.ts` 양쪽을 수정 — 이제 두 줄에 걸친 단어는 각 줄에 딱 맞는 박스 두 개로 보인다(텍스트 선택 시 브라우저 네이티브 하이라이트가 여러 줄을 각각 감싸는 것과 동일한 방식).
      4. **(2번 수정 후에도 재현) 팝업 문맥 맨 위 빈 줄 — 진짜 주 원인 확인, 사용자가 브라우저 콘솔로 직접 라이브 DOM을 찍어줘서 발견.** `<script>` 태그 자체를 걸러내도 여전히 재현됐다 — `curl`로 받은 정적 HTML만 보고 진단했더니, 실제 브라우저가 그 `<script>`(영상 플레이어, video.js)를 실행한 뒤에는 같은 `<p>` 안에 video.js가 주입한 접근성 안내문(`"Video Player is loading.Play VideoPlayMuteCurrent"`, `"This is a modal window."` 등, 클래스 `vjs-control-text`)이 실제 텍스트 노드로 들어와 있었다. 이 안내문은 `display:none`/`visibility:hidden`이 아니라 `width:1px; height:1px; overflow:hidden`으로 시각적으로만 숨기는 "스크린리더 전용" 패턴이라, 기존 `isHidden()`(display/visibility만 확인)이 걸러내지 못했다 — 게다가 안내문 텍스트 자체에 개행 문자가 섞여 있어(`"\nVideo Player is loading..."`) 그 개행이 팝업 문맥의 "앞 N줄" 계산에서 빈 줄 하나로 잡혔다. `domWords.ts`의 `isHidden()`에 `getBoundingClientRect().width <= 1 && height <= 1`(1×1px 이하로 접힌 요소는 사실상 안 보임 — Readability.js 등도 쓰는 흔한 휴리스틱) 판정을 추가하고, `webArticle.ts`가 자체 보유하던 중복 `isHidden()`도 이걸로 통합했다.
    - **(2026-07-30) 자막(highlight.ts)-웹 문단(articleHighlight.ts) 로직 통합.** 사용자 지적("자막 부분도 내 담당이니 로직 통합해서 자막도 웹처럼 되게 고쳐") — 앞선 CJK 그루핑 실패 조사 과정에서 자막 경로가 웹 경로에만 있던 개선을 못 받고 있던 게 드러났다. 개별 재현이 아니라 **중복 코드를 공용 모듈/메서드로 뽑아 두 경로가 항상 같은 동작을 하도록** 정리했다(다음부터 한쪽만 고치고 다른 쪽이 뒤처지는 드리프트 자체를 구조적으로 없앰). 이 작업은 위 3번(줄바꿈 박스 풀 수정)과 동시에 진행돼 합쳐졌다 — 새 `extension/src/hoverBox.ts`가 `showHoverBoxesAt(rects[])`/`hideHoverBox()`로 박스 풀·커서 오버라이드·전체화면 재부착을 `highlight.ts`/`articleHighlight.ts` 양쪽 공용으로 제공한다(각자 복제해 갖고 있던 `ensureBox`/`showBoxAt`/`hideBox`/`setHoveringCursor`/`onFullscreenChange`를 대체).
      1. **자막에 스크롤/리사이즈 시 박스 숨김, 클릭 시 박스 숨김이 빠져 있었음** — `articleHighlight.ts`엔 이미 있던(2026-07-30 앞선 수정) `scroll`/`resize` → `hideBox()`와 클릭 시 `hideBox()` 호출이 `highlight.ts`엔 없어서, 자막 hover 중 스크롤(전체화면 자막 스크롤 등)하거나 단어를 클릭한 직후 이전 박스가 화면에 남는 문제가 구조적으로 있었다(웹은 이미 고쳐졌지만 자막은 방치돼 있던 케이스) — 동일하게 추가.
      2. **bridge.ts 세그멘테이션 요청(`requestWordSegments`)의 eld 오판/응답 소실 취약점이 자막 경로에도 그대로 있었음** — 앞선 "중국어만으로 구성된 문단이 형태소 분할 안 됨" 조사에서 웹 문단(`pageParagraphText`)만 원인이었던 게 아니라, `requestWordSegments`(자막 `'subtitles'` 케이스와 웹 `'pageParagraphText'` 케이스가 이미 공유하던 메서드) 자체에 두 가지 구조적 결함이 있었다 — (a) `detectCjkVariant`(eld 단독)가 짧거나 순수 CJK뿐인 표본에서 판별에 실패하면 그 줄/문단은 세그멘테이션 대상에서 영구 제외됐다 — `resolveCjkLanguage()`(`shared/languageDetect.ts` 신설, eld 실패 시 가나→ja/한자 다수결→zh-Hans·zh-Hant 문자 기반 폴백)로 교체. (b) 세그멘테이션은 성공했지만 응답이 확장까지 전달되지 못한 경우(탭 전환 중 릴레이 실패 등) `segmentedLines`(영구 `Set`, 한 번 요청하면 끝까지 스킵)에 막혀 그 줄/문단이 영원히 글자 단위로 고정됐다 — `segmentResults`(완료 결과 캐시, 재요청 시 즉시 재전송) + `segmentInFlightSince`(타임스탬프 기반 진행중 표시, `SEGMENT_INFLIGHT_TIMEOUT_MS=4000` 지나면 재시도 허용)로 교체. 자막은 300ms 폴링이 자연스러운 재시도 트리거가 되고, 웹은 `articleHighlight.ts`의 `requestedSegmentsAt`(타임스탬프 맵, `SEGMENT_RETRY_MS=4000`, 기존 영구 `Set` 교체)가 재진입 시 재시도 트리거가 된다 — 양쪽 다 하나의 `requestWordSegments` 개선으로 해결됨.
      3. **(2026-07-30 후속) 세그먼트 그룹핑 계산 자체는 통합에서 빠져 있었음.** 위 통합 당시 hover 박스 **렌더링**(`hoverBox.ts`)만 공용화됐고, "세그먼트를 찾아 그 안의 단어 rect들을 줄 단위로 묶는" 계산 로직(`highlight.ts`의 `wordAtPoint` 내부, `articleHighlight.ts`의 `groupWordAt`)은 입력 형태(`SubLine[]` vs `SubWord[]+idx`)만 다르게 여전히 각자 복제돼 있었다 — 전수 점검(중복 로직 탐색) 중 발견해 `shared/wordMapping.ts`의 `groupSegmentAt(text, words, seed, segments)`로 통합, 두 파일은 이 함수를 호출만 하도록 정리(`refactor/reduce-duplication`, dev 병합).
- [ ] **(2026-07-30, 계획 재검토)** ~~브라우저에서 연 PDF — 확장 프로그램으로 텍스트 레이어를 직접 추출해 호버박스 표시~~ → **이 접근 자체가 불가능한 것으로 판명, 대안 조사 중.**
  - **왜 안 되는가**: 크롬 내장 PDF 뷰어는 별도 컴포넌트 확장(`chrome-extension://mhjfbmdgcfjbbpaeojofohoefgiehjai/`)이 out-of-process iframe으로 띄우는 화면이라 (1) 우리 확장의 content script가 `<all_urls>`로도 다른 확장의 `chrome-extension://` origin에는 주입 자체가 안 되고, (2) 설령 주입돼도 PDFium이 캔버스/플러그인 표면에 픽셀로 그리는 방식이라 `webArticle.ts`/`articleHighlight.ts`가 의존하는 DOM 텍스트 노드 + `getBoundingClientRect()` 모델이 원천적으로 성립하지 않는다(글자 좌표 자체를 주는 API가 없음). 파이어폭스 pdf.js는 진짜 DOM 텍스트 레이어가 있지만 그 뷰어도 특권 `resource://` 페이지라 마찬가지로 확장이 못 건드림.
  - **검토한 대안**: (A) DNR로 PDF 네비게이션을 확장 내장 pdf.js 페이지로 리다이렉트해 뷰어 자체를 대체(모질라 PDF.js 확장 방식) — URL 패턴 매칭이라 확장자 없는 PDF·POST 생성 PDF는 놓치고 쿠키/인증 있는 PDF는 재fetch 문제가 있음. (B) Nuance 자체 내장 뷰어(epub/txt와 동일 방향)로 흡수 — 가장 견고하지만 "브라우저에서 연 PDF" 시나리오 자체를 못 살림. (C) 크롬 창에 macOS 접근성 API(AX) 시도 — Preview.app에서 검증된 경로를 브라우저 창에도 그대로 적용 가능한지 실측 → **아래에서 실측, 성공으로 확인, (C)를 채택.**
  - **(2026-07-31) 대안 (A) DNR 리다이렉트도 프로토타입으로 실측 — 실패, 이 경로 폐기.** 최소 확장(manifest + `declarativeNetRequest` 규칙 + `viewer.html`)을 만들어 별도 프로필 크롬에 로드하고 로컬 서버로 서빙한 실제 PDF 를 열어봤다. 규칙 형태를 두 가지로(정규식 + `regexSubstitution` 으로 원본 URL 을 쿼리에 넘기는 방식 / 오해 소지를 없앤 `urlFilter: "*.pdf"` + `extensionPath` 고정 리다이렉트) 시도했으나 **둘 다 리다이렉트가 아예 발동하지 않았다** — AX 로 실제 렌더된 창을 조회해도 크롬 내장 뷰어 그대로였고, 로컬 서버 로그에도 우리 뷰어가 원본을 다시 fetch 한 흔적이 없었다(manifest/rules JSON 문법 오류 없음, 크롬 로그에도 확장 오류 없음). 크롬이 메인 프레임의 PDF Content-Type 처리를 DNR 훅보다 앞서거나 별개 경로에서 하는 것으로 보인다. **참고**: 크롬 설정에는 "특정 확장을 PDF 핸들러로 지정"하는 사용자 옵션 자체가 없다(`chrome://settings/content/pdfDocuments` 는 "크롬에서 열기 / 다운로드" 둘뿐). → 브라우저에서 연 PDF 는 mac 은 AX 경로(위), Windows 는 OCR, 그리고 사용자가 직접 여는 경우엔 자체 뷰어로 커버한다.
  - **(2026-07-30, macOS 실측 조사 완료 — (C) 채택 근거)** Swift 스크립트로 `AXUIElement` API를 크롬 창(실제 텍스트 레이어 있는 PDF, `9780198570004.pdf`)에 직접 호출해 확인.
    - **PDF 본문 텍스트가 실제로 AX 트리에 노출됨**: 창 전체를 얕은 깊이(≤12)로만 훑으면 PDF 내용이 하나도 안 보이는데(툴바 그룹/버튼만 잡힘), 이는 PDF 콘텐츠가 `AXWebArea`가 **세 겹 중첩된 구조**(크롬 창 웹뷰 → PDF 뷰어 확장 UI 웹뷰 → PDF 콘텐츠 자체 웹뷰, 깊이 약 24) 안에 있어서였다. 깊이 제한 없이 전수 순회하면 실제 페이지 안에 `AXStaticText` 노드가 4,701개 잡히고 텍스트도 실제 본문("An Introduction to Quantum Computing", "Phillip Kaye" 등 저자명)과 정확히 일치.
    - **좌표 API도 Preview.app과 동일한 방식으로 정상 동작**: 각 `AXStaticText` 노드가 `AXBoundsForRange`/`AXStringForRange` 파라미터 속성을 노출하고, opaque `AXTextMarker`가 아니라 **Preview.app과 동일한 단순 `CFRange` 기반**(`AXValueCreate(.cfRange, ...)`)이라 코드 재사용이 가능하다. 문자 범위(0,5)만 지정해도 그 부분 문자열의 정확한 좌표("An In" → `(632, 306, 66, 24)`)가 나오고 `AXStringForRange`로 확인한 문자열도 일치. **주의**: 노드 자신의 `AXPosition`/`AXSize`는 줄바꿈 포함 멀티라인 텍스트에서 왜곡되거나(예: height=0) 대표값만 주므로 신뢰하지 말고, 항상 `AXBoundsForRange`로 원하는 문자 범위를 직접 질의해야 함(Preview.app 조사 때와 동일한 결론).
    - **크롬은 `AXTextMarker` 계열 API(`AXTextMarkerRangeForLine`, `AXBoundsForTextMarkerRange` 등)도 함께 지원**하지만, 단순 `CFRange` 기반 `AXBoundsForRange`/`AXStringForRange`가 그대로 동작하므로 이 두 개만으로 충분 — koffi 바인딩 난이도가 Preview.app 때 우려했던 것보다 낮음.
    - **결론(2026-07-30, (C) 채택 확정)**: 브라우저(크롬)에서 연 PDF도 Preview.app과 **동일한 AX 코드 경로**(`CFRange` 기반 `AXBoundsForRange`/`AXStringForRange`, 깊이 제한 없는 트리 순회로 중첩 `AXWebArea` 통과)로 텍스트+좌표를 확보할 수 있음이 확인됨. 확장 프로그램 경로(위 항목 97-98)는 폐기하고, 브라우저 PDF는 Preview.app PDF 항목(아래)에 **AX 대상 프로세스만 다르게(크롬/사파리 등) 취급하는 방식으로 흡수 통합** — 별도 구현이 아니라 기존 macOS Preview PDF 작업의 확장으로 처리.
    - **Windows(크롬 + UIA) 실측 완료(2026-07-30) — No-Go**: `WINDOWS_PDF_UIA_VERIFICATION.md` 참고. 크롬 UIA 트리는 기본적으로 PDF 본문을 아예 노출 안 하다가(PDF 뷰어가 별도 guest view 프로세스라 접근성 활성화가 자동 전파 안 됨), `--force-renderer-accessibility`로 강제해야 `ControlType.Document`로 본문이 노출되고 `TextPattern`도 지원함(기준 1·2 충족) — 여기까진 macOS와 비슷. 하지만 **기준 3(부분 범위 좌표)에서 탈락**: 문서 맨 앞 5글자는 텍스트는 정확한데 `GetBoundingRectangles()`가 좌표 0개를 반환했고, 문서 중간(1000번째 글자 부근)에서는 반환된 좌표 15개가 전부 `X=797 Y=1031 W=1 H=1`로 완전히 동일한 1×1px — macOS 조사에서 Kindle이 탈락한 것과 같은 "자리표시자만 주는 가짜 응답" 패턴. **Windows에서는 크롬 PDF에 이 경로를 못 쓴다 — 기존 계획대로 OCR 기반 유지.**
- [ ] **(2026-07-30, 계획 변경 → 범위 확장)** macOS 프리뷰(Preview.app) **및 브라우저(크롬 등, 위 항목 실측 확인)**에서 연 PDF, 텍스트 레이어가 있는 경우 — 접근성 API(AX)로 텍스트+좌표를 직접 획득해 OCR 없이 오버레이에서 바로 호버박스 표시. 스캔본(텍스트 레이어 없는 PDF)은 기존처럼 OCR로 폴백.
  - **(2026-07-30, macOS 실측 조사 — 위 계획 변경의 직접 근거, 미구현·조사만 함)** 실제 코드 작성 없이 Swift 스크립트로 `AXUIElement` API를 직접 호출해 Preview.app/Kindle for Mac의 접근성 트리를 확인했다.
    - **Preview.app + 진짜 텍스트 레이어가 있는 PDF**: 페이지가 `AXStaticText` 노드로 노출되고, 표준 파라미터 속성(`AXBoundsForRangeParameterizedAttribute`/`AXStringForRangeParameterizedAttribute`/`AXRangeForLineParameterizedAttribute`, opaque `AXTextMarker` 아닌 단순 `CFRange` 기반이라 koffi 바인딩이 비교적 쉬움)이 정상 동작 — 문자 범위(0,5)="Hello"만 지정해도 그 단어만 감싸는 정확한 CGRect가 나오고, 줄 단위(`AXRangeForLine`)도 각 줄의 정확한 텍스트+bbox를 줬다. **텍스트+좌표 둘 다 얻을 수 있어 호버 박스 구현이 가능** — 위 계획 변경의 채택 근거.
    - **Preview.app + 스캔본(이미지) PDF**: 페이지가 `AXImage` 리프 노드(자식 0개, 텍스트 속성 전혀 없음)로만 노출됨 — 애초에 텍스트 레이어가 없는 파일이라 당연히 AX로도 못 얻는다(기존처럼 OCR 필요).
    - **Kindle for Mac(임포트 epub·구매한 책 둘 다 동일)**: 리딩 화면이 커스텀 `AXGenericElement`로 페이지 전체 텍스트를 `AXValue`/`AXStringForRange`로는 정확히 준다(텍스트 추출은 됨). **하지만 `AXBoundsForRange`/`AXRangeForLine`이 사실상 고장나 있음** — 어떤 범위를 넣어도 항상 창 자신의 좌상단 좌표에 크기 0을 붙인 동일한 자리표시자 값만 반환(에러 없이 "성공"이라고는 응답하지만 의미 없는 값). 임포트한 epub("Exhalation")과 실제 구매한 책("Mother of Learning") 둘 다 정확히 같은 증상이라, 파일 포맷 차이가 아니라 **Kindle 앱 자체가 좌표 조회 부분을 구현 안 해놓은 것**으로 보인다. → 이 좌표 문제 때문에 epub/txt는 외부 뷰어 접근성 API에 기대는 대신 **Nuance 자체 내장 뷰어**로 방향을 바꿈(아래 항목).
    - **결론(2026-07-30 계획 확정)**: PDF(Preview, 텍스트 레이어 있는 경우)는 AX만으로 텍스트+좌표 둘 다 확보 가능해 그대로 채택. Kindle 등 외부 epub 뷰어는 좌표가 안 나와(위 결론) 접근성 API 경로 자체를 epub/txt에는 적용하지 않기로 하고, 대신 자체 뷰어로 대체(하이브리드 방식은 채택하지 않음).
- [x] ~~**(2026-07-30, 계획 변경)** epub/txt — Nuance 앱 안에 자체 뷰어(렌더러)를 만들어 원본 파일을 직접 파싱·표시하고, 그 자체 렌더러 위에서 웹페이지와 동일한 방식(direct 텍스트 추출 + 좌표)으로 호버박스를 띄운다.~~ → **(2026-07-31) pdf/epub/txt 세 포맷 모두 구현 완료**(`feat/doc-viewer`). 범위가 epub/txt 에서 **PDF 까지** 넓어졌다 — 브라우저 PDF 를 확장으로 가로채는 마지막 대안(DNR 리다이렉트)마저 실측 실패해(아래 항목) 자체 뷰어가 PDF 의 답이기도 하게 됐기 때문.
  - **진입점**: 메인 화면 "PDF / EPUB / TXT 뷰어" 버튼 → 확장자를 셋으로 제한한 파일 다이얼로그 → 전용 뷰어 창(`createViewerWindow`, `windows.ts`). 파일은 메인이 `fs` 로 읽어 IPC 로 넘긴다(contextIsolation 유지 — 렌더러는 fs 에 직접 접근하지 않음).
  - **호버박스는 확장과 같은 소스를 공유한다(핵심)** — `extension/src/` 의 `domWords`/`webArticle`/`articleHighlight`/`hoverBox`/`wordSegments` 가 `chrome.*` API 를 한 곳도 안 쓰는 순수 DOM 코드였고 진입점(`startArticleHighlight`)도 이미 콜백 주입식이라, `src/shared/hover/` 로 옮기고 확장은 그대로 import 하도록 바꾸는 것만으로 확장·뷰어가 한 소스를 쓴다(빌드 설정 변경 불필요 — 양쪽 다 이미 `@shared` alias 보유). 전역 `document`/`window` 를 쓰던 소수 지점은 넘어오는 컨테이너에서 유도(`container.ownerDocument`/`.defaultView`)하도록 일반화했고, 이 한 번의 일반화가 epub(epubjs iframe = 별도 document) 까지 자동으로 해결했다. 문단 선택자만 주입 가능하게 해(`paragraphSelector`) PDF 는 pdf.js 텍스트 레이어 span 을 문단으로 쓴다.
  - **CJK 형태소 분할도 공용화** — 확장 bridge 와 뷰어가 `main/nlp/segmentCjk.ts` 를 공유한다. 경로마다 다른 기준으로 쪼개면 같은 문장인데 hover 로 묶이는 단위가 갈리므로 반드시 한 소스여야 한다.
  - **포맷별 렌더링**: PDF 는 **원본 레이아웃 유지**(pdf.js 캔버스 + 그 위 투명 텍스트 레이어 — 그림/수식/다단이 원본 그대로), epub 은 epubjs(페이지네이션·CSS·폰트 위임), txt 는 빈 줄 기준 문단을 `<p>` 로. epub/txt 는 글자 크기 슬라이더 + 라이트/다크 전환 제공(PDF 는 원본을 그대로 보여주는 게 목적이라 제외).
  - **뷰어 창은 창 선택 목록에 일부러 남긴다** — `capture.ts` 의 "Nuance 자기 창 제외" 필터에 뷰어 창만 예외를 뒀다(`isViewerWindow`). 스캔본 PDF 처럼 direct 추출이 안 되는 문서는 사용자가 그 창을 골라 기존 OCR 을 얹을 수 있다. `decideOcr.ts` 는 손대지 않았다(일반 창은 자동으로 OCR 로 떨어짐).
  - **주의(하마터면 못 찾을 뻔한 것)**: `pdfjs-dist` 는 **v4 로 고정**해야 한다. v6 는 `Promise.try` 를 쓰는데 이 프로젝트의 Electron 31(Chromium 126)엔 없어서 PDF 가 한 장도 안 그려진다(런타임 TypeError). v4.10 에도 `TextLayer` API 가 그대로 있어 코드 변경 없이 동작하며, `page.render()` 의 `canvas` 인자만 v4 시그니처에 맞추면 된다.
  - [ ] **남은 검증** — 뷰어 창을 창 선택으로 골라 OCR 이 실제로 도는지는 미확인(작업 워크트리에 python venv 가 없어 OCR 엔진이 안 떠서 확인 못 함). 대형 문서 성능(수백 쪽 PDF 초기 렌더 비용)도 실사용 확인 필요 — 현재는 전 페이지를 한 번에 그린다(284쪽 PDF 로 동작 자체는 확인).
- [x] ~~언어 자동 감지 (유니코드 블록 기반 경량 분류) — 현재 `detectLanguage()`는 항상 `'en'` 반환하는 스텁.~~ → **tier2 확장(2026-07-30)에 맞춰 구현 완료**: `langDetect.ts`가 Tesseract OSD(`osd.traineddata`)로 스크립트(문자 체계)만 먼저 가볍게 판별하고, 라틴/키릴/아랍/데바나가리처럼 한 스크립트에 여러 언어가 걸리는 경우 `resolveAmbiguousScript()`가 대표 언어팩으로 1차 OCR한 텍스트를 eld(자막 판별기와 동일)로 재판별한다(zh-Hans/zh-Hant를 가르던 패턴의 일반화).
- [x] OCR 엔진 연동 — **범용 엔진(전체 언어 공통/자동감지용) + 언어별 최적 엔진(개별 특화) 이중 구조**로 결정.
  - [x] 범용 엔진: **Tesseract.js** 채택 확정 및 연동 완료 — `ocr.ts`: `captureFocusedWindow()`(창 캡처 → PNG) → `createWorker` → `recognize(image, {}, {blocks:true})` → block/paragraph/line 을 평탄화해 단어별 bbox 추출. 언어별 워커를 재사용(언어 바뀌면 재생성)하고, `detectLanguage()`가 고른 `Language`로 traineddata를 선택. 실제 창 캡처 + OCR 로 검증됨(단, 언어 자동 감지가 스텁이라 항상 `eng` 모델 사용 — 한국어 등 미지원 언어 인식 시 깨진 텍스트가 나오는 게 정상, 언어 자동 감지 구현 후 해소).
  - [x] **창 캡처 크로스플랫폼** — OCR 엔진(Tesseract)은 PNG 버퍼만 받으므로 플랫폼 무관, 캡처만 분기. Windows: win32 `PrintWindow`. **macOS: 내장 `screencapture -o -l<windowID>`**(`capture.ts`, 네이티브 해상도라 PrintWindow 동급 품질). mac 은 물리 픽셀(Retina 2x)이라 `alignWordsToOverlay`(`extractionCache.ts`) darwin 분기에서 bbox 를 `scaleFactor` 로 나눠 오버레이 DIP 에 정합. `screencapture -l` 실기기 동작 확인(760×460 창 → 1520×920 PNG).
    - [ ] mac 실사용 검증 — 실제 선택모드에서 단어 hover/클릭 좌표가 정확히 맞는지(배율·프레임 미세보정 필요 여부) GUI 확인 필요. **화면 기록 권한** 최초 허용 + 앱 재시작 1회 필요.
    - [x] OCR 대상 영역 지정(바로 위 항목) — **macOS 좌표 변환 구현 완료**(`regionSelection.ts: submitRegionFromOverlay`). win32는 `getCaptureOriginOffset`+`getPhysicalToDipScale`로 원점·배율을 함께 보정하지만, macOS는 `alignWordsToOverlay`(`extractionCache.ts`) darwin 분기와 대칭으로 원점 오프셋 없이 `getMacWindowBounds`+`screen.getDisplayMatching().scaleFactor` 배율 보정만 적용(캡처·오버레이 원점이 창 좌상단으로 동일해서). 영역 내용 변화 감지(`changeWatcher.ts`)는 영역 지정에 의존하므로 이제 macOS에서도 함께 동작.
  - [x] 언어별 특화 엔진 — 중국어/일본어(가로쓰기 포함)는 PaddleOCR로 전환 완료(Tesseract 대비 인식률 개선, 실사용 확인). **일본어 세로쓰기는 NDLOCR-Lite + PaddleOCR 조합으로 최종 결정**(2026-07-29) — NDLOCR-Lite(정확히는 NDLkotenOCR-Lite, 일본 국립국회도서관 고문서 특화 모델, `python/ocr_ndlocr.py`)가 검출+인식을 전담하고, PaddleOCR은 상시 보조로 병행한다(`selection/ocrNdlocr.ts`): (1) `fillInterLineGaps` — 같은 열 안 인접 두 줄 사이 간격에 실제 잉크가 있으면 그 구간만 재인식해서 채움, (2) `resolveSuspiciousLines` — 스페이스 마커(NDLOCR이 못 읽은 자리에 남기는 신호)가 있거나 줄 자신의 칸 크기가 공유 기준보다 크게 벗어난 줄만 대조 재인식해서 마커/불일치 구간을 교정. NDLOCR 자체가 실패하면(Python 환경 없음 등) PaddleOCR 전체 경로로 폴백(`ocr.ts: runVerticalOcr`). 영어는 계속 Tesseract 단독. → **세로쓰기(주로 중국어) 열 경계에 '\n' 마커 삽입(2026-07-29, 사용자 제보 — "중국어 세로쓰기 텍스트 박스를 누르면 가끔 본문이 비어있는 팝업이 뜸")**: `recognizeOrderedLines`(`ocrPaddle.ts`)가 열 단위 `Word[][]`로 이미 갖고 있는 결과를 마지막에 `flat()`으로 펼쳐 반환해, 페이지 전체가 '\n' 하나 없는 거대 단일 문자열이 됐었다 — 팝업 1차 문맥 표시 범위(`popup/selection.ts: computeLineContextRange`, '\n' 기준 앞뒤 2줄)가 페이지 전체로 잡혀 한자 수천 개 atom + 글자별 DOM 측정(`popup/measureLines.ts`)으로 첫 렌더가 느려지는 원인으로 지목되어, NDLOCR 문단 마커와 같은 방식(bbox 없는 순수 텍스트 Word, `text = words.join('')` 불변조건 유지)으로 열 경계마다 '\n'을 끼워 넣도록 수정. **다만 이것만으로는 실제 버그가 고쳐지지 않았음** — 진짜 원인은 `windows.ts`의 `popupContext`/`destroy()` 순서 레이스(아래 팝업 창 재사용 항목 참고)였고, 이 마커 추가는 첫 렌더 성능 개선으로서 별도로 유지.
    - [ ] 알려진 한계(2026-07-29 실측 확인, 아래 "미해결 문제" 항목도 참고) — NDLOCR-Lite가 글자를 놓쳐도(縦中横 숫자, "……" 등) bbox 자체는 실제 물리적 범위를 그대로(또는 넉넉하게) 보고하는 경향이 있어서, 마커도 없이 조용히 빠지는 "완전 침묵" 케이스가 줄 bbox **안쪽**에서 발생하면 지금 있는 어떤 신호(높이 비교, 잉크 픽셀 합산 비교, PaddleOCR 독자 경계 비교)로도 원리적으로 못 잡는다는 게 페이지 전체 데이터로 확인됨. 이 방향(경계/합산 비교)은 여기서 멈추기로 함 — 다음 시도가 있다면 "의심 여부와 무관하게 일부/전체 줄을 다른 엔진(예: Yomitoku)으로 무조건 재대조"하는 방향(속도 트레이드오프 있음) 정도가 남아있음.
    - [ ] **(조사만 함, 미착수, 2026-07-29) 중국어 세로쓰기 전용 엔진 — CHAT/Kraken 모델 검토** — 일본어의 NDLOCR-Lite에 대응하는 중국어 전용 모델이 있는지 조사한 결과, 콜레주드프랑스 계열 "Numerica Sinologica" 컨소시엄의 **CHAT(Chinese Historical documents Automatic Transcription) 모델**(`github.com/colibrisson/CHAT_models`, Kraken OCR 엔진용)이 가장 근접한 대안으로 확인됨 — 10~20세기 세로쓰기 고문헌(170만 줄, 16,000자+)으로 학습, 세로쓰기 검출+인식 모델 쌍(`chat_seg.mlmodel`/`chat_rec.mlmodel`)을 통째로 제공해 방향 처리 자체는 이미 해결돼 있음(*Siku Quanshu* 사고전서 전용 파생 모델도 별도 존재). **두 가지 걸림돌**: (1) **라이선스가 CC BY-NC-4.0(비상업 전용)**이라 상업적 배포 시 문제가 될 수 있음 — 사용자 확인 요청함. (2) **Kraken OCR 엔진 자체가 Windows를 지원하지 않음**(공식 문서에 Linux/macOS만 명시) — 이 앱의 다른 Python 엔진들(PaddleOCR/NDLOCR-lite/Yomitoku)처럼 서브프로세스로 바로 못 붙이고 WSL2나 Docker 안에서 돌려야 해서, 아키텍처 변경 부담이 라이선스보다 더 클 수 있음. 실제 통합 착수 전 이 두 문제를 먼저 해결/감수할지 결정 필요.
    - [x] **(2026-07-30) 중국어 세로쓰기 인식 성능/정확도 일괄 수정** — 사용자 실사용 제보로 여러 문제를 연쇄로 발견·수정:
      - [x] **첫 인식 시 콜드스타트 9초+** — `warmUp()`(`ocrPaddle.ts`)이 `ja` 인식 엔진만 예열하고 `zh-Hans`/`zh-Hant`는 빠져 있었음(`ocr_paddle.py: get_engine`이 언어 태그별로 엔진을 따로 캐싱 — 가중치 파일은 동일해도 인스턴스는 새로 생성됨). zh-Hans/zh-Hant 예열을 추가하고 병렬화(`Promise.all`) — 단계별 타이밍 로그(`[timing]     줄 검출/줄별 인식/격자 분할` 등, `recognizeVerticalColumnWithPaddle`/`recognizeOrderedLines`)로 실측: "줄별 인식" 단계가 9637ms→5230ms로, 전체 19553ms→14670ms로 단축.
      - [x] **열 순서가 뒤섞임(A B C 대신 A C B, 또는 한 열의 일부가 다른 열 중간에 낌)** — `clusterVerticalLinesIntoColumns`(같은 함수를 `ocrNdlocr.ts`의 일본어 세로쓰기 주 경로도 공유)가 "바로 직전 열"하고만 비교하는 단일 패스라 노이즈에 취약했음. (1) 직전 열이 아니라 지금까지 만들어진 모든 열 중 가장 가까운 열을 찾아 병합하도록 변경 + 펼치기 전 열 평균 x로 재정렬 추가. (2) `DEBUG_OCR_DUMP`로 실제 페이지의 열 중심 x를 실측한 결과, 서로 다른 두 열의 중심 간격(66.5px)이 그 페이지 글자 폭 중앙값(45px)×기존 배율(1.5)=67.5px과 1px 차이로 오판된 것을 확인 — `COLUMN_GAP_RATIO`를 1.5→1.2로 하향(실제 정상 열 간격 비율은 산수상 폭의 0.5배 근처가 흔해 1.5가 구조적으로 그 경계에 걸쳐 있었음).
      - [x] **쉼표가 아포스트로피(`'`)로 오인식** — PaddleOCR 인식 모델(PP-OCRv6)의 원시 오인식(둘 다 저해상도에서 구분이 어려운 작은 곡선 하나짜리 글자). `ocrNdlocr.ts`의 `KANJI_LATIN_CONFUSION`과 같은 패턴으로 `fixChineseCommaMisread`(zh-Hans/zh-Hant 전용, 일본어/영어는 대상 아님 — 영어는 it's/don't 등 진짜 아포스트로피가 흔해 위험) 추가.
      - [x] **문장 중간 마침표/쉼표가 자리표시자도 없이 조용히 누락 + 이후 글자 위치가 계통적으로 밀림** — 기존 미검출 구간 자리표시자 로직(`insertGapPlaceholdersForLine`, `GAP_RATIO_THRESHOLD=1.5`)이 "글자 한 칸 통째로 빠짐"만 잡게 튜닝돼 있어 마침표 하나 누락(간격 ≈ 칸 크기의 1배 미만)은 안 걸림. `DEBUG_OCR_DUMP`로 모든 인접 간격 비율을 실측(정상 -0.3~+0.33, 진짜 누락 0.9569로 뚜렷이 분리)해 `GAP_RATIO_THRESHOLD`를 0.6으로 하향. → **재수정**: 0.6으로 낮추자 이번엔 문장부호(잉크가 좁아 자기 칸을 다 못 채움) 바로 뒤 간격(실측 0.72~0.79, 하필 새 임계값과 진짜 누락 사이 빈틈)이 "한 글자 더 빠짐"으로 오판돼 자리표자가 중복 삽입됨 — 바로 앞 원시 단위가 문장부호(、。，！？…, comma-fix 전 원시 단위 기준이라 아포스트로피 오독 형태도 포함)면 간격 크기와 무관하게 항상 건너뛰도록 수정.
      - [x] **미검출 자리표시자가 일본어 관례(게타 마크 〓)를 씀** — 사용자 요청으로 중국어 전용 자리표시자(`UNKNOWN_GAP_PLACEHOLDER_ZH = '□'`) 추가, 언어별로 분기(`recognizeOrderedLines`). `ocrNdlocr.ts`(일본어 전용)는 영향 없음.
      - 원시 per-word bbox를 안 믿고 줄 전체 bbox를 인식 글자 수로 균등 격자 분할하는 현재 설계(`groupCjkCharsGrid`) 자체에 대한 사용자 의문(왜 원시 검출 영역을 그대로 안 쓰는지) — PaddleOCR 원시 bbox는 조사(が/を/で 등)가 3~4px 높이로 잡히거나 여러 글자가 박스 하나로 뭉쳐 나오는 등 신뢰할 수 없어(실사용 중 "박스 테두리가 글자 가운데 있음" 확인) 격자 방식으로 이미 전환된 이력이 있음을 확인·설명, 원복하지 않음.
    - [x] **(2026-07-29) 일본어 가로쓰기 후리가나 미제외 + 다단 큰 블록 속도 저하 수정 — Windows/macOS 공통**(순수 JS/Python 리소스 서버 로직, OS 분기 없음). NHK "やさしいことば"(글자마다 후리가나 있는 뉴스) 페이지 테스트로 두 문제 발견: (1) 후리가나 제외 필터(`excludeFurigana`)가 세로쓰기 경로에만 연결돼 있고 기하학도 세로쓰기 전용(폭 좁음+y축 겹침 기준)이라 가로쓰기 후리가나(높이 낮음+x축 겹침)는 전혀 안 걸러짐 — 대칭 함수 `excludeFuriganaHorizontal`을 추가해 `recognizeLinesWithPaddle`(가로쓰기 경로)에 연결. (2) DocLayout이 다단(여러 블록)으로 나눈 경우 블록 하나가 실제로는 본문 전체만큼 커도(실측: 250~340단어) 그 블록을 `recognizeWithPaddle`로 통째로(줄 단위 병렬화 없이) 인식해서 57~74초씩 걸리던 문제 — 열 개수와 무관하게 항상 `recognizeLinesWithPaddle`(줄 단위 병렬 + 후리가나 필터 적용)을 쓰도록 통일(`ocr.ts: runNonTesseractOcr`).
      - [x] **(2026-07-29) `FURIGANA_HEIGHT_RATIO` 1.8→1.15 조정** — 실사용 재보고("후리가나가 전혀 안 걸러짐")로 실측 로그를 찍어보니, 세로쓰기용으로 튜닝된 1.8 비율이 가로쓰기 실제 본문:후리가나 높이 비율(실측 1.17~2.3 분포)보다 높아 대다수(55개 중 48개)가 필터를 통과해 살아남고 있었다 — 실측 최솟값(1.167)보다 낮은 1.15로 낮춰 대부분을 잡도록 수정(`ocrPaddle.ts`).
    - [x] **(2026-07-30) 가로쓰기 인식 오류 2건 수정** — 사용자 실사용 제보로 발견:
      - [x] **선택 영역 일부(예: 1/3)만 인식되고 나머지는 조용히 스킵됨** — 재현 이미지로 원인 확인: 큰 폰트 제목 + 그 아래 구분선을 DocLayout이 표(table) 헤더/테두리로 오분류(신뢰도 0.32~0.72)해서, `BODY_LABELS` 필터가 그 거대한 table 블록을 걸러내고 나면 우연히 별도로 잡힌 작은 text 블록 몇 개(선택 영역 916px 높이 중 231px)만 인식 대상으로 남았다. 세로쓰기는 이미 "DocLayout 블록 경계를 못 믿는다"는 원칙으로 실제 열 구분을 자체 로직에 맡기도록 고쳐뒀는데 가로쓰기엔 이 안전장치가 없었음 — 본문 라벨 블록들의 합계 면적이 선택 영역 면적의 50% 미만이면 그 블록들을 아예 못 찾은 것(blocks=0)과 동일하게 취급해, 영역 전체를 직접 줄 단위로 스캔하는 기존 폴백 경로를 타도록 수정(`ocr.ts: runOcr`, `MIN_BODY_COVERAGE_RATIO`). English Tesseract 경로(열이 1개면 이미 region 전체를 그대로 씀)와도 동작이 일관돼짐.
      - [x] **팝업에서 클릭한 줄과 다른 줄이 선택되는 것처럼 보임(예: bb를 눌렀는데 cd가 선택됨)** — 클릭 지점→매칭 단어/줄→anchor 계산까지는(`selection/index.ts`) 실측 확인 결과 항상 정확했음(진단 로그로 확인 후 제거) — 원인은 그보다 더 앞단: 가로쓰기 OCR 결과(zh/ja, 띄어쓰기 없는 문자 체계라 원래 이어붙임)에는 애초에 줄 사이 `'\n'`이 전혀 없어서, 팝업의 1차 문맥 표시 범위(`popup/selection.ts: computeLineContextRange`, `'\n'` 기준 앞뒤 2줄)가 "줄이 하나뿐"으로 보고 텍스트 전체를 문맥 범위로 반환해버렸다 — 세로쓰기가 먼저 겪었던 "본문이 비어있는 팝업"(2026-07-29)과 근본 원인이 완전히 같은 문제(그때는 열 경계에만 `'\n'`을 넣고 가로쓰기 줄 경계는 놓쳤었음). 가로쓰기도 줄 경계마다 `'\n'` 마커를 넣도록 세로쓰기와 통일(`ocrPaddle.ts: recognizeOrderedLines`) — 부수 효과로 `mergeWithPreviousContext`(직전 회차 문맥 병합, 2026-07-29 도입)의 줄 경계 계산도 가로쓰기에서 정상 동작하게 됨(그전엔 이것도 같은 이유로 텍스트 전체를 "한 줄"로 오인했음).
    - [x] **(2026-07-30) 영어(Tesseract) 경로 text/words 불일치 수정** — 사용자가 영어 텍스트로 테스트하며 발견: (1) 팝업 본문에서 단어가 반토막 등 이상한 단위로 선택됨, (2) 영역 가장자리 단어는 팝업 본문엔 나오는데 텍스트 박스가 안 생김. 원인은 `recognizeRegion`(`ocr.ts`)이 `extracted.text`를 Tesseract가 자체 조립한 원문(`data.text`)에서 만들고 있었던 것 — CJK 경로 전부가 지키는 `text = words.map(w => w.text).join('')` 불변조건(클릭 오프셋 계산이 이 등가성에 의존, `selection/index.ts: findWordSpan`)을 이 함수만 어기고 있었다. 잘린 단어가 `words`(박스용)엔 없어도 Tesseract 원문엔 남아있어 (2)가 생겼고, `words`가 `text`보다 문자 수가 적어져 그 뒤 모든 오프셋이 밀려 (1)이 생겼다 — `words` 를 만들 때 구분자(공백/문단 구분 `\n\n`)도 bbox 없는 Word로 같이 끼워 넣어 `text`를 `words` 자체에서 뽑아내도록 변경, `normalizeOcrText` 삭제(더 이상 안 씀). → **재수정 2건(같은 날, 실사용 재확인)**: (a) 줄 사이 구분자만 넣고 같은 줄 안 단어 사이 공백을 빼먹어 전체 텍스트가 붙어버림 — 단어마다 사이에 공백 삽입 추가. (b) 잘린 단어를 텍스트에서도 아예 제외해버려 그 자리 글자가 통째로 누락됨 — bbox만 빼고(hover/클릭 대상에서만 제외) 텍스트는 그대로 남기도록 수정(원래 동작 복원). → **재수정 3건째**: `splitWordBySymbols`가 클릭 단위 경계를 위해 끝 문장부호/대시를 텍스트에서 트리밍하던 것도 같은 이유로 팝업 본문에서 통째로 사라짐("문장부호가 모두 사라짐") — 트리밍된 부분을 bbox 없는 Word로 남겨 텍스트는 보존하고 클릭 단위만 유지하도록 수정.
      - [ ] **남은 문제**: 선택 영역 경계가 글자 중간을 정확히 가로지르면 Tesseract가 그 픽셀 자체를 못 봐서 글자가 빠진 채로 인식됨(우리 코드로 못 고침) — 다단(열 2개 이상)일 때만 있는 `BLOCK_PADDING_X/Y` 여유 크롭이 열 1개(대부분의 경우)일 땐 전혀 적용 안 됨. 선택 영역보다 살짝 넓게 캡처해 Tesseract에 넘기고 초과분 단어는 여전히 클릭 제외 처리하는 방향을 제안했으나 미착수(사용자 확인 대기).
    - [x] **(2026-07-30) 팝업 초기 선택이 클릭한 단어에서 수십 자 뒤 단어(들)로 밀리던 버그 수정(담당 B 영역이지만 파이프라인 조사 중 발견)** — 브라우저 PDF 영어 테스트 중 발견(사용자 제보: "yet 을 눌렀는데 sandy hole 이 선택됨", 클릭마다 밀림량이 17/22자 등 제각각, 안 밀릴 때도 있음). 단계별 진단(메인 anchor 로그 → 렌더러 선택 모델 로그)으로 범인 확정: 메인 프로세스의 클릭 판정·anchor 계산·문맥 병합은 전부 정확했고(`hitWord="yet"`, `anchor=[161,164)`, merge 미발동 실측 확인), 렌더러의 선택 모델 계산(`buildSelectionModel`)도 정확했다(`initialAtoms=["yet"]` 실측 확인) — 문제는 그 사이 `PopupScreen.tsx`의 "선택 유지 재매핑"(`lastSpanRef`, 글자 단위 토글 등에서 드래그 선택을 유지하려고 2026-07-29 도입): 선택 문자 범위를 **displayText(표시 창) 좌표로** 기억했는데, model 이 바뀌는 이유 중 measuredRange 도착(근사 창 → DOM 측정 창)은 atoms 구조만이 아니라 표시 창 시작점(`windowStart`, 실측 11→31)까지 이동시켜서, 낡은 표시 좌표를 새 창의 atoms 에 그대로 얹으면 창 이동량만큼 뒤의 엉뚱한 단어로 재매핑됐다(3글자 범위가 두 단어 경계에 걸치면 두 단어가 같이 선택되는 증상까지 동일 원인). 선택 범위를 원문(extracted.text) 절대 좌표로 기억하고 재매핑 비교 시 각 atom 도 절대 좌표로 변환(`displayOffsetToAbsolute` 신설, `popup/selection.ts`)하도록 수정 — 창이 어떻게 바뀌든 항상 같은 원문 범위를 가리킨다. 근사 창과 측정 창의 차이가 큰 긴 문단 텍스트(PDF 등)에서만 재현되고 자막류 짧은 텍스트에선 두 창이 우연히 일치해 그동안 안 드러났던 것.
    - [x] **선택 영역이 두 페이지/두 단(가로쓰기는 왼쪽부터, 세로쓰기는 오른쪽부터)일 때도 올바른 순서로 읽기** — 세로쓰기는 이미 DocLayout 블록 경계를 안 믿고 영역 전체를 x좌표로 직접 클러스터링해 오른쪽→왼쪽으로 읽어(`clusterVerticalLinesIntoColumns`) 검증 결과 문제없었고, 가로쓰기도 아래 항목들로 같은 방식(자체 좌표 클러스터링)으로 전환 완료. 핵심 통찰: "여러 단"과 "여러 페이지"를 구분할 필요 없이 "간격으로 그룹 나누고 정해진 방향으로 순서대로 읽기"라는 하나의 메커니즘이 둘 다 커버함.
      - [x] **(2026-07-30) 세로쓰기 다단 OCR 품질 문제 5건 수정(사용자 실측 테스트로 발견)**:
        - **열 순서가 가끔 뒤바뀜**: `texts-*.json` 덤프(`gapRatioToMedian`)로 실측 확인 — 순서가 깨지는 지점 전부 간격 비율이 정확히 "열 하나 폭만큼"(0.90~1.02배)이었는데 병합 임계값(`COLUMN_GAP_RATIO`, 당시 1.2)엔 못 미쳐 서로 다른 두 좁은 열이 하나로 합쳐지고 있었다 — 1.2 → 0.85로 재하향(`ocrPaddle.ts`).
        - **허결호(□) 스팸 + 텍스트 박스가 대체로 한 칸씩 길어짐(같은 원인)**: 글자 칸 크기 추정(`estimateCellSizeFromIndent`, 들여쓰기 최빈값 기반)이 캡처마다 34→28처럼 흔들려서(실제 칸은 ~32.5px), 19글자짜리 열에서 그 오차가 누적돼 실행마다 박스 길이가 달라졌다 — 실제 인식된 글자들의 y간격 중앙값(`measureCellPitchFromUnits`, 잉크 위치에서 직접 측정)을 최우선으로 쓰도록 교체, 표본 부족(5개 미만) 시에만 기존 추정으로 폴백. 세미콜론(；)/콜론(：) 뒤 간격도 쉼표/마침표와 같은 이유(잉크가 좁아 다음 글자 간격에 여백이 얹힘)로 허결호가 붙었는데 `NARROW_PUNCTUATION_RE`에서 빠져 있었던 것도 같이 추가.
        - **열 끝 마침표가 검출 단계에서 아예 빠져 허결호로도 안 나옴**: 검출 모델이 끝 문장부호를 줄 bbox 에 통째로 안 잡아서(잉크가 작아 검출 경계가 그 앞 글자에서 끝남) 기존 trailing-gap 판정(간격 자체가 0) 으로는 원리적으로 못 잡는 케이스 — 페이지 공통 하단(대부분 열이 끝나는 y)보다 한 칸 정도 못 미치는 열은 끝 글자가 빠진 것으로 보고 하단을 연장하는 보정 추가(`insertUndetectedMarks`). 다만 두 페이지 PDF 실측에서 `extended=0`으로 확인돼(공통 하단 계산은 정상 작동했지만 이 케이스의 마침표 누락은 해당 안 됨) 이 특정 증상은 미해결로 남음 — 원인이 검출 단계가 아니라 PaddleOCR **인식** 단계에서 글자가 조용히 드롭되는 다른 층위일 가능성이 있음(박스는 있는데 인식 텍스트에서만 빠짐, 별도 조사 필요).
        - **시간이 오래 걸림**: 워커 풀(`POOL_SIZE`, CPU 코어 기반 2~8)이 열 개수보다 훨씬 적어 순차 라운드가 여러 번 도는 구조적 문제로 확인(41열÷6워커≈7라운드, 라운드당 ~2.8~5.7초) — 근본 개선(줄 여러 개를 한 번의 Python 호출로 배치 처리)은 범위가 커서 보류, 대신 줄별 개별 소요시간+POOL_SIZE를 로그에 남겨 진단 가능하게만 해둠.
        - **PaddleOCR 검출 파라미터(`unclip_ratio`) 실험**: 지금까지 전부 PaddleX 기본값(2.0)만 썼다는 걸 소스 확인(`paddlex/inference/models/text_detection/predictor.py`가 호출 시점 오버라이드를 지원함) 후, 짧은/희소한 줄의 검출 경계 흔들림을 줄여보려 1.5로 낮춰 실험 도입(`OCR_DET_UNCLIP_RATIO` env로 조정 가능, `ocr_paddle.py: detect_lines`/`ocrPaddle.ts: detectLinesWithPaddle`) — 정상 줄 부작용 여부는 계속 관찰 필요.
      - [x] **(2026-07-30) 가로쓰기 다단 읽기 순서 도입 — 여러 층위에 걸친 버그 4건 연쇄 수정** (사용자 제보: "오른쪽 맨 위 줄부터 시작해서 왼쪽/오른쪽 번갈아 나옴" → 고친 뒤에도 "y좌표 높은 순서대로 열 무관하게 나옴"):
        - **`mergeIntoColumns`가 DocLayout 의 `column` 필드를 맹신**: 실측 확인 결과 이 값이 물리적 열 구분이 아니라 모델이 페이지를 훑는 내부 순서를 반영하는 것으로 보여, 두 열의 블록이 0,1,0,1... 처럼 번갈아 번호 매겨지면 그룹핑 자체가 서로 다른 열을 한데 묶었다 — 블록 자신의 **왼쪽 경계** x좌표로 재군집화하도록 교체(세로쓰기 `clusterVerticalLinesIntoColumns`와 같은 패턴, 중심 x 대신 왼쪽 경계를 쓰는 이유는 문단마다 길이가 달라 중심 x가 불안정하기 때문).
        - **위 수정에도 재현됨 — 더 앞단이 원인**: DocLayout이 이번 캡처의 본문을 통째로 "table"로 오분류해(신뢰도 0.897, `blocks-*.json`로 실측 확인) `BODY_LABELS` 필터를 거의 다 걸러내고, 커버리지 부족(0.3% < 50%) 안전장치가 블록 자체를 버리고 선택 영역 전체를 크롭 하나로 통짜 스캔하는 폴백으로 빠졌다 — 그 폴백 안에서 실제 줄 순서를 정하던 `recognizeLinesWithPaddle`이 `sort((a,b)=>a.y-b.y)`로 열 구분 없이 순수 y정렬만 하고 있었던 게 진짜 원인. 줄(line) 단위 재군집화 함수(`clusterHorizontalLinesIntoColumns`) 신설해 PaddleOCR/Yomitoku 두 경로 모두에 적용.
        - **재군집화 임계값 두 번 재조정**: 1차(줄 폭 중앙값×0.5)는 가로쓰기 줄 폭이 열 하나 너비만큼 넓어서(세로쓰기는 글자 하나 크기라 작음) 실제 열 간격보다 임계값이 커져 전부 하나로 합쳐짐(무효화) — 2차(간격 중앙값×4)로 바꿨더니 이번엔 문단 들여쓰기 스타일이 여러 단계라(37~125px대 간격이 자주 나옴) 174줄이 12열로 과분할됨. 최종적으로 **전체 간격 중 최댓값 대비 비율**(70% 이상만 열 경계로 인정)로 교체 — 실측 검증(176줄→2열, maxGap=218px vs 나머지 최대 125px)으로 확정.
        - **클러스터링은 정확했는데도 여전히 순서가 깨짐 — 하류에 중복 정렬 잔존**: `recognizeLinesWithNdlocr`(NDLOCR-lite 가로쓰기 인식) 안에 `sort((a,b)=>a.y-b.y)`가 그대로 남아 있어서, 위에서 정확하게 재정렬해 넘긴 순서를 이 함수가 받자마자 다시 순수 y정렬로 덮어쓰고 있었다 — 유일한 호출부가 이미 정렬해서 넘기므로 이 재정렬을 제거.
      - [x] **(2026-07-30) 가로쓰기 줄 끝에 다른 줄의 일부/노이즈가 붙는 문제 + 텍스트 박스가 줄 일부만 덮는 문제 수정** (순서 문제 해결 후 이어서 발견):
        - **줄 끝에 다른 줄 조각이 섞임**: 줄 크롭 여백(`padLine`, `LINE_PADDING_Y=6`)이 세로쓰기 전용으로 설계돼 있었다 — 세로쓰기는 Y가 읽기 진행 방향(열의 시작/끝)이라 위아래 여백이 안전한데, 가로쓰기는 방향이 90도 달라 Y가 오히려 **줄 쌓임 방향**(위/아래 인접 줄과 붙어 있는 방향)이라 6px 여백이 줄간격이 좁을 때 인접 줄을 침범했다 — `padLine`에 `vertical` 인자를 추가해 세로쓰기는 Y축, 가로쓰기는 X축에 여백을 주도록 축을 방향에 맞게 전환.
        - **"1. 科学边界"처럼 숫자+마침표+공백 접두어가 있는 줄에서 뒤 글자("科")만 박스가 찌그러짐**: 반각 ASCII 문장부호(.)/공백이 전각 한자와 똑같이 "폭 1칸"으로 계산되고 있어서(줄 양 끝 CJK 문장부호만 좁게 잡는 기존 로직 대상 밖) 접두어의 실제 폭을 과대추정 — 그 뒤 글자의 그리드 추정 시작 위치가 통째로 밀려서, 이어지는 잉크 스냅 보정(`findInkEdge`)이 엉뚱한 경계에 스냅되는 2차 피해로 번졌다. `computeSlotWeightsHorizontal`에 위치 무관 반각 ASCII 전용 좁은 가중치(`NARROW_ASCII_RE`/`NARROW_ASCII_WEIGHT`)를 추가 — 잉크 스냅은 한 번 꺼봤다가(다른 여러 줄이 짧아지는 회귀 발생, 대부분 정상 줄엔 순기능이 있었던 것으로 판단) 다시 켜서 함께 적용.
      - [x] **(검증 중 발견) 언어가 영어로 수동 고정돼 있으면 세로쓰기 중국어/일본어가 통째로 Tesseract(가로쓰기 영어 인식)로 잘못 처리됨** — 실사용 중 확인: 설정에서 언어를 영어로 고정해둔 상태로 세로쓰기 2페이지를 테스트했더니 텍스트 박스가 태반 안 생기고 팝업도 비었음. `langDetect.ts`의 "설정에서 언어를 수동 지정하면 OSD 자동 감지를 건너뛰고 그 값을 바로 쓴다"(2026-07-30, tier1/tier2 확장 작업에서 도입)는 의도된 동작이었음 — 버그 아님, 자동 판별로 바꾸면 정상 동작. 다만 이 실패 모드(OSD 로그가 아예 안 찍히고 0ms 만에 강제 언어로 떨어짐)를 처음 겪으면 원인 특정이 오래 걸리므로 기록해둠.
    - [x] **(2026-07-30) 선택 모드 중 화면 전환 애니메이션 도중 캡처돼 DocLayout이 한 열을 둘로 오검출하는 문제 완화** — 실사용 중 확인(2페이지 세로쓰기 테스트 도중 왼쪽 페이지 내용이 잠깐 바뀜): 재추출 후 왼쪽 영역의 노란 블록(자동 탐지 시각화)이 2개로 쪼개지고 열 아래쪽에만 텍스트 박스가 생김 — `blocks-*.json` 덤프로 원인 확인, 왼쪽 영역 블록들 신뢰도가 0.30~0.46(정상측 0.70~0.76 대비 크게 낮음)에 서로 겹치는 중복 검출이 7개나 잡혔고, 원래 하나였어야 할 영역이 간격 6.6px 짜리 두 `column` 인덱스로 쪼개짐 — 화면이 아직 다 안 그려진 순간(전환 애니메이션 도중)에 캡처된 것으로 판단. `changeWatcher.ts`(`SETTLE_DELAY_MS=800` 안정 대기는 이미 있었음)를 두 가지로 보강: (1) 안정 대기가 끝나 실제로 추출을 시작하기 **직전**에 한 번 더 캡처해서 비교 — 폴링 주기(500ms) 사이의 미세한 변화가 개별적으론 임계값(2%)을 안 넘어도 누적되면 전환이 아직 안 끝났을 수 있어서, 그 사이에도 바뀌었으면 건너뛰고 안정 대기를 다시 건다(`onSettled`). (2) 추출이 진행 중(`regionRefreshInFlight`)일 때 새 변화가 감지되면 예전엔 그냥 건너뛰고 끝이었는데(그 변화가 딱 한 번만 있고 그 뒤로 화면이 잠잠해지면 아무도 재추출을 다시 안 걸어서 전환 도중의 불완전한 결과가 그대로 굳어버림) — `pendingRetrigger` 플래그로 기억해뒀다가 진행 중이던 추출이 끝나자마자 안정 대기를 다시 건다(`runExtractionCycle`). 부수 효과로 같은 세션에서 겪었던 "세로쓰기 열 하나를 클릭했는데 처음 두 글자만 선택됨"(NDLOCR 줄 검출이 잉크 옅은 구간에서 하나의 열을 둘로 쪼개 검출했을 가능성 — `fillInterLineGaps` 참고) 증상도 재현이 안 되게 됐음(사용자 확인) — 근본 원인이 같은 "불안정한 순간의 캡처"였을 가능성.
      - [x] **(2026-07-30) OSD 스크립트 오판정으로 세로쓰기 중국어/일본어가 통째로 영어(Tesseract)로 잘못 처리되는 문제 수정** — 한자 위주 세로쓰기 화면에 애매한 글자 하나(×/X 등)가 섞여 있으면 Tesseract OSD가 전체 스크립트를 Latin으로 오판정(`langDetect.ts`, `OSD 결과: {script: 'Latin', scriptConfidence: 0.48}`)하고, 그 뒤 대표 언어(영어)로 러프 인식해봐도 텍스트가 거의 없어 eld 재판별도 실패(`raw=yo` 등 라틴 후보 밖 결과)해 결국 대표 언어(영어)로 폴백했다 — OSD `script_confidence`는 정상 판정에서도 낮게(0.29~0.48) 나오는 경우가 많아 신뢰도 임계값만으로는 오판정을 구분할 수 없음을 확인, 대신 `resolveAmbiguousScript`의 eld 재판별 실패 분기에서 러프 인식 결과에 한자가 과반(`HAN_CROSSCHECK_RATIO=0.5`) 이상 섞여 있으면 대표 언어 대신 `detectChineseScript`로 재분류하도록 수정. 사용자 확인(문제 영역엔 진짜 영어 텍스트가 없었고 ×/X 하나만 애매했음)으로 이 교차 확인이 안전하다고 판단.
        - [x] **(2026-07-30) 대칭 버그 — 영어가 아랍어/러시아어(Arabic/Cyrillic)로 오판정되는 문제 2건 연쇄 수정**(한자 교차 확인과 반대 방향, 사용자 제보):
          - **eld 재판별 실패 시 라틴 교차 확인 추가**: OSD가 (당시 가정으론 기울임체 폰트 탓으로) 영어를 Arabic으로 오판 — Arabic 대표 언어(ar)로 러프 인식 후 eld 재판별까지 실패하면, 영어 팩으로 한 번 더 인식해 라틴 문자 비율이 높으면(`LATIN_CROSSCHECK_RATIO=0.7`) 그쪽으로 재분류하는 `crosscheckLatin` 신설(한자 교차 확인과 대칭 구조).
          - **eld 요구 조건 제거**: 위 수정 후에도 재현 — 짧은 화면 캡처 표본에서 eld 가 "이건 영어다"까지 확신 있게 판별을 못 하면(문자 구성은 명백히 라틴인데도) 안전망 자체가 무력화됨을 확인. 이 안전망의 목적은 "구체적으로 어떤 라틴 언어인지"가 아니라 "스크립트 판정 자체가 틀렸는지"만 확인하면 충분하므로, eld 성공 시 그 세부 언어를 쓰고 실패해도 문자 비율만으로 대표 언어(en)로 확정하도록 단순화(`crosscheckLatin`이 더 이상 null을 반환하지 않음, 비율 임계값 미달일 때만 예외).
          - **eld가 "성공"으로 착각하는 경우까지 재검토**: 위 두 수정 모두 "eld 재판별 실패" 분기에서만 도는데, 이번엔 eld가 러시아어 팩으로 억지로 읽은 가짜 텍스트를 "진짜 러시아어"라고 확신 있게 오판(로마자/키릴 문자는 O/A/E 등 생김새가 같은 글자가 많아 그럴듯한 키릴 문자가 나옴)해 재판별이 "성공"해버려서, 실패 시에만 도는 라틴 교차 확인까지 도달을 못 함 — eld가 후보 목록 안 언어로 성공 판별했더라도, 그 원문 자체가 라틴 문자 위주면(모양만 비슷한 가짜 문자였을 가능성) 라틴 교차 확인을 한 번 더 거치도록 보강(`resolveAmbiguousScript`).
          - **최종적으로 라틴 교차 확인을 맨 앞으로 이동**: 위 재검토 로직도 실측(`재판별: ar → ar`)에서 뚫림을 확인 — "대표 언어팩으로 인식한 결과의 라틴 문자 비율"로 재검토 여부를 판단하는 방식 자체가 틀렸다는 게 드러났다. 아랍어/러시아어 인식기는 출력 자체가 그 언어 문자 체계로 제약돼 있어서, 원본이 진짜 아랍어든 오판된 영어든 그 출력엔 라틴 문자가 사실상 안 나온다 — 판단 재료 자체가 항상 "라틴 아님"으로 나와 재검토 조건이 트리거될 수 없었다. 유일하게 신뢰할 수 있는 신호는 "영어 팩으로 직접 인식했을 때 실제로 라틴 문자가 나오는가"뿐이므로, en 이 후보에 없는 스크립트는 eld 결과와 무관하게 대표 언어 재판별을 시도하기 전에 항상 먼저 라틴 교차 확인부터 하도록 순서 자체를 재구성.
      - [x] **(2026-07-30) OSD 스크립트 오판정의 반대 방향 — 기울임 폰트 영어 문단이 아랍어로 오판되는 문제 수정** — 크고 선명한 영어 문단(해리포터 텍스트, 데스크톱 메모 앱)이 종종 `ar`로 자동 판별된다는 사용자 제보. 원인: 기울임(oblique/italic) 계열 폰트는 획이 일관되게 기울고 이어져 보여 Tesseract OSD의 획 방향 통계가 Arabic 스크립트 쪽으로 쏠린다(알려진 약점) — 한 번 Arabic으로 찍히면 `resolveAmbiguousScript`가 `ar` 언어팩으로 러프 인식(영어라 결과가 깨짐) → eld 재판별 실패 → 한자 교차 확인도 해당 없음 → 대표 언어 `ar`로 확정되는 구조였고, 위 121번(Latin↔Han)과 달리 Latin↔Arabic 오판을 되돌릴 안전망이 없었다. `HAN_CROSSCHECK_RATIO`와 대칭인 라틴 교차 확인(`crosscheckLatin`)을 추가: 비라틴 후보군(Arabic/Cyrillic/Devanagari)에서 eld 재판별이 실패하면 영어 언어팩으로 한 번 더 러프 인식해, 결과가 대부분 ASCII 라틴 글자(`LATIN_CROSSCHECK_RATIO=0.7` — 진짜 아랍/키릴 텍스트도 영어 모델로 읽으면 깨진 라틴 글자가 어느 정도 나오므로 한자 교차 확인(0.5)보다 보수적으로)이면서 eld가 라틴 후보 중 하나로 판별하면 그 언어로 재분류.
      - [x] **(2026-07-30) 영어(Tesseract) 다단 인식 품질 문제 일괄 수정** — 사용자가 영어 2단(호빗 텍스트) 실사용 테스트로 여러 문제를 연쇄로 발견·수정. **PaddleOCR로 영어 엔진 자체를 바꿔보는 실험도 시도했으나 기각**(아래 참고) — 결국 Tesseract를 그대로 쓰되 크롭/여백/해상도를 튜닝하는 방향으로 정착:
        - **열 사이 노이즈**: 열 크롭 여백(`BLOCK_PADDING_X=50px`)이 선택 영역 전체 경계만 알고 옆 열은 몰라서, 열 사이 간격(거터)이 50px보다 좁으면 그 여백이 옆 열까지 뻗어 들어가 옆 열 글자가 섞였다 — 각 열의 크롭 경계를 이웃 열과의 중간 지점까지로 제한(`ocr.ts` `columns.length > 1` 분기).
        - **어느 지점부터 클릭 박스-팝업 본문 미스매치**: 다단 경로가 `text: texts.join('\n\n')`로 열 사이에 구분자를 넣는데 `words` 배열엔 그 구분자에 해당하는 항목이 없어서, `text = words.map(w=>w.text).join('')` 불변조건이 열 경계마다 깨져 있었다(2단이면 정확히 첫 열 경계부터 오프셋이 밀림) — `texts.join('\n\n')`과 정확히 같은 지점에 bbox 없는 `'\n\n'` Word를 끼워 넣도록 수정.
        - **다단 열 맨 위 줄 등 일부 단어의 클릭 박스가 안 뜸(팝업 본문엔 있음)**: 처음엔 크롭 상단 경계 문제로 의심해 `BLOCK_PADDING_Y`를 12→20으로 올려봤으나 효과 없었음 — 새로 추가한 진단 로그(`[ocr] 잘린 단어로 bbox 제외: ... clippedByRegion=... truncated=...`)로 실측한 결과 전부 `clippedByRegion=false, truncated=true`였다. 즉 경계 문제가 아니라 "This"(confidence 59), "in"(57), "the"(57)처럼 멀쩡한 흔한 단어들도 신뢰도가 `MIN_WORD_CONFIDENCE=60` 문턱 밑으로 나오는 순수 신뢰도 문제 — 이 문턱은 예전에 "정상 단어 최저 86, 진짜 깨진 단어 46" 실측으로 잡은 값인데 이번 캡처(축소된 화면)에는 안 맞았다. 문턱을 낮추는 대신 근본 원인에 대응: 기존엔 Tesseract의 `SetRectangle`로 원본 전체 이미지에 사각형만 지정해 해상도가 원본 그대로였는데, region을 실제로 크롭해 2배 확대한 뒤 그 위에서 인식하도록 변경(`recognizeRegion`) — Tesseract는 해상도가 낮을수록 confidence 도 체계적으로 낮아지는 특성이 있어, 업스케일로 같은 글자의 confidence 자체를 끌어올림. 반환된 bbox는 확대된 크롭 좌표계라 원본 이미지 절대좌표로 즉시 되돌려(하류 로직은 그대로 원본 좌표 계약을 유지) 실측 확인 — 사용자 재테스트로 관련 문제(UNEXPECTED 제목 글자 누락, "This" 등 박스 누락) 전부 해소 확인.
        - [x] ~~**Tesseract를 PaddleOCR로 교체하는 실험**~~ — 이미 CJK에 쓰던 PaddleOCR 인프라를 영어에도 재사용(env `FORCE_PADDLE_EN`, `Language` 타입에 en이 이미 포함돼 있어 타입 변경 없이 연결 가능했음)해 A/B 비교. 시도 중 두 가지 구조적 결함을 더 발견해 같이 고쳤었음(1: PaddleOCR 인식 결과는 공백 토큰을 걸러내(`python recognize()`) 그냥 이어붙이면 단어가 다 붙어버림 — bbox 없는 공백/줄바꿈 Word로 보정, 2: 문장부호 앞에도 공백이 붙던 회귀 — 문장부호는 예외 처리, 3: 검출된 줄마다 무조건 개행해 문단 구조가 사라짐 — 줄 사이 y간격으로 진짜 문단 경계와 단순 줄바꿈을 구분하도록 보강) — 하지만 사용자 판단으로 **속도가 너무 느려 최종 기각**, 관련 코드는 이전 `FORCE_TESSERACT` 실험과 동일하게 흔적 없이 원상복구.
        - **(업스케일 도입 후 다른 파일에서 새로 발견) em-dash("—")가 거의 인식이 안 됨**: Electron `nativeImage.resize()`가 명시 안 하면 기본값이 이미 `quality: 'best'`(가장 매끄러운 보간)였는데, em-dash 처럼 아주 얇은 가로 획은 부드러운 보간으로 확대하면 대비가 흐려져(주변과 섞임) Tesseract가 아예 못 보게 되는 것으로 추정 — 굵은 글자 획엔 도움 되는 보간이 얇은 단일 획엔 역효과. `quality: 'good'`(더 거친/덜 매끄러운 보간)으로 명시해 얇은 획의 대비 손실을 줄임 — 사용자 재테스트로 해결 확인, 다른 글자 정확도 회귀 없음.
      - [x] **(2026-07-30) 선택 영역 안에서 드래그(또는 PDF 클릭 시 나타나는 드래그형 하이라이트)를 취소하면 재추출이 불필요하게 오래 걸리거나 팝업이 안 뜨는 문제 다건 수정** — 실사용 제보로 순차 발견·수정:
        - **감지 주기/안정 대기 시간 조정**: `POLL_INTERVAL_MS`/`SETTLE_DELAY_MS`를 500/800 → 1000/1000으로 통일(`changeWatcher.ts`) — 사용자가 제시한 "감지 → 1초 재확인 → 안정이면 시작, 아니면 다시 1초" 상태 흐름과 기존 구현이 이미 거의 동일함을 확인, 상수만 정리.
        - **드래그하다 취소해 결국 마지막 추출 시점과 동일한 화면으로 되돌아온 경우, 재추출 자체를 건너뛰고 캐시 복원**: 기존엔 "화면이 안정됐는지"만 봐서 이런 순변화 없는 경우도 매번 전체 재추출(OCR 정상도 15~20초)을 돌렸다 — 마지막으로 실제 추출에 쓰인 화면(`lastExtractedBitmap`)을 기록해뒀다가, settle 시점에 현재 화면과 비교해 동일하면 `sendOverlayWords`/`sendDebugBlocks`를 캐시(`peekCachedExtraction` — `getExtraction()`과 달리 캐시가 비어 있어도 새 추출을 트리거하지 않는 부작용 없는 조회 함수를 신설)된 결과로 즉시 복원(`onSettled`).
        - **드래그를 잡고 있는 동안은 애초에 추출을 시작하지 않음**: 진단 로그로 "드래그 하이라이트가 뜬 화면이 1초간 안정된 것으로 오판정돼 그 화면 기준으로 추출이 시작"되는 것을 직접 확인 — 마우스 왼쪽 버튼이 눌려 있는지 실시간으로 아는 게이트를 새로 추가(win32: `inputHook.ts`의 저수준 후크에 `WM_LBUTTONDOWN`/`UP` 추적 추가, macOS: `macInput.ts`에 권한 불필요한 `CGEventSourceButtonState` 질의 추가)해, 버튼이 눌려 있으면 settle 판정 자체를 미루도록 수정(`onSettled`).
        - **이미 시작된 "오염된" 추출(드래그 화면 기준)을 취소 시점에 폐기**: 위 개선에도 드래그를 오래 잡고 있다 취소하면 그 사이 이미 추출이 시작돼 있을 수 있는데, 이 진행 중인 추출을 못 끊으면 (a) 그 사이 클릭한 팝업이 `getExtraction()`의 inFlight 대기 때문에 최대 1분까지 안 떴고 (b) 완료되면 오염된 결과가 방금 복원한 멀쩡한 캐시/오버레이를 덮어썼다 — `extractionCache.ts`에 `abandonInFlightExtraction()`(inFlight 참조만 비워 `refreshExtractionCache`의 기존 커밋 가드가 결과를 버리게 함, 이미 시작된 비동기 작업 자체는 못 끊지만 "더 새 호출로 이전 결과 무시"하는 기존 메커니즘과 동일 원리) 추가, 복귀 판정 시 진행 중이던 사이클을 이걸로 폐기.
        - **(폐기 로직 도입 후 새로 드러난 회귀) 취소 후 텍스트 박스 크기가 전반적으로 짧아짐**: 원인은 노란 자동 탐지 블록(`sendDebugBlocks`)이 단어 박스(`sendOverlayWords`)와 달리 커밋 가드 없이 OCR 완료 즉시 전송되고 있었던 것 — 폐기된(오염된 화면 기준) 추출도 이 블록만은 그대로 덮어써서 더 짧게/쪼개져 그려졌다. `CachedExtraction`에 `debugBlocks` 필드를 추가해 단어 박스와 동일한 커밋 가드 안에서만 전송하고, 캐시 복원 시에도 같이 복원하도록 수정(`extractionCache.ts`/`changeWatcher.ts`). 부수 방어로 폐기된 사이클이 `setRegion`으로 영역을 이미 좁혀놨을 경우, 복귀 시점에 마지막 커밋 당시 영역(`lastExtractedRegion`)으로 되돌리는 처리도 추가.
      - [x] **(2026-07-29) Yomitoku 검출 도입** — 위 필터 조정 이후에도 일부 페이지(지진 뉴스 NHK 페이지)에서 후리가나가 여전히 노이즈로 섞이는 사례 발견 — 원인은 PaddleOCR `TextDetection`이 페이지에 따라 **후리가나와 본문 한자를 아예 하나의 검출 박스로 합쳐버림**(rect 기반 필터로는 원리적으로 못 잡음, 검출 단계 자체의 문제). 대응으로 Yomitoku(일본어 특화 OCR)를 검출 전용으로 도입: `python/ocr_yomitoku.py`(`TextDetector`만 로드 — 처음엔 `OCR`(검출+인식 통합) 클래스를 썼는데 인식 단계가 병목이라(실측: 검출 4.7초 vs 인식 44.8초, 워커 풀 없이 단일 프로세스 순차 처리) 인식은 빼고 검출만 남김), `ocrYomitoku.ts`(`detectLinesWithYomitoku` — 가로쓰기 전용, 세로쓰기는 다루지 않음).
      - [x] **(2026-07-29) 중국어 가로쓰기 경로에서 후리가나 필터 건너뛰기 — Windows/macOS 공통**(순수 JS 로직, OS 분기 없음). `recognizeLinesWithPaddle`이 언어와 무관하게 `excludeFuriganaHorizontal`을 항상 거쳤는데, 후리가나는 일본어 전용 표기라 중국어(zh-Hans/zh-Hant) 줄엔 애초에 안 걸려 사실상 no-op이었음 — `language === 'ja'`일 때만 필터를 타도록 명시 분기(`ocrPaddle.ts: recognizeLinesWithPaddle`). 불필요한 검사 제거 + 중국어 경로에 일본어 전용 필터가 걸린다는 오해 방지 목적, 동작 변화는 없음(중국어 결과는 필터 적용 전후 동일).
        - [x] Tesseract.js로 가로쓰기 인식을 통째로 대체해보는 실험 — **기각**. `FORCE_TESSERACT` env 스위치로 A/B 비교해봤는데 정확도가 크게 떨어져 사용자가 바로 취소 요청, 관련 코드(`ocr.ts`의 스위치 분기)는 원상복귀함(코드베이스에 흔적 없음).
        - [x] PaddleOCR 인식 모델을 가벼운 `PP-OCRv6_small_rec`로 교체(가로쓰기 경로만, 세로쓰기는 그대로 `medium`) — `ocr_paddle.py: get_engine`이 `(lang, model_name)` 쌍으로 엔진을 캐싱해 요청마다 다른 모델을 쓸 수 있음(`rec_model` 필드, 하위호환: 없으면 기존 기본값). 정확도는 괜찮았으나 여전히 "이전과 다른 노이즈"가 섞인다는 재보고를 받아 결국 아래 NDLOCR-lite 조합으로 대체 — 이 모델 자체는 지금도 가로쓰기 PaddleOCR 폴백 경로(`recognizeLinesWithPaddle`)에 남아있음.
        - [x] **(2026-07-29) Yomitoku 검출 + NDLOCR-lite 인식 조합으로 최종 전환, 실사용 검증 완료** — 사용자 제안("detector/recognizer는 서로 독립된 모델이라 자유롭게 섞어 쓸 수 있다")에 따라, 이미 세로쓰기에 쓰던 NDLOCR-lite(`python/ocr_ndlocr.py`)에 검출 없이 크롭 하나만 읽는 `recognize_crop` 모드를 추가하고, Node 쪽에 `recognizeLinesWithNdlocr`(`ocrNdlocr.ts`)를 새로 만들어 Yomitoku가 찾은 줄들을 이걸로 읽는다(`ocr.ts: runNonTesseractOcr`가 언어 ja면 우선 시도, 실패 시 PaddleOCR 경로로 폴백). 원래 고문서용 모델이라 현대 인쇄 가로쓰기에서 정확도가 어떨지 미검증이었던 우려는 실사용으로 해소됨 — **"시간도 엄청 단축됐고 노이즈 없이 잘 나온다"**로 확인(사용자 검증). `LINE_PADDING_Y`(세로쓰기용으로 튜닝된 상수를 가로쓰기에도 공용으로 쓰던 문제로 추정됐던 옛 가설)는 이 새 경로가 처음부터 별도의 비대칭 여백(`LINE_MARGIN_TOP=1`/`LINE_MARGIN_BOTTOM`, 아래 DPI 항목 참고)을 쓰도록 설계돼 우회됐다 — PaddleOCR 쪽 `padLine`/`LINE_PADDING_Y` 자체는 손 안 댔고(폴백 경로에만 남음, 세로쓰기와 공유), 그 가설이 맞았는지는 결국 직접 검증하지 않음.
          - [x] **(2026-07-29) 여백을 절대 픽셀 → 줄 높이 비례로 수정(DPI 배율 대응)** — 사용자가 노트북(150%)/모니터(100%)처럼 디스플레이 배율이 다른 두 화면에서 결과가 달라지는 문제 보고("노트북에서 줄이 통째로 누락되거나 순서가 꼬임") — 캡처가 물리 픽셀 기준이라 배율이 높을수록 같은 글자가 더 큰 물리 픽셀로 찍히는데, 여백은 고정 픽셀(1px/4px)이라 배율이 높을수록 실제 글자 크기 대비 여백이 상대적으로 좁아져 획/문장부호가 잘리기 쉬워진다는 게 유력 가설 — `LINE_MARGIN_TOP_RATIO`(0.04)/`LINE_MARGIN_BOTTOM_RATIO`(0.16)로 줄 자신의 높이에 비례하도록 수정(`ocrNdlocr.ts: writeLineCrop`). **재현이 간헐적이었고(같은 화면·같은 코드로 재시도했더니 문제 없이 재현되기도 함) 이 수정이 실제 원인을 고쳤는지는 검증되지 않음** — DPI와 무관하게 항상 더 안전한 방향의 수정이라 일단 반영, 재발하면 다시 조사 필요.
        - [x] **(2026-07-29) 가로쓰기 단어 단위 hover 시도했다가 줄 단위로 되돌림** — "가로쓰기도 최대한 단어별로" 요청에 따라 `lineId`를 안 붙이는 방식으로 단어 단위 hover/선택을 구현(`groupCjkCharsGrid`가 만드는 단어별 bbox를 그대로 사용, `wordMapping.ts`/`selection/index.ts`의 기존 "lineId 없음 = 단어 하나" 폴백 재활용 — 두 파일은 무변경). 단어 박스 위치 정확도를 여러 차례 튜닝했으나(아래 항목들) 사용자 판단으로 **잉크 기반 튜닝은 나중으로 미루고 우선 세로쓰기와 동일하게 줄 전체 박스로 되돌림**(`ocrPaddle.ts: recognizeOrderedLines`/`ocrNdlocr.ts: recognizeLinesWithNdlocr` 둘 다 다시 `lineId` 부여) — `groupCjkCharsGrid`의 단어별 위치 계산 자체(그리드+잉크 스냅)는 삭제하지 않고 그대로 남겨둠, 나중에 이 `lineId` 부여만 다시 빼면 단어 단위로 즉시 복귀 가능.
          - [x] 단어 박스 위치를 그리드(균등 칸 폭 가정)로 계산 — 세로쓰기용 `computeSlotWeights`(縦中横 2자리 숫자만 압축)와 별도로 가로쓰기 전용 `computeSlotWeightsHorizontal` 추가(반각 숫자는 자릿수 무관 항상 0.5, 줄 양 끝 문장부호만 0.4) — 사용자 지적(숫자 가중치, 문장부호 폭) 반영.
          - [x] **잉크 위치 기반 경계 보정 실험** — 그리드 추정 경계를 실제 캡처 이미지의 "잉크 없는 열"에 스냅(`computeColumnDarkness`/`snapToInkGap`/`findInkEdge`, `ocrPaddle.ts`) — 단어 시작/끝 경계는 방향을 정해(공백→잉크, 잉크→공백) 훑어서 문장부호와 다음 글자 사이 공백이 박스에 안 딸려오게 함. 사용자 3가지 지적(숫자·문장부호가 박스에 포함됨/문장부호 앞뒤 공백 포함됨) 중 앞 두 개는 `WORD_EDGE_TRIM_RE`로 토큰 양 끝 숫자·문장부호를 잘라내고 gap 문자로 넘기는 방식, 뒤 하나는 위 방향성 잉크 스냅으로 대응 — **실사용 검증 전 상태에서 줄 단위로 되돌아가 보류 중, 나중에 이어서 튜닝**. 코드(`groupCjkCharsGrid`의 `image` 파라미터, `computeColumnDarkness`/`snapToInkGap`/`findInkEdge`, `WORD_EDGE_TRIM_RE`)는 그대로 유지됨.
    - [x] **(2026-07-30) 팝업 문단 분리/불필요한 줄바꿈 정리 — 일본어 세로쓰기(NDLOCR) 첫 케이스** — 사용자 요청("문단 분리, 불필요한 줄바꿈 삭제 위주로 팝업 텍스트 포맷 다듬기", 세로쓰기부터 시작)으로 `alignColumnStarts`(`ocrNdlocr.ts`)의 열별 문단 시작 판정을 보강. 기존엔 기하학적 들여쓰기(`nearestMultiple===1`, 열이 기준선보다 한 칸 아래에서 시작)만 신호로 썼는데, 실사용 재현 페이지에서 이 신호가 못 잡는 두 가지 케이스가 발견돼 연쇄로 추가:
      - **대사/독백 줄이 들여쓰기 없이 맨 앞 기호(―「『)만으로 새 문단을 표시하는 조판 관례** — 실측(`DEBUG_OCR_DUMP`) 확인 결과 이 소설은 그런 줄의 `rawRatio`가 정확히 0.000(들여쓰기 전혀 없음)이라 기하학적 판정으로는 원리적으로 못 잡는 케이스였음 — `PARAGRAPH_LEADING_MARK_RE=/^[「『―—]/`로 줄 첫 글자 기준 독립 신호 추가(기존 들여쓰기 판정과 무관하게 OR로 결합).
      - **위 두 신호로도 못 잡는 나머지 케이스(사용자가 직접 열 인덱스로 제보)** — 사용자 제안("이전 열 y좌표 차이로 구분하면 안 되나")에서 출발: 문단이 열 중간에서 끝나면 그 열은 페이지의 공통 하단(`computeCommonBottom`, `computeBaseline`과 대칭 구조, 열들의 bottom 최빈값)까지 못 채우고 짧게 끝난다는 게 조판 관례와 무관하게 거의 항상 성립하는 구조적 신호임을 확인 — 이전 열이 공통 하단보다 뚜렷이(`SHORT_COLUMN_GAP_RATIO`×칸 크기 이상) 짧으면 다음 열을 새 문단으로 판정. **다만 이 신호 하나만으로는 사용자가 신뢰하지 않음**("문단이 우연히 열 끝까지 딱 맞아떨어지는 경우"엔 이 신호가 아예 안 뜨는 게 이미 알려진 한계) — 실측(i=7 케이스)으로 이 우려가 실제로 재현됨을 확인: 이전 열이 공통 하단보다 겨우 9px(약 0.5칸) 모자란데도 문턱(처음엔 1칸)에 못 미쳐 놓쳤고, 반면 진짜 "이어지는" 열들의 자연스러운 측정 오차는 0~3px 수준이었음 — 문턱을 0.3칸으로 낮추고, 사용자 요청대로 **"다음 열(=이번 열) 자신이 이어지는 열처럼 보이는지"까지 함께 봐서 최종 판단**하도록 게이트 추가: 이 페이지 실측 결과 이어지는 열은 전부 `nearestMultiple=-1`(기준선보다 한 칸 위로 어긋남), 새 문단인 열은 전부 `nearestMultiple=0`(기준선에 정확히 맞음)이었음 — 이번 열이 `nearestMultiple<0`(이어짐처럼 보임)이면 짧은-이전-열 신호를 적용하지 않도록 제한. "이전 열이 짧다" + "이번 열이 이어짐 신호를 안 보인다" 둘 다 맞아야 최종적으로 새 문단 처리. 사용자 재테스트로 해결 확인("해결됐어").
      - PaddleOCR 쪽 `recognizeOrderedLines`(가로쓰기/세로쓰기 공용)는 아직 열 경계마다 무조건 `'\n'`을 넣어 들여쓰기/이어짐 구분이 전혀 없음(연구 조사로 확인된 구조적 차이) — 다음 케이스로 다룰 후보이나 아직 착수 안 함.
  - [x] ~~중국어 간체/번체 자동 판별 — `TESS_LANG.zh` 를 `chi_sim` 단일 언어팩에서 `chi_sim+chi_tra`(Tesseract 가 `+` 로 합쳐진 문자열을 내부에서 분리해 두 언어팩을 함께 로드, `createWorker.js` 확인됨)로 변경. 사용자가 간체/번체를 미리 고르지 않아도 Tesseract 가 글자마다 더 맞는 사전으로 인식(`ocr.ts` `TESS_LANG`).~~
  - [x] ~~일본어·중국어 OCR 단어 재분할(의미 단위) — Tesseract 자체 단어 경계는 공백이 없는 CJK 에서 신뢰하기 어려워서, 줄(line) 단위로 글자(symbol) bbox 를 모두 이어붙인 뒤 언어별 형태소 분석기로 다시 분리하도록 변경(`ocr.ts` `buildCjkLineWords`). 일본어는 kuromoji(IPADIC, `main/nlp/japanese.ts`), 중국어는 segmentit(jieba 스타일, 순수 JS라 네이티브 빌드 불필요, `main/nlp/chinese.ts`). 잘린 단어 제외(`isWordClippedByRegion`/`looksTruncated`) 판정은 기존처럼 Tesseract 단어 단위로 먼저 하고, 살아남은 단어들만 연속 구간(run)으로 이어 분석기를 돌려 필터링된 자리에서 단어가 잘못 이어붙지 않게 함. 각 단어의 bbox 는 가로(x0/x1)만 구성 글자 bbox 들의 min/max 로 계산하고, 세로(y/height)는 기존 `splitWordBySymbols` 와 동일하게 줄(line) bbox 를 그대로 써서 같은 줄 단어들의 높이를 통일함(`buildCjkLineWords`). kuromoji 사전(.dat.gz)과 segmentit 사전 데이터가 실제 파일 경로로 로드돼야 해서 두 패키지 모두 `electron.vite.config.ts` main 번들 external 에 추가(tesseract.js 와 동일 이유 — 인라인되면 자기 패키지 폴더 기준 상대경로 계산이 깨짐). kuromoji 사전 로드(약 1초)는 앱 시작 시 미리 예열(`main/index.ts` `warmJapaneseTokenizer`)해 첫 사용 지연을 없앰.~~ → **segmentit 제거(2026-07-30)**: 2026-07-28 중국어 분절이 jieba/chinese-tokenizer 스위치(`main/nlp/chinese.ts`)로 교체된 뒤 어디서도 import되지 않던 채 `node_modules`에만 남아있던 것(15M, 전이 의존성 포함 23개 패키지)을 확인하고 `package.json`/`electron.vite.config.ts` external 목록/타입 선언 파일(`segmentit.d.ts`)에서 걷어냄. kuromoji는 이미 2026-07-28에 별도로 제거됨(239번 항목 참고). 같은 이유로 미사용인 `lindera-wasm-nodejs-ipadic`(13M, `JA_ENGINE` 스위치가 sudachi 고정이라 코드는 있지만 호출 안 됨)은 개발자 전용 엔진 전환 스위치 용도로 의도적으로 남겨둠(사용자 확인, 제거 안 함).
- [x] ~~좌표 기반 노이즈 제거(제목·페이지번호·메뉴바·상태표시줄)~~ → **위치 휴리스틱 방식은 삭제함**(`ocr.ts: removeNoise` 및 관련 상수/헬퍼 제거). "OCR 대상 영역 지정" 기능이 생기면서 사용자가 드래그로 본문만 감싸면 메뉴바·상태표시줄·페이지 번호는 애초에 캡처/OCR 대상에서 빠지게 됐고, 그러면서 이 휴리스틱은 "주로 걸러내는 로직"이 아니라 오탐 위험을 감수하는 보조 안전망 정도로 위상이 축소됐었음 — 실효성 대비 유지 비용(정확도 낮음, 언어별 단어 목록 관리 필요)이 안 맞아서 코드째로 걷어냄. 필요해지면 그때 다시 붙이거나 아래 반복 기반 방식으로 대체.
- [ ] 반복 기반 노이즈 제거 — 같은 창을 여러 번 캡처했을 때 같은 위치에 반복되는 텍스트(헤더/워터마크 등)를 찾아 제거하는 방식. 최근 캡처 몇 개를 기억해두는 히스토리 저장소가 새로 필요하고, 첫 캡처에는 비교 대상이 없어 효과가 없음(재진입을 몇 번 해야 누적됨). **우선순위 낮음** — 영역 지정 기능으로 메뉴바/상태바 문제 자체가 대부분 해소돼서, 이 방식이 굳이 필요한 남은 케이스(제목처럼 애매한 텍스트가 지정 영역 안에 반복적으로 딸려오는 경우 등)가 많지 않음. 필요성이 실사용에서 재확인되면 그때 추가.
- [ ] 페이지 경계 문장 이어붙이기 — 미구현.

<a id="b-담당"></a>

## 🅱️ 담당 B — 선택 확정 & 질문 (팝업 후)

<a id="b-select"></a>

**팝업 선택 & 문맥 확정**
- [x] ~~A가 넘긴 근방 텍스트·단어 좌표를 팝업에 표시 (`PopupScreen.tsx` + `popup/ContextView.tsx`; `getPopupContext()`/`onPopupContext()` 로 수신, 없으면 목업 fallback)~~
- [x] ~~팝업 내 범위 지정 — 영어=단어(atom) 단위, 하이픈 단어는 조각별 선택 가능(`popup/selection.ts`). 중국어(한자)는 글자 하나가 atom 하나(문자 단위 세밀 선택). 일본어는 한자는 마찬가지로 글자 단위, 가나가 섞이면 kuromoji 형태소 분석(IPC `TOKENIZE_JA` → `main/nlp/japanese.ts`) 결과의 품사 태그로 의미 단위 병합 — 조사(助詞)는 항상 독립 atom, 조동사(助動詞)로 시작하는 토큰만 앞 atom 에 이어붙여 동사 어간+어미를 하나로 취급(예: "渡った" → 渡 / った, `selection.ts` `segmentKanaRunWithTokens`). kuromoji 결과가 도착하기 전 짧은 순간은 Intl.Segmenter 기반 즉석 대체 규칙(`segmentKanaRunFallback`)으로 먼저 그리고, 도착하면 `PopupScreen.tsx` 가 재계산해 교체.~~
- [x] ~~팝업 미리보기 데모를 언어별(영어/일본어/중국어) 3개 버튼으로 확장 — 기존 호빗 "well-to-do" 목업 하나뿐이던 것을, 일본어(《容疑者Xの献身》"新大橋")·중국어(《三体》"天线") 목업을 `popup/mockSelection.ts` 에 추가(`mockDevotionExtraction`/`mockThreeBodyExtraction`, 대상 단어가 원문에 여러 번 나올 때 몇 번째 등장을 anchor 로 쓸지 지정하는 `occurrence` 옵션 포함). `MainScreen.tsx` 데모 버튼이 `window.nuance.openPopup(demo?)` 로 언어 태그를 넘기면 `main/windows.ts` `createPopupWindow(ctx, demo)` 가 팝업 창을 `#/popup?demo=ja|zh` 해시로 로드하고, `App.tsx` `getRoute()` 는 해시의 `?` 앞부분만 라우트로 보도록 수정(쿼리 무시), `PopupScreen.tsx` 가 그 쿼리값으로 초기 목업을 선택. 담당 A 선택 파이프라인이 실제 `ExtractedSelection` 을 넘기기 시작하면 이 데모 버튼들은 제거될 임시 기능.~~ → **번체(zh-Hant) 데모 추가(2026-07-29)**: ja/zh-Hans 는 실존 소설 발췌였지만 이번엔 실제 저작물을 정확히 재현할 자신이 없어, 대신 PLAN.md §5.2 "맥락 의존 발음"이 이미 예시로 든 `行`(xíng·háng) 다의어 판정을 직접 겨냥해 짧은 대만 배경 이야기를 새로 썼다(`mockTaipeiBankExtraction`, `#/popup?demo=zh-Hant`) — 銀行/行員/銀行(모두 háng) 뒤에 旅行/行啊(모두 xíng)가 이어져 문맥에 따라 발음이 갈리는 상황을 보여준다. → **팝업 첫 렌더 시 호빗 데모 텍스트가 잠깐 보였다 실제 텍스트로 바뀌는 버그 수정(2026-07-29)**: `PopupScreen.tsx`가 `baseCtx` 초기 state를 `initialMockExtraction()`(demo 쿼리 없으면 호빗 "well-to-do")로 잡아두고 `getPopupContext()` IPC 응답(비동기)이 오면 그때 실제 ctx로 교체했는데, 실사용(선택 확정 → `createPopupWindow(ctx)`)도 호빗 데모 버튼(`openPopup()`, demo 인자 없음)도 URL 해시가 똑같이 `#/popup`(쿼리 없음)이라 첫 렌더 시점엔 둘을 구분할 방법이 없어 실사용에서도 항상 호빗 텍스트가 먼저 그려졌다 — 첫 렌더는 빈 텍스트(`emptyExtraction()`)로 그리고, `getPopupContext()` 응답이 null일 때(데모/ctx 미도착)만 그때 가서 데모 목업으로 확정하도록 변경. → **실사용 중에도 가끔 호빗 데모가 뜨는 문제 완전 차단(2026-07-29 재수정)**: 위 수정 이후에도 "자막 누르면 뜬금없이 호빗 데모창이 뜬다"는 제보 — 원인은 `MainScreen.tsx`의 기본(영어) 데모 버튼이 여전히 `openPopup()`을 demo 인자 없이 호출해서, 그 URL 해시가 실사용과 완전히 동일(`#/popup`, 쿼리 없음)했다는 것. 그래서 실사용 중 `getPopupContext()`가 어떤 이유로든(아래 창 재사용 레이스 등) null을 반환하면 무조건 호빗 데모로 fallback했다. 기본 데모 버튼도 `openPopup('hobbit')`로 명시적 demo 값을 넘기도록 바꾸고, `PopupScreen.tsx`는 **demo 쿼리가 없으면 절대 목업으로 fallback하지 않도록**(`mockExtractionForDemoParam`, 없으면 그냥 로딩 상태 유지) 변경 — 이제 데모 화면은 MainScreen 미리보기 버튼으로만 들어갈 수 있다. 겸사겸사 실제 null 유발 레이스도 하나 고침: `windows.ts createPopupWindow`의 `'closed'` 핸들러가 `popupContext`를 무조건 null로 지웠는데, 이 창이 닫히는 도중 다음 클릭이 거의 동시에 들어와 `createPopupWindow`가 다시 호출되면(새 `popupContext`를 이미 설정한 뒤) 그 새 값까지 같이 지워지는 레이스가 있었음 — 이 창이 여전히 현재 tracked된 창일 때만 지우도록 수정. → **빈 창이 뜬 다음 내용으로 채워지는 깜빡임 제거(2026-07-29 사용자 피드백)**: 위 수정들 이후에도 창 자체는 `'ready-to-show'`(첫 페인트, 아직 `emptyExtraction` 자리표시자 상태)에 바로 `show()`했기 때문에 "빈 창 → 내용 채워짐" 전환이 짧게 보이는 문제가 남아있었다. 새 IPC `POPUP_CONTENT_READY`(렌더러→메인, `preload.notifyPopupContentReady`)를 추가해, `PopupScreen.tsx`가 `baseCtx`가 실제 값(빈 자리표시자 아님)으로 바뀐 뒤 두 번의 `requestAnimationFrame`(그 프레임이 실제로 그려졌다고 볼 수 있는 표준적인 방법)을 기다렸다가 통지하도록 하고, `windows.ts createPopupWindow`는 이 신호를 받을 때까지(`popupShowSignal`) 창을 숨겨둔 채로 기다린다 — `ready-to-show`에 바로 보여주지 않음. 렌더러가 어떤 이유로든 신호를 못 보내는 경우를 대비해 1.5초 안전망 타임아웃도 같이 둠(그때까지 안 오면 그냥 보여줌).
- [x] ~~클릭(단어) vs 드래그(범위) 구분 — `popup/ContextView.tsx` 가 mousedown+mouseenter+전역 mouseup 으로 처리(클릭=단일 atom, 드래그=범위). 단어 사이(공백·문장부호) gap 에서 mousedown 해도 그 gap 인접 단어를 anchor 로 드래그가 시작되고, 드래그 중 gap 을 지날 때도 진행 방향에 맞는 인접 단어까지 선택이 확장됨(gap 도 onMouseEnter 로 처리). atom(단어) hover 시 커서를 `pointer` 로 표시(`styles.css` `.ctx-text .atom`). 드래그가 시작되면 어떤 세그먼트 위에 있든 커서가 텍스트(I-beam)로 고정되도록 `.ctx-text` 에 `dragging` 클래스를 붙임(`styles.css` `.ctx-text.dragging`)~~
- [x] ~~최종 선택 확정 → `SelectionContext` 생성 (`popup/selection.ts` `deriveContext`) — 초기엔 앞뒤 문맥을 `precedingText`/`followingText`로 미리 잘라 담았으나, 팝업 표시 범위(256바이트 창)가 LLM에 보내는 문맥까지 제한해버리는 버그가 있어 `fullText`+`selStart`/`selEnd`(원문 좌표)만 넘기고 LLM 문맥 자르기는 `buildContextBlock`(`llm/adapter.ts`)이 설정값 기준으로 별도 수행하도록 변경(`fix: 1b7f0a1`)~~
- [x] ~~팝업 원문 문맥 표시 범위를 선택 앞뒤 각 512바이트로 제한 — `buildSelectionModel`(`popup/selection.ts`)이 A가 넘긴 추출 텍스트 전체를 그대로 보여주던 것을, `@shared/context.ts` `computeContextRange` 로 앞뒤 512바이트(+문장 경계까지 확장, 부족하면 있는 만큼만) 잘라 표시하도록 변경. 데모 목업(`popup/mockSelection.ts`)도 문장 수 기반 사전 트리밍을 없애고 원문 전체를 넘기도록 정리해 동일 로직을 그대로 시연~~ → **바이트 기준 폐기, 줄 기준으로 교체(2026-07-29)**: 바이트 예산은 몇 줄이 보일지 예측하기 어렵다는 사용자 피드백으로, 선택한 표현이 속한 줄 기준 앞 3줄·뒤 3줄을 보여주는 `computeLineContextRange`(`popup/selection.ts`, 문장 경계 확장 없음 — 줄 자체가 이미 문단 경계)로 전면 교체. LLM에 넘기는 문맥(설정 화면 Byte 범위)은 이 표시와 무관하게 그대로 바이트 기준 유지. → **줄 기반 창 위에 실제 렌더링 줄·문장 경계 확장 추가, 앞뒤 3→2줄로 축소(2026-07-29)**: `computeLineContextRange`는 '\n' 문단 구분 기준이라 팝업 실제 너비에서 자동 줄바꿈된 화면상 줄과 달랐던 것을, `popup/measureLines.ts`(`measureVisualLineRange`, DOM Range API 로 문자별 렌더링 top 좌표를 읽어 실제 줄 경계 탐지)로 교체 — `PopupScreen.tsx` 가 컨테이너 마운트 후 `useLayoutEffect`(의존성은 `baseCtx` 뿐, 창 크기 변경으로는 재측정 안 함)로 측정해 문단 기반 근사치를 교체한다. 측정된 창 경계에 `@shared/context.ts` `sentenceStart`/`sentenceEnd` 로 문장 경계까지 바깥으로 확장(창이 문장을 중간에 자를 때만 늘어남 — 자막처럼 한 줄=한 문장인 텍스트에서 뒤 문맥이 과하게 길어 보인다는 피드백으로 기본값을 앞뒤 각 3줄→2줄로 축소). → **구두점 없는 자막에서 문장 경계 확장이 문서 끝까지 폭주하던 버그 수정(2026-07-29)**: 구두점(．！？ 등)이 아예 없는 자동 생성 자막(실측: 중국어 유튜브 자막, 각 줄이 문장 하나인데 종결부호가 하나도 없음)을 선택하면 `sentenceStart`/`sentenceEnd`(`@shared/context.ts`)가 종결부호를 못 찾고 계속 스캔하다 영상 첫 줄/끝까지 통째로 확장돼버렸다 — 종결부호뿐 아니라 줄바꿈('\n')도 확장을 멈추는 경계로 보도록 수정(`isSentenceBoundaryStop`), 종결부호가 없으면 최소한 그 줄만큼만 확장되게 함. 같은 함수를 공유하는 LLM 문맥 구성(`llm/adapter.ts` `buildContextBlock`→`computeContextRange`)도 동일하게 안전해짐.
- [x] ~~문장 경계 판정이 영어 약어·이니셜·소수점·줄임표(`Mr.`/`e.g.`/`i.e.`/`U.S.`/`3.14`/`J. K.`/`...`)의 `.`을 문장 끝으로 오인해 LLM 문맥·팝업 표시 범위가 조기에 잘리던 문제 수정 — `@shared/context.ts` `isAbbreviationDot`. CJK 종결부호(`。！？…`)는 영향 없음~~
- [x] ~~새 문단 시작에 들여쓰기(`PARAGRAPH_INDENT`, `popup/selection.ts` `indentParagraphs`)로 넣은 공백이 화면에 안 보이던 버그 수정 — `.ctx-text`가 `white-space: pre-line`이라 줄바꿈(`\n`) 직후의 일반 스페이스(U+0020)는 CSS 공백 축약 규칙상 렌더링 시 제거된다. `PARAGRAPH_INDENT`를 축약 대상이 아닌 non-breaking space(U+00A0)로 변경~~
- [x] ~~팝업 "글자 단위" 토글을 누르면 드래그해둔 선택 범위가 사라지고 초기 anchor 선택으로 리셋되던 버그 수정(2026-07-29) — `PopupScreen.tsx`가 `model`(atoms 배열)이 바뀔 때마다(charLevel 토글도 새 atoms 를 만들어 model 을 바꿈) 항상 `model.initialFrom/To`로 리셋했는데, atom 인덱스는 charLevel 이 바뀌면 경계 자체가 재분할돼 의미가 달라진다. 지금 선택된 "문자 범위"(atom 인덱스가 아니라 displayText 오프셋)를 `lastSpanRef`로 따로 기억해뒀다가, baseCtx 자체가 바뀐 경우(진짜 다른 선택으로 교체)만 anchor 로 리셋하고 그 외(같은 baseCtx인데 atoms 구조만 바뀐 경우: charLevel 토글, 형태소 분석 결과 도착 등)는 그 문자 범위와 겹치는 새 atom 인덱스로 재매핑해 선택을 유지하도록 수정. 툴바 라벨도 "글자 단위" → "글자 단위 선택"으로 변경.~~ → **재수정(2026-07-29)**: 문장부호까지 포함해서 자유롭게 선택되게 바뀐 뒤로는 "글자"(낱말/글씨 느낌)보다 "문자"(특수문자처럼 기호·부호까지 포괄하는 용어)가 더 맞는다는 판단으로 라벨을 "문자 단위 선택"으로 변경, 툴팁(`title` 속성)은 제거.
- [x] ~~글자 단위 선택 모드에서 커서가 이미 선택된 글자 위에 올라가면 글자·배경이 모두 흰색이 돼 안 보이던 버그 수정(`styles.css`, 2026-07-29) — `.ctx-text.char-level .atom:hover { background: none; }`(클래스 4개, 명세 0,4,0)이 `.ctx-text .atom.sel`(클래스 3개, 명세 0,3,0)보다 명세가 높아서, 선택된 글자에 커서가 올라가면 `.sel`이 준 배경(보라)만 지워지고 글자색(흰색)은 그대로 남아 흰 배경에 흰 글자가 됐다(헤드리스 크롬으로 재현·검증). `:hover` 규칙에 `:not(.sel)`을 추가해 이미 선택된 글자는 hover와 무관하게 항상 `.sel` 스타일을 유지하도록 수정 — 선택 안 된 글자는 기존대로 hover 강조 없음, 커서만 I자 유지.~~
- [x] ~~ja/zh "글자 단위" 선택 모드에서 문장부호가 선택 안 되는 문제 수정(`popup/selection.ts`, 2026-07-29) — 기존엔 KANJI_CHAR_RE/가나 런/zhWords 어느 것에도 안 걸리는 문자(문장부호·기호 등)는 `tokenizeAtoms`가 그냥 건너뛰어(`i += 1`) atom 이 아니라 gap 으로 렌더링됐는데, gap 은 항상 가장 가까운 atom 에 선택이 얹혀갈 뿐 자기 혼자 드래그 시작/끝점이 될 수 없었다. "형태소 분석 기준으로 더 잘게 쪼개는" 방식이 아니라, "글자 단위"라는 이름 그대로 일반 텍스트 드래그처럼 자유롭게 아무 글자에서나 시작/끝낼 수 있어야 한다는 요청으로, charLevel=true 일 때 남는 문자를 전부 그 글자 하나짜리 atom 으로 만들도록 변경 — atom/gap 구분 자체를 이 모드에선 사실상 없앤다(단어 단위 모드는 기존 동작 그대로 유지). "你好，世界！这是测试。" 같은 문장부호 포함 텍스트로 시뮬레이션해 모든 글자(문장부호 포함)가 개별 atom 이 됨을 확인.~~
- [x] ~~팝업 ja/zh 범위 선택이 갑자기 문장 전체(줄 단위처럼) 선택되는 회귀 수정(`popup/selection.ts`, 2026-07-29) — en 팝업 atom 규칙을 확장 hover 박스와 공유하도록 통일하면서(`@shared/wordTokenize` `WORD_ATOM_PATTERN`) `LATIN_ATOM_RE`가 `\p{L}\p{N}`(유니코드 일반 문자) 기반으로 넓어졌는데, 유니코드 `\p{L}`은 한자·가나도 "문자"로 분류해서 `tokenizeAtoms` 루프에서 `LATIN_ATOM_RE`를 제일 먼저 검사하던 순서 그대로면 CJK 런 전체가 greedy 하게 한 atom으로 통째 매칭돼버렸다(ja는 형태소 분석 엔진 단위, zh는 zhWords 단어 단위를 유지해야 하는데 문장 전체가 한 atom이 됨). CJK 전용 분기(zhWord/가나/한자)를 `LATIN_ATOM_RE`보다 먼저 검사하도록 순서만 바꿔 수정 — 한글·키릴 등 진짜 비-CJK 문자만 `LATIN_ATOM_RE`로 떨어진다.~~ → **재수정(2026-07-29, 사용자 피드백 — "호버박스에서 そう만 선택해서 클릭했는데 팝업에선 そうなると가 선택됨")**: 팝업의 초기 선택은 호버박스가 고른 anchor(문자 오프셋 범위)와 "겹치는" atom 을 통째로 고르는 방식인데, ja/zh atom 은 팝업이 독자적으로 돌리는 형태소 분석/단어 분절 결과(anchor 와 무관하게 계산됨)라 병합 규칙이 anchor 경계 중간을 가로지르는 atom 을 만들 수 있었다(형태소 분석기가 そう+なると를 접속사구로 병합) — 이 경우 그 atom 전체가 초기 선택이 돼 호버박스보다 넓게 뜬다. "무조건 호버박스가 고른 범위 그대로 선택된 채 열려야 한다"는 요청으로, atom 계산 직후 anchor 의 시작/끝 위치에서 항상 atom 경계가 생기도록 강제하는 `splitAtomAt`을 추가(`buildSelectionModel`) — anchor 중간을 가로지르는 atom 이 있으면 그 지점에서 둘로 쪼갠 뒤 초기 선택을 계산해, 초기 선택이 anchor 와 정확히 일치하게 됨. 이미 경계가 맞아떨어지는 언어(영어 등)는 no-op이라 기존 동작 그대로. 재현 시나리오(형태소 분석기가 そうなると를 한 토큰으로 병합)로 직접 시뮬레이션해 초기 선택이 정확히 "そう"로 좁혀짐을 확인.
- [x] ~~팝업 범위 지정 선택 표시(`.atom.sel`/`.gap.sel`) 스타일을 hover 박스(word-box)와 통일(`styles.css`, 2026-07-29) — 진보라 배경+흰 글자였던 걸 연보라 배경(`--theme-weak`)+진보라 테두리+둥근 모서리(3px)로 변경, 글자색은 더 이상 덮어쓰지 않음. 테두리는 `border` 대신 `outline`을 써서(레이아웃에 관여 안 함) 선택 시 `.ctx-text`(justify)의 줄바꿈 위치가 흔들리지 않게 함(예전 padding 관련 버그와 같은 종류의 문제를 처음부터 피함) — 헤드리스 크롬으로 렌더링 확인. → **여러 단어 선택 시 단어마다 따로 박스로 분절돼 보이던 문제 수정(2026-07-29)**: outline은 네 변을 전부 그려서 여러 단어를 선택하면 단어마다 개별 박스처럼 보였다(사용자 피드백 — "dining rooms all were on the same"처럼 분절). `box-shadow`(inset)로 바꿔 위/아래 테두리는 모든 선택 세그먼트에 공통으로 긋고, 좌/우 테두리+둥근 모서리는 선택 범위의 맨 처음·맨 끝 세그먼트에만 주도록 변경(`ContextView.tsx`가 렌더링 시 `sel-start`/`sel-end` 계산) — 같은 줄에서는 하나로 이어진 띠처럼, 줄바꿈되면 자연스럽게 다음 줄로 이어지고, 단어 하나만 선택하면(맨 처음=맨 끝) 기존 hover 박스와 동일하게 네 변 다 둥글게 나온다. 헤드리스 크롬으로 여러 단어/단일 단어 둘 다 렌더링 확인. → **재수정(2026-07-29, 실사용 확인 결과 "dining-rooms"처럼 하이픈 단어 선택 시 가운데가 푹 들어가 보이고 테두리가 겹쳐 보임)**: `.ctx-text .atom`의 기본 `border-radius: 3px`가 `sel-start`/`sel-end`가 아닌 중간 세그먼트에도 안 지워지고 남아있어, 인접 세그먼트와 맞닿는 모서리가 둥글게 파여 틈이 생겼다(헤드리스 크롬 스크린샷으로 원인 확인). 중간 세그먼트는 `border-radius: 0`으로 명시적으로 초기화하고 `sel-start`/`sel-end`는 각자 자기 쪽 모서리만 라운드, 반대쪽은 0으로 명시. 위/아래 오프셋 비대칭 보정값도 3px(내림선 잉크를 덮어버림, 실측 확인)에서 2.25px로 조정. 재검증(헤드리스 크롬): 하이픈 단어·여러 단어 줄바꿈 케이스 모두 이음매 없이 깔끔하게 렌더링됨. → **3차 재수정(2026-07-29)**: hover 박스(word-box)와 수치까지 맞춰달라는 요청으로 글자-테두리 사이 여백(2px, "보라 영역이 텍스트에 너무 붙어있다" 피드백 반영)과 모서리 반지름(5px)을 word-box와 통일 — 처음엔 안쪽 여백 겹을 `box-shadow` `spread`로 만들었는데, spread는 그 겹의 반지름을 spread만큼 자동으로 키워(border-radius+spread) 바깥 테두리 겹(반지름 그대로)과 안쪽 여백 겹(반지름이 커짐)이 모서리에서 안 맞아떨어져 바깥쪽이 움푹 파여 보이는 버그가 생겼다(실사용 확인 — "모서리 바깥쪽이 파여있다"). 안쪽 여백 겹도 바깥 테두리 겹과 동일하게 spread 대신 방향별 offset으로만 만들도록 변경(반지름을 그대로 유지한 채 평행이동만 하므로 모서리가 어긋나지 않음). 테두리 두께는 word-box(2px)로 맞췄다가 "너무 두껍다"는 재피드백으로 원래 두께(1.5px)로 되돌리고 여백(2px)·반지름(5px)만 유지. → **롤백(2026-07-29)**: 3차 재수정 이후에도 실사용 중 모서리 파임이 재현된다는 피드백이 이어져, 커밋 `7842720`(hover 박스와 톤만 맞춘 가장 단순한 버전 — 진보라 대신 연보라 배경+진보라 outline 1.5px+반지름 3px, 세그먼트 구분 없음)으로 되돌림. 여러 단어 선택 시 단어마다 개별 박스로 보이는 것(분절)은 이 버전의 알려진 특성으로 감수 — `ContextView.tsx`의 `sel-start`/`sel-end` 클래스는 계속 붙지만 CSS가 더 이상 참조하지 않아 지금은 효과 없음. → **4차 재수정(2026-07-29, 분절 문제 재요청)**: box-shadow 트릭(spread·offset 둘 다) 은 실기기에서 서브픽셀 반올림으로 모서리가 어긋나는 문제가 반복됐던 것으로 추정 — 이번엔 세그먼트 버튼(탭바 등)에 흔히 쓰이는 표준 패턴인 **진짜 `border`**로 재구현(레이아웃에 관여해 선택 시 살짝 커지는 트레이드오프는 감수). 위/아래 테두리는 모든 선택 세그먼트 공통, 좌/우 테두리+둥근 모서리는 `.sel-start`/`.sel-end`에만 적용해 중간은 이어지게 함(`ContextView.tsx`가 이미 계산해두고 있던 클래스를 다시 사용). 헤드리스 크롬으로 1x/2x/3x 배율 전부 하이픈 단어·여러 단어 줄바꿈·단일 단어 케이스 확인, 모서리 파임·겹침 없이 깔끔함. → **5차 재수정(2026-07-29, "단어 선택하면 폭이 변하면서 줄 개수가 바뀐다" 피드백)**: `border`는 실제 박스 모델에 관여해 선택된 세그먼트 폭이 테두리 두께만큼 늘어나므로, `justify` 정렬 문단에서 줄바꿈 위치 자체가 흔들리는 문제가 있었다 — box-shadow/outline은 레이아웃에 전혀 관여하지 않아 이 문제 자체가 없다. 되짚어보니 이전에 box-shadow에서 모서리가 파였던 원인은 box-shadow 기법 자체가 아니라 **서로 다른 offset/spread 레이어를 겹친 것**(패딩 흉내용 레이어 + 테두리용 레이어)이었다 — 배경은 이미 `background`로 채워져 있으니 box-shadow는 테두리 역할만 하는 **레이어 하나**로 되돌리면 그 문제가 재발하지 않는다. `box-shadow`(inset 없이 offset만, 위/아래 공통 + `sel-start`/`sel-end`에 좌/우 각각, `sel-start.sel-end` 조합은 네 변 다 명시)로 재변경 — 헤드리스 크롬 1x/2x/3x 재검증, 모서리 파임 없고 레이아웃도 그대로 유지됨(줄바꿈 안 흔들림). → **6차 재수정(2026-07-29, 색상만 원복 요청)**: 테두리 포함 크기/구조(box-shadow 단일 레이어 등)는 그대로 두고 배경/글자색만 원래(진보라 배경 `var(--theme)` + 흰 글자 `var(--theme-text)`)로 되돌림 — 중간에 hover 박스 톤 맞춤으로 바꿨던 연보라 배경+어두운 글자를 폐기.~~ → **7차 재수정(2026-07-29, "hover 박스가 선택 박스보다 크기가 작다", "모서리 반경 좀 줄여줘" 피드백)**: `.atom:hover`는 그냥 `background`만 칠해서 글자 폭(padding:0)만큼만 차지했는데, `.sel`은 box-shadow로 상하좌우를 1.5px씩 넓혀 서로 크기가 달라 보였다 — hover 도 동일한 box-shadow 오프셋 기법(색만 `--theme-weak`)으로 확장해 두 박스 크기를 통일. char-level 모드에서 `:not(.sel):hover`가 hover를 끄는 규칙에도 `box-shadow: none`을 같이 추가(안 하면 배경만 지워지고 테두리만 남는 문제 예방). 겸사겸사 모서리 반지름을 3px→2px로 줄임(`.atom` 기본값과 `.sel-start`/`.sel-end`/`.sel-start.sel-end` 전부) — 헤드리스 크롬으로 hover/선택 박스 크기 일치 확인. → **8차 재수정(2026-07-29, "모서리가 파여있다"·"둥글게 해줘" 피드백)**: 2px는 헤드리스 크롬 합성 테스트에선 깨끗했지만 실제 화면에서 너무 작아 거의 안 둥글어 보여(=파인 것처럼 보임) 4px로 다시 키움(`.atom` 기본값·`.sel-start`/`.sel-end`/`.sel-start.sel-end` 전부) — 하이픈 단어·단일 단어 케이스 헤드리스 크롬 1x/2x/3x 재검증, 2px·4px 둘 다 합성 테스트에선 깨끗해서 이번엔 "너무 작아서 안 둥글어 보이는" 문제였을 가능성이 높음.
- [x] ~~팝업이 이미 떠 있는 상태에서 다른 텍스트를 새로 선택하면 팝업 내용은 바뀌는데 창이 대상 앱 뒤에 가려진 채로 안 보이던 버그 수정(`windows.ts`, 2026-07-29) — 방금 클릭한 대상 앱(브라우저 등)이 그 클릭으로 포그라운드를 가져간 직후라, Windows가 백그라운드 프로세스의 `SetForegroundWindow`(Electron `BrowserWindow.focus()` 내부 호출)를 막는 정책 때문에 `focus()`만으로는 팝업이 안 올라왔다. `setAlwaysOnTop`을 껐다 켜는 토글로 z-order 재계산을 강제하는 `forceWindowToFront` 헬퍼를 추가해 `createPopupWindow`의 재사용 경로(+최초 오픈 경로도 동일 위험이 있어 함께)에 적용.~~ → **2차 재수정(2026-07-29, 실사용 확인 결과 1차 수정으로도 여전히 안 됨)**: window 단위 z-order 조작(`setAlwaysOnTop`/`focus`/`moveTop`)만으론, 대상 앱(다른 애플리케이션)이 이미 활성 앱으로 올라와 있는 상태 자체를 못 뒤집는다(특히 mac) — `forceWindowToFront`에 `app.focus({ steal: true })`(mac의 "포커스 훔치기" 방지 기본 동작 우회, 애플리케이션 자체를 활성화)를 추가. → **3차 변경(2026-07-29, 사용자 요청 — "앞으로 오는 방식 말고 지우고 새로 띄우는 방식으로")**: 팝업이 떠 있는 채로 새 단어를 고르면 기존 창을 재사용(내용만 갱신 + 앞으로)하던 걸, 기존 창을 `destroy()`로 즉시 없애고 항상 새 `BrowserWindow`를 만드는 방식으로 변경(`createPopupWindow`) — 이전 대화(채팅 로그 등) 상태가 새 선택에도 남아있어 헷갈린다는 피드백. `close()` 대신 `destroy()`를 쓴 이유: `close()`는 `before-input-event` 등 비동기 정리 경로를 타서 그 사이 새 창을 만들면 "닫히는 중인 창"과 "새 창"이 잠깐 공존해 z-order/포커스가 꼬일 수 있음 — `destroy()`는 동기로 즉시 제거해 그 틈이 없다.~~ → **4차 재수정(2026-07-29, "넷플릭스 전체화면에서 자막 누르면 팝업이 옆 데스크탑으로 이동해서 뜬다" 피드백)**: 3차 변경(재사용→매번 새 창) 때문에 생긴 부작용 — macOS는 전체화면(자기 전용 Space) 앱 위에 "새로 나타나는" 창을 그 자리에 못 띄우고 별도 Space를 새로 만들어 전환해버린다. 예전엔 팝업 창을 재사용해서 macOS가 "이미 있던 창"으로 취급해 같은 Space 위에 그냥 보여줬던 것인데, 매번 destroy 후 새로 만들면서 이 문제가 드러났다. mac에서 팝업 생성 직후 `setVisibleOnAllWorkspaces(true, { visibleOnFullScreen: true })`를 걸어 이 창이 전체화면 Space에도 나타날 수 있다고 미리 알려주면 Space 전환 없이 바로 그 자리에 뜬다. → **5차 재수정(2026-07-29, 사용자 재요청 — "팝업이 닫혔다 새로 열리는 게 아니라 기존 팝업 내에서 바뀌면 좋겠다")**: 3차 변경(destroy+새 창)을 다시 뒤집어, ctx 있는 실사용 클릭에서 팝업이 이미 떠 있으면 창을 재사용(`popupContext` 갱신 + 기존 `POPUP_GET_CONTEXT` IPC 채널로 새 ctx push + `forceWindowToFront`)하도록 변경(`createPopupWindow`). 3차 변경의 원래 동기(이전 대화 상태가 새 선택에도 남아 헷갈림)는 창을 새로 만들지 않고도 해결— `PopupScreen.tsx`에 `baseCtx`가 바뀔 때마다 채팅 메시지/스트리밍 상태를 명시적으로 리셋하는 `useEffect`를 추가해 대신 처리. 재사용 경로에서 겸사겸사 두 가지를 더 고침: (1) **본문이 비어있는 팝업 버그**(중국어 세로쓰기에서 자주 재현되던 것) — `popupContext = ctx` 대입이 기존 창 `destroy()` **앞**에 있어서, `destroy()`가 동기로 발화하는 `'closed'` 핸들러의 `popupWindow === win` 가드가 그 시점엔 아직 참이라 방금 대입한 새 컨텍스트를 도로 null로 지워버렸다 — 대입을 destroy 뒤로 옮겨 수정(위 '\n' 마커 항목은 부차적 성능 개선이었을 뿐, 실제 원인은 이 대입 순서였음). (2) **재사용 시 팝업이 매번 특정 위치로 튀는 문제**(사용자 재보고) — 재사용 경로에서도 `centerOnCursorDisplay`로 매번 재배치하고 있었던 게 원인, 재사용 시엔 위치/크기를 아예 건드리지 않도록 수정(사용자가 드래그해 옮겨둔 자리를 그대로 유지). demo/미리보기 경로(ctx 없음)는 여전히 destroy+새 창 그대로 유지. → **6차 수정(2026-07-29, 재사용 도입으로 새로 생긴 회귀 — "팝업이 뜬 채로 다른 텍스트 박스를 누르면 가끔 엉뚱한 다른 줄이 선택됨")**: `PopupScreen.tsx`의 `measuredRange`(DOM 실측 표시 범위)가 `useState`+`useLayoutEffect` 2단계로 갱신되는데, `baseCtx`가 바뀐 바로 그 렌더에서는 아직 이전 `baseCtx` 기준으로 계산된 `measuredRange`가 남아있는 채로 `model`이 한 번 잘못 계산되고, `range` 리셋 이펙트가 그 잘못된 문자 범위를 `lastSpanRef`에 새겨버린다 — 곧이어 `useLayoutEffect`가 `measuredRange`를 올바르게 고쳐도, `range` 리셋 이펙트는 `baseCtx` 객체 참조가 그 사이 안 바뀌었으니 "같은 컨텍스트"로 오판해 anchor 로 리셋하는 대신 방금 저장해둔 틀린 범위를 새 model 에 재매핑해버려 완전히 다른 줄이 선택됐다. 예전엔 클릭마다 창을 새로 만들어 `measuredRange`가 항상 `null`로 시작했기 때문에 이런 "다른 컨텍스트의 잔여값"이 있을 수 없었는데, 재사용 도입 이후 처음 드러난 문제 — `measuredRange` state 를 `{ ctx, range }` 형태로 바꿔 지금 `baseCtx`에 대해 계산된 값이 아니면(`measured.ctx !== baseCtx`) 문단 기반 근사치(`null`)로 취급하도록 수정, 한 프레임짜리 불일치가 아예 밖으로 새어나가지 않게 함.
- [x] ~~팝업 헤더의 출처 표시가 넷플릭스 워치 URL처럼 트래킹 쿼리스트링이 긴 경우 언어 라벨이 화면 밖으로 밀려 아예 안 보이던 문제 수정(`PopupScreen.tsx` `sourceLabel`, 2026-07-29) — `"{출처} · {언어}"` 한 줄을 CSS `text-overflow: ellipsis` 하나에만 맡겼더니, 긴 URL 혼자 헤더 폭을 다 잡아먹어 뒤에 이어 붙는 언어 라벨(`LANGUAGE_LABEL`)이 잘려나갔다. `sourceLabel`이 출처 문자열을 60자로 미리 잘라 "..."을 붙이도록 수정해, 언어 라벨이 항상 보이는 폭 안에 들어오게 함.~~
- [x] ~~일본어 형태소 분석 엔진을 kuromoji/Lindera/Sudachi(mode B/C) 중 스위치 가능한 구조로 리팩터링 — `main/nlp/japanese.ts` `JA_ENGINE` 상수 하나로 전환(개발자 전용, 사용자 UI 없음). 엔진별 구현은 `main/nlp/engines/`(kuromoji.ts/lindera.ts/sudachi.ts) 아래 독립 파일로 분리해 나중에 하나를 깔끔하게 걷어낼 수 있게 함. kuromoji는 유지보수가 2022-06 이후 중단된 반면 Lindera(kuromoji-rs 포크, WASM, IPADIC 동일)는 유지보수가 활발해 무손실 대체 후보로 확인(26개 텍스트 비교 결과 kuromoji와 완전 동일)했고, Sudachi(UniDic, `python/sudachi_tokenize.py` 상주 서버, 담당 A의 Python 인프라 재사용)는 一日/ご都合 같은 일부 어휘에서 더 정확하지만 おじいさん/清水寺 같은 고빈도 어휘에서 오히려 밀리는 경우도 있어 승패가 갈림(상세 근거는 사내 비교 보고서 참고). IPADIC 병합 로직(`@shared/nlp/ja.ts`)과 UniDic 병합 로직(`@shared/nlp/ja-unidic.ts`)은 품사 체계가 달라(UniDic 은 "지금 이 자리에서 독립동사로 쓰였는지" 구분이 없음, 渡る 도 いる 도 動詞,非自立可能 로 동일 태깅됨 실측 확인) 완전히 분리된 파일로 관리. 병합 정책은 활용어미(て/で)+조동사 직접결합까지만 하고, 補助動詞(ている 의 いる 등)·접미동사(過ぎる/出す 등)·명사 자동 병합은 전부 하지 않음 — 다 그 자체로 독립된 사전 표제어라 따로 선택해 조회할 수 있어야 한다는 판단(실사용 피드백으로 확정, 예: "食べている" → 食べて / いる, "御飯を食べ過ぎる" → 食べ / 過ぎる).~~
- [x] ~~팝업에서 선택한 표현을 클립보드에 자동 복사 — 선택 범위(atom 재지정 포함)가 바뀔 때마다 `currentCtx.selectedText` 를 클립보드에 씀(`PopupScreen.tsx`). 빈 문자열은 기존 클립보드 내용을 덮어쓰지 않도록 제외.~~
- [x] ~~팝업 마지막 줄에서 아래로 드래그하면 선택이 거꾸로(앞쪽으로) 튀는 버그 수정(`popup/ContextView.tsx`) — 기존 `nearestAtomIndex` 가 커서-atom 간 순수 2D 거리만 보고 글의 흐름(읽는 순서)을 몰라서, 문단 마지막 줄 아래(글자 없는 빈 공간)로 드래그하면 다음 단어가 없으니 오히려 같은 줄의 지나온 단어가 더 가깝다고 판정되던 문제(Electron 앱을 Playwright `_electron` 드라이버로 직접 띄워 재현·검증). `document.caretRangeFromPoint`(글의 흐름을 아는 브라우저 API) 기반의 `atomIndexAtPoint` 를 우선 시도하도록 변경, 거리 기반 로직은 그 API 를 못 쓸 때만 fallback.~~
- [x] ~~팝업 프롬프트 입력창(`.chat-input textarea`) 한 줄 높이가 채팅 말풍선(`.bubble`) 한 줄 높이보다 낮아 보이던 문제 수정(2026-07-29) — 패딩(7px)은 이미 `.bubble`과 동일했지만 `line-height`가 브라우저 기본값(~1.2)이라 `.bubble`의 `line-height: 1.5`보다 줄 높이가 낮았다. `textarea`에도 `line-height: 1.5`를 맞춰 한 줄 높이를 거의 동일하게(테두리 유무로 인한 2px 차이만 남음, 헤드리스 크롬으로 실측 확인: 36.5px vs 38.5px) 맞춤.~~

<a id="b-llm"></a>

**LLM 어댑터**
- [x] ~~LLM 공통 어댑터 인터페이스 (provider 추상화)~~
- [x] ~~GPT / Gemini / Claude 클라이언트 구현 + 스트리밍~~
- [x] ~~어댑터가 `history: ChatTurn[]`을 받아 요청에 이어붙임 (`streamLlm`, `llm/adapter.ts`)~~
- [x] ~~문맥 프롬프트 구성 + 프롬프트 캐싱(비용 절감)~~
- [x] ~~API 키 무효 / 크레딧(사용 한도) 소진 / 요청 과다 / 네트워크 오류를 구분하는 에러 체계 — `QuestionError`(`shared/types.ts`), `question/errors.ts`(메시지), `question/llm/errors.ts`(HTTP 상태코드 분류). UI 렌더링은 미구현(아래 UI 항목)~~
- [x] ~~provider별 실제 사용 모델 확정 — **설정 화면에서 사용자가 선택**하도록 결정·구현. API 키 입력/수정·provider 선택 시 무과금 GET(`question/llm/validate.ts`)으로 유효성 검사 + 사용 가능 모델 목록을 받아 드롭다운으로 고르게 함. 선택값은 `AppSettings.models[provider]` 에 저장되고 `streamLlm()` 이 `settings.models[provider] || DEFAULT_MODELS[provider]` 로 사용(`||`: "Default" 옵션 선택 시 저장되는 빈 문자열도 `??`와 달리 없는 값으로 취급해 기본 모델로 대체됨 — 원래 `??`였다가 빈 문자열이 그대로 API에 전송되는 버그가 있어 수정됨). `DEFAULT_MODELS`(gpt: `gpt-4o`, gemini: `gemini-pro-latest`, claude: `claude-sonnet-5`)는 미선택 시 fallback 으로만 남김~~
- [x] ~~API 키 유효성 검사 + 사용 모델 드롭다운 — IPC `PROVIDER_VALIDATE`(`validateProvider`) 로 provider별 models 엔드포인트 호출(OpenAI `/v1/models`, Gemini `/v1beta/models`, Anthropic `/v1/models`). **무과금 GET**(토큰 미소비)이라 유효성+모델목록만 확인. 401/403 → 유효하지 않은 키로 분류(`classifyLlmError`). 설정 화면은 0.5초 디바운스 후 호출하고 상태(확인중/유효/에러)+모델 `<select>` 표시. **잔액/크레딧 조회는 세 provider 모두 공개 API 부재로 제외** — 크레딧 부족은 실제 질문 시 에러 배너로 안내~~
- [x] ~~(2026-07-30 후속, 전수 점검으로 발견) GPT/Claude의 "모델이 temperature 를 400 으로 거부하면 값 없이 재요청" 로직과 GPT/Gemini의 cacheableContext→system 병합 문자열이 각자 파일에 복제돼 있었음~~ → `llm/sse.ts`에 `withTemperatureFallback(request, isTemperatureRejection)`/`mergeSystemWithContext(system, cacheableContext)`로 통합, `gpt.ts`/`claude.ts`/`gemini.ts`는 호출만 하도록 정리(`refactor/reduce-duplication`, dev 병합). provider별 거부 조건 판별(`detail.includes(...)`)은 실측 근거가 다르므로 각 파일에 그대로 남김.

<a id="b-feature"></a>

**질문 기능**
- [x] ~~발음: IPA / 히라가나 / 병음 + 맥락 의존 발음 판정 — `question/pronunciation.ts` 가 전용 시스템 프롬프트(`prompts/pronunciation.txt`)로 `llm/adapter.ts` `streamLlm`(구 `askLlm` 오케스트레이션을 'ask'/'pronunciation' 공용으로 일반화)을 호출해 문맥 기반 발음을 스트리밍 반환. 언어별 표기 체계는 `shared/languages.ts` `pronunciationNotation`에 등록. 지역별 발음이 여러 개일 땐 `[미국]`/`[영국]`(영어), `[대륙]`/`[대만]`(중국어) 순으로 대괄호 라벨을 발음 앞에 붙이고, 근거(`근거:`)는 의미 중의성(품사·뜻 차이)을 판정해 발음을 좁힌 경우에만 작성 — 단순 지역/격식 변이거나 발음이 하나뿐이면 생략. `llm/adapter.ts` `LlmRequest.temperature` 를 추가해 GPT/Gemini/Claude 클라이언트 모두 전달하도록 배선하고, 발음 질문은 형식이 고정된 판정 작업이라 `temperature=0.2`로 낮춰 응답 이탈(문맥과 무관한 토큰 삽입 등)을 줄임(`pronunciation.ts` `PRONUNCIATION_TEMPERATURE`). `ask`(자유 질문)는 다양성을 위해 기본값 유지~~
- [x] ~~사전: 언어별 사전 API + 단어 분해 + LLM 뜻 번호 매핑~~ → **구현 및 폴백 오케스트레이션까지 완료(2026-07-28)** — en/ja/zh-Hans/zh-Hant 8개 소스(MW/OEWN/daijisen/JMdict/汉典/教育部重編國語辭典/CC-CEDICT/Wiktionary) 전부 `dev`에 병합 완료, `question/dictionary.ts`의 `FALLBACK_CHAINS`가 언어별 정식 순서로 실제 호출·서식화까지 담당(아래 136번 항목). 세부 어댑터별 완료 기록은 아래 목록 참고.
  - [x] ~~`shared/types.ts`의 `Language` 타입 확장 — `'zh'` 단일값을 `'zh-Hans' | 'zh-Hant'`로 분리. `LANGUAGES` 레지스트리(`shared/languages.ts`)·OCR 언어팩 로딩(`selection/ocr.ts`, 결합 로드 대신 판정된 스크립트 하나만 로드)·팝업 UI 라벨(`中文(简体)`/`中文(繁體)`)까지 반영 완료. `selection/langDetect.ts`는 아직 스텁이라 담당 A가 zh-Hans/zh-Hant 판별 시 참고할 가이드만 문서화해둠(OpenCC 매핑 데이터로 판별, 변환과 달리 모호함이 적음).~~
  - [x] ~~`selection/langDetect.ts` 구현 — 담당 A가 `origin/experiment/doclayout-yolo` 브랜치에서 구현 완료 확인(2026-07-27). Tesseract OSD(스크립트 감지 경량 기능)로 1차 언어 판별 후 "Han"(한자, 중/일 미구분)이면 표본 문자 카운트로 zh-Hans/zh-Hant까지 판별 — 위 zh-Hans/zh-Hant 가이드를 그대로 따름. 다만 OpenCC 전체 매핑 테이블 대신 고빈도 상용한자 표본(약 50자)으로 대체(해당 환경에서 OpenCC 데이터 파일을 못 받아서 — 나중에 상수 2개만 교체하면 OpenCC 데이터로 전환 가능하게 설계해둠). 아직 `dev` 브랜치엔 머지 안 됨(정정: `experiment/doclayout-yolo` → `dev` 병합이 2026-07-28에 완료돼 지금은 dev의 `langDetect.ts`에 실구현이 반영돼 있음).~~ **OpenCC 데이터로 전환 완료(2026-07-29, `feat/language-expansion`)**: 아래 247번 항목의 "확장 언어 자동 판별 통합"에서 이 상수 2개(당시 50자 표본)를 실제로 OpenCC 데이터 기반(간체전용 3875자/번체전용 3222자)으로 교체 — 예고했던 대로 `detectChineseScript` 로직 자체는 그대로 두고 표본 상수만 `@shared/languageDetect`의 `detectHanziVariant`로 위임하도록 바뀜.
  - [x] ~~사전 응답 통일 스키마 설계 및 타입 반영 — en/ja/zh 8개 소스(MW/WordNet/Wiktionary/daijisen/JMdict/汉典/萌典/CC-CEDICT)가 전부 형식이 달라서(JMdict의 `v1`/`vt` 같은 EDICT 약어 코드, MW의 `sseq`/`dt`/`vis` 중첩 구조, CC-CEDICT의 슬래시 구분 평문 등), 각 어댑터가 원본을 파싱해 공통 타입으로 변환한 뒤에만 LLM에 넘기도록 `shared/types.ts`에 `CanonicalPos`/`DictionarySense`/`DictionaryEntry`/`DictionarySourceId` 추가(`llm/adapter.ts`가 GPT/Gemini/Claude를 `QuestionResult` 하나로 통일하는 것과 동일 패턴).~~
    - ~~`pos`는 언어 간 겹치는 범위로만 표준화(noun/verb/adjective/adverb/pronoun/preposition/conjunction/article/particle/interjection/classifier/other) — 언어마다 없는 품사도 있음(ja/zh엔 관사 없음, en엔 조사 없음). ja 助詞/zh 助词는 이름만 같고 실제 기능이 다름(전자는 격조사 중심, 후자는 상 표지·구조조사 중심)이라 세부는 `posRaw`로만 구분.~~
    - ~~`posRaw`(원본 약어 그대로, 디버깅용)는 LLM 프롬프트에 넣지 않기로 했는데, 그러면 ja 동사의 一段/五段/変格, い형용사/な형용사 같은 활용 분류처럼 문법 설명에 실제로 필요한 정보까지 함께 버려지는 문제가 있어 **`conjugationClass`(사람이 읽을 수 있는 문자열, LLM에도 전달)로 별도 승격**해서 해결.~~
    - ~~`classifiers`(zh 양사, CC-CEDICT의 "CL:")도 같은 이유로 별도 필드로 분리.~~
    - ~~필드가 옵셔널이라 en/zh 항목엔 `conjugationClass` 값 자체가 안 들어가 실행 시점엔 안전(JSON 직렬화 시 undefined 키는 자동 생략) — 다만 타입 수준에서 "이 필드는 이 언어 전용"이라는 걸 컴파일러가 강제하진 않음(판별 유니온 대신 공통 베이스+옵셔널 확장 필드 방식을 의도적으로 택함, 필드 2-3개 규모에선 유니온이 과한 복잡도로 판단).~~
    - ~~남은 확인 필요 항목(아직 미반영): en 불규칙 동사 활용(MW의 `ins` 필드) — 별도 `irregular?: boolean` 플래그 후보. zh 이합사(离合词, 结婚/见面 등 중간에 성분 삽입 가능한 특이 문법) — 汉典/萌典/CC-CEDICT가 이를 태깅하는지 실제 소스 조사 후 필요하면 `conjugationClass`와 같은 패턴으로 추가.~~
    - [x] ~~**사전 소스별 실측 노트** — 전체 내용은 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md)로 이전됨(공통 스키마 결정 사항 + MW/OEWN/daijisen/JMdict/汉典/教育部重編國語辭典/CC-CEDICT/Wiktionary 소스별 실측, 날짜별 정정 이력 포함). 여기 있던 중복 사본은 정리했으니 앞으로 실측 내용은 그 문서에 바로 추가한다.~~
    - [x] ~~**공통 스키마 검토(2026-07-28) 중 발견된, 아직 해결 안 된 이슈들** — 어댑터 구현 착수 전 재검토 필요:~~ → 아래 하위 항목 전부 해결/구현 완료.
      - [x] ~~최상위 "조회 결과" 타입이 스키마에 없음~~ → **결정(2026-07-28)**: `shared/types.ts`에 안 두기로 함 — `feat/dictionary-en`에서 실제로 구현해보니 MW 어댑터의 조회 결과(`MwLookupResult`, `mw.ts`)도, 다중 단어 폴백 결과도 전부 IPC를 안 타고 `lookupDictionary()` 내부에서만 쓰이고 끝남(최종적으로 렌더러에 나가는 건 LLM이 이미 풀어쓴 `QuestionResult.content` 문자열 하나뿐). 그래서 `DictionaryLookupResult` 같은 공유 타입 대신, 각 파일 안에서만 쓰는 로컬 타입(`MwLookupResult`/`WordLookupOutcome` 등)으로 충분— shared 타입이 필요해지는 유일한 경우는 UI가 "출처: JMdict" 같은 구조화된 정보를 직접 보여줘야 할 때인데, 이건 이미 있는 `QuestionResult.meta?: Record<string, unknown>`로 해결됨(`meta: { provider, source }`로 실제 사용 중).
      - [x] ~~`DictionarySense.usageTags`가 성격이 다른 정보(격식/표기 관례/방언)를 전부 문자열 배열 하나에 섞어 담음~~ → **해결(2026-07-28)**: `usageTags: string[]` → `UsageTag[]`(`{ text, kind?: 'register'|'convention'|'dialect'|'other' }`)로 구조화(`shared/types.ts`). 소스별 라벨 분류는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#공통-스키마-결정-사항) 참고 — MW `sls`=register/`lbs`=convention, JMdict `misc`는 값마다 다르므로 원본 misc 코드 룩업 테이블로 분류 필요(어댑터 구현 시 마저 결정), CC-CEDICT는 `(coll.)`/`(slang)`/`(derog.)`/`(archaic)`/`(literary)`=register·`(dialect)`=dialect·`(loanword)`/`(bound form)`=other. **`feat/dictionary-en`의 `mw.ts`/`senseSelect.ts`는 아직 옛 `string[]` 모양이라 병합 전 맞춰 고쳐야 함.**
      - [x] ~~`DictionarySense.seeAlso`도 관계 성격이 다른 포인터(표기 변이/줄임말/관련어 등)를 문자열 배열 하나에 섞어 담는 같은 문제~~ → **해결(2026-07-28)**: `seeAlso: string[]` → `SeeAlsoRef[]`(`{ text, kind?: 'variant'|'dialectVariant'|'abbreviation'|'usedIn'|'related' }`)로 구조화. CC-CEDICT 라벨별 분류·`usageTags`와 안 합치기로 한 이유는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#공통-스키마-결정-사항) 참고. `classifiers`(CC-CEDICT 양사)도 같이 검토했으나 원소가 전부 동일 개념이라 이 문제에 해당 안 됨 — 원본 표기(파이프·대괄호) 그대로 남는 별개 문제는 구조화(`{ hanzi, pinyin? }`)를 검토했으나 **안 하기로 결정(2026-07-28)** — 원본 세그먼트 그대로 두는 현 방침 유지. **`feat/dictionary-en`도 이 필드를 아직 안 써서 영향 없음(usageTags 와 달리 병합 전 손볼 코드 없음).**
      - [x] ~~`DictionarySense.classifiers`가 사실상 CC-CEDICT 1개 소스 전용 필드 — "언어 전용 필드 2-3개까진 공통 베이스+옵셔널이 낫다"던 기존 판단(위 119번 항목)의 전제가 흔들리기 시작하는 지점~~ → **트리거 발동, 전환 완료(2026-07-28)**: 실제로 세어보니 이미 ja 3개(`conjugationClass`/`isCommon`/`appliesToHeadwords`)·en 1개(`irregularForms`)·zh 1개(`classifiers`)로 "2-3개 규모"를 넘어서 있었음(`transitive`는 en/ja 공유라 제외). "최상위(entry)만 판별 + readings/senses는 제네릭으로 연동"하는 `DictionaryEntry<L extends Language = Language>` 구조로 전환 — `entry.language`로 좁히면 안쪽 언어 전용 필드까지 자동으로 좁혀짐(`src/shared/types.ts`, 격리된 타입체크로 좁히기/미좁히기 둘 다 검증 완료). 상세 근거·트레이드오프는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#공통-스키마-결정-사항) 참고. **`feat/dictionary-en`은 아직 이 구조 이전이라 병합 시 `DictionaryEntry`/`DictionaryReading`/`DictionarySense` 참조하는 곳(`mw.ts`/`senseSelect.ts`)을 제네릭 버전에 맞춰 고쳐야 함.**
      - [x] ~~MW `sdsense`(하위 한정 정의, 예: "photosynthesis"의 "especially" 하위 정의)와 daijisen(デジタル大辞泉)의 번호매김(`①②③`→`㋐㋑㋒`)이 둘 다 "병렬 대안 뜻"이 아니라 "상위 뜻의 더 좁은 하위 구분"이라는 계층 구조인데, `DictionarySense.gloss: string[]`는 OEWN의 병렬 대안 정의용으로 설계돼 있어 이 부모-자식 관계가 평면화되며 사라짐 — 계층을 감수하고 버릴지, 별도 필드로 살릴지 결정 안 됨~~ → **해결(2026-07-28)**: `DictionarySenseBase.parentIndex?: number` 신설 — 같은 `DictionaryReading.senses` 배열 안에서 직속 부모 sense 의 인덱스를 가리킨다(다단이면 부모의 parentIndex 를 따라 조상까지 거슬러 올라감). 계층 없는 소스는 항상 undefined. **어댑터가 실제로 이 값을 채우는 로직과, LLM 프롬프트/UI 가 이 정보를 어떻게 쓸지(들여쓰기 등)는 아직 미정** — en/ja 어댑터 구현 시점에 정하기로 함, 지금은 필드만 마련.
      - [x] ~~CC-CEDICT 어댑터가 8개 소스 중 가장 깨지기 쉬울 것으로 예상 — 슬래시 구분 평문 하나에 gloss/usageTags/domain/seeAlso/classifiers/pronunciation variety가 다 섞여 있어 세그먼트 분류가 100% 정규식 휴리스틱(다른 7개 소스는 최소한 필드가 구조적으로 분리돼 있음). 패턴에 안 걸리는 새 라벨이 계속 나올 걸 감안하고 파서를 짤 것.~~ → `cccedict.ts` 구현 시 이 우려를 그대로 반영해 CL:/교차참조 접두어/`(Taiwan pr. [...])`/알려진 사용역·전문분야 라벨을 순서대로 분류하는 파서로 완료(아래 CC-CEDICT 어댑터 항목, 세부는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#cc-cedict)).
      - [x] ~~MW 조회 응답 배열에 관련 없는 파생/복합어 엔트리가 섞여 옴~~ → **이미 해결돼 있었음(확인: 2026-07-28, `feat/dictionary-en` 코드 리뷰)**: `mw.ts`의 `isRelevantEntry(entry, word)`가 `entry.hwi.hw`(중점 제거)나 `entry.meta.stems`가 조회어와 정확히 일치하는 엔트리만 통과시켜, "hit" 조회 시 `hit-and-run`/`hit list` 같은 무관한 엔트리를 이미 걸러내고 있음. MW API 자체엔 exact-match 전용 쿼리 파라미터가 없어(공식 문서 확인) 클라이언트 필터링이 맞는 방식이었음.
      - [x] ~~어댑터 내부 raw 중간 타입 도입 여부~~ → **이미 해결돼 있었음(확인: 2026-07-28)**: `mw.ts`가 이미 `MwEntry`/`MwSense`/`MwDtItem` 등 원본 JSON 구조를 그대로 반영한 raw 타입을 먼저 정의해 파싱하고, `fetchMwEntry()` 안에서만 공통 스키마(`DictionaryEntry`)로 축약하는 2단계 구조로 구현돼 있었음.
  - [x] ~~**활용형(활용된 동사/형용사) 대응 — 위 소스별 실측 노트의 en/ja/zh 요약**: en(MW·Wiktionary는 원형과 교차 연결돼 있어 그대로 조회 가능, OEWN은 Morphy 라이브러리 없이 JSON 릴리스 자체의 `form[]` 필드+활용형 역인덱스로 해결, [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#oewn-open-english-wordnet) 참고) / ja(daijisen·JMdict 둘 다 자동 변환 안 해줘서 조회 전 형태소 분석 엔진으로 基本形 전처리 필수) / zh(활용·어미 변화 자체가 없는 언어라 해당 없음).~~ → **ja 해결 완료(2026-07-28)**: daijisen·JMdict·Wiktionary 전부 활용형을 원형으로 자동 변환해주지 않아, 표면형 통째 조회가 실패했을 때만 `main/nlp/japanese.ts`(`JA_ENGINE` 설정값) 형태소 분석 결과의 첫 토큰으로 기본형을 얻어 같은 폴백 체인 전체(daijisen→jmdict→wiktionary)를 재조회하도록 `dictionary.ts`(`toJapaneseDictionaryBaseForm`)에 연결 완료 — 관용구(표면형 자체가 정답인 경우)는 통째 조회가 먼저 성공해 재조회를 안 타므로 깨지지 않음(실측: 歩いた/歩いて→歩く, 食べた→食べる, 静かだ→静か, 大きかった→大きい, 勉強する→勉強 전부 확인). **en도 재검증 후 보강 완료(2026-07-29)**: OEWN `resolveLemmas`가 직접 표제어("closed" 형용사)를 찾으면 활용형 역인덱스를 아예 안 보던 버그 수정(running/closed/better 등 동사·원형 해석 복구) + 규칙동사(-ed/-ing/-s) 활용형 추측 로직(`guessEnglishBaseForms`, Morphy detachment rule 일부 재구현) 신설 — MW 키가 없어 OEWN이 이어받는 상황에서 "walked"/"looked" 같은 흔한 규칙동사도 Wiktionary의 얇은 굴절 안내 한 줄로 새지 않고 OEWN에서 원형을 찾아냄. **소스 하나당 후보(표면형+기본형/추측 원형) 전부를 시도해 결과를 합치도록 설계 변경**(첫 성공에서 멈추지 않음) — "closed"가 그 자체로 형용사 표제어가 있어 "성공"으로 끝나버려 "close"(동사) 후보를 아예 안 물어보던 문제(en 동형이의어) 해결. 트레이드오프: ja 관용구도 첫 토큰이 그 자체로 유효한 표제어면 관용구 자체 뜻과 그 단어 단독 뜻이 함께 후보에 섞임(틀린 답은 아니고 후보만 늘어남, 사용자 확인 후 감수). 세부 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#oewn-open-english-wordnet) OEWN 절 참고. **ja 기본형 변환이 단일 토큰 활용형을 놓치던 버그 수정(2026-07-29)**: `toJapaneseDictionaryBaseForm`이 `tokens.length < 2`일 때 무조건 null을 반환해서, 활용된 단어가 어간+활용어미 2토큰으로 안 쪼개지고 토큰 하나에 baseForm만 다르게 채워지는 경우(실측: "はしゃぎ"— 동사 連用形이 명사처럼 단독 쓰인 형태, Lindera가 단일 토큰으로 `surface: "はしゃぎ", baseForm: "はしゃぐ"`를 줌)를 전부 걸러내 daijisen/JMdict 모두 표면형만으로 조회를 시도하다 실패했다. 토큰 개수와 무관하게 첫 토큰의 baseForm≠surface만으로 판단하도록 수정(토큰이 하나뿐이고 baseForm도 같으면 여전히 null).
  - [x] ~~en 어댑터 — **Merriam-Webster**(1순위): 키 등록, 무료 개인용 티어~~ → `feat/merriam-webster-adapter`에 구현 완료, **`dev` 병합 완료**(2026-07-28) — `question/dictionary/merriamWebster.ts`(raw 타입 파싱 + `isRelevantEntry` 필터링 + `uros`/`cxs`/`uns`/`lbs`/`vd`/`sdsense` 처리, 세부 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md)의 MW 노트), `question/dictionary/senseSelect.ts`(번호매김+LLM 판정/번역+서식화), `question/dictionary.ts`(파이프라인 오케스트레이션 + 다중 단어 폴백, 아래 항목 참고). 병합 후에도 실측 기반 하드닝 계속: `def[].vd`→`DictionarySense.transitive`(타동/자동사, UI·LLM 후보 목록에 "타동사"/"자동사"로 표시), `fl`("definite article"/"indefinite article")→`DictionarySense.definite`(en 3개 소스 중 MW만 구조화 지원, OEWN/Wiktionary는 항상 undefined라 자동으로 "관사" 폴백), `dt`가 `uns` 블록 하나로만 채워져 gloss 없이 통째로 버려지던 버그 수정("the"/"a"의 정관사/부정관사 핵심 뜻풀이가 안 나오던 문제).
  - [x] ~~en 어댑터 — **OEWN**(Open English WordNet, 2순위, MW 실패 시 폴백): 공식 GitHub Releases 에셋(`english-wordnet-2025-json.zip`)을 로컬 번들(Morphy 포함 라이브러리 사용). 원본 Princeton WordNet(정체·발음 정보 없음) 대신 커뮤니티가 계속 갱신하는 후속판(Global WordNet Association, CC-BY 4.0)으로 교체 확정 — 라이브 API·정적 링크(en-word.net)는 실측 결과 불안정(503)이라 GitHub Releases 직접 다운로드로 대체(세부 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#oewn-open-english-wordnet) 참고).~~ → **구현 완료(`feat/oewn-adapter`, dev 병합됨 2026-07-28)**: `question/dictionary/oewn.ts` `fetchOewnEntry(word)` — 원본 GitHub Releases JSON(entries-\*.json + 40여 개 렉시코그래퍼 파일)을 `scripts/build-oewn-bundle.py`로 `resources/oewn/{entries,synsets}.json` 두 파일로 병합해 로컬 번들(관계 정보 제외해 70MB→22MB, 재생성 시 스크립트 재실행). 표제어 직접 매칭 → 대소문자 폴백 → 활용형 역인덱스(`ran`→`run` 등, 위 항목 참고) 순으로 조회해 `DictionaryEntry<'en'>`로 변환, 네트워크 호출 없음. `run`/`ran`/`kick the bucket`/`Bach`/`mice`로 실제 번들 데이터 대상 스모크 테스트 완료. **아직 `dictionary.ts`의 정식 MW→OEWN→Wiktionary 폴백 오케스트레이션엔 안 엮여 있고, `registry.ts`/`forceSource` 디버깅 경로에서만 직접 호출된다**(오케스트레이션은 별도 담당). 병합 후 하드닝: OEWN도 CC BY 4.0이라 저작자 표시 의무가 있어(2026-07-28) `senseSelect.ts` `SOURCE_LICENSE`에 등록 — 표제어별 딥링크는 `en-word.net` 라이브 API/정적 링크가 둘 다 503으로 불안정해(위 실측) 죽은 링크 위험이 있어, 이 어댑터가 실제로 쓰는 GitHub Releases 데이터 출처인 공식 저장소 프로젝트 페이지(`github.com/globalwordnet/english-wordnet`)로 고정 연결(세부는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#oewn-open-english-wordnet) 참고).
  - [x] ~~ja 어댑터 — **daijisen**(デジタル大辞泉, 1순위, kotobank.jp 스크래핑): 위 기본형 전처리 선행 필요~~ → **`feat/daijisen-adapter`에 구현 완료(2026-07-28)** — `question/dictionary/daijisen.ts`. `kotobank.jp/word/{표제어}`로 직접 접근하면 검색 없이 정확한 ID 페이지로 자동 리다이렉트됨을 실측 확인해 별도 검색 스텝 불필요(존재하지 않으면 HTTP 404). 페이지 내 9개 안팎 소스 중 daijisen article만 채택, 그 안의 동형이의 하위 항목(`<div class="ex cf">`, 예: 花 페이지의 한자항목/곡명 항목)은 전부 별도 reading으로 후보에 넣는다(LLM이 문맥상 고르게 함, MW 동형이의어 처리와 동일 패턴). 번호매김(`<b>N</b>`→`㋐㋑㋒`)을 parentIndex로 표현하되, 뜻풀이 없이 하위만 묶는 "그룹 헤더"(실측: 食べる sense 3)는 MW sdsense 처리와 동일 방침으로 빈 부모를 만들지 않고 하위를 곧바로 독립 sense로 승격. **실측으로 새로 발견한 함정**: (0) article 경계를 고정 문자열로 찾으면 안 됨 — 실제 마크업은 `<article`과 `class=` 사이에 다른 속성이 끼어 있어 고정 문자열 검색이 항상 실패, daijisen 이후 페이지 전체(다른 사전+광고)를 통째로 삼켜버리는 버그가 실제로 났음(정규식으로 수정). (1) 한 표제어 안에 품사가 바뀌는 여러 그룹(예: 犬의 ［名］/［接頭］)이 있으면 번호가 그룹마다 1부터 재사용되는데, `[類語]`는 항상 첫 그룹(주 품사) 번호를 가리켜서 번호→sense 매핑을 first-write-wins으로 처리해야 함(overwrite 방식이면 다른 그룹에 유의어가 잘못 붙음, 犬로 실측 확인). (2) `[類語]（1）...／（2）...`처럼 한 블록에 여러 sense 번호가 "／"로 이어질 수 있음(食べる 실측). (3) 「…」가 실측용례("―"로 headword 자리표시 포함)와 정의문 안 인용(다른 단어를 「」로 지목, "―" 없음) 두 용도로 섞여 있어 "―" 포함 여부로 구분. (4) 類語 라벨 자체가 평문("花")과 `<a>` 링크("食べる") 둘 다 나올 수 있음. 품사(pos)는 daijisen 마커가 불안정하다는 기존 방침대로 채우지 않음(JMdict가 담당). `FALLBACK_CHAINS`(`dictionary.ts`) ja 배열 맨 앞에 추가 완료. → **반의어 표시 "⇔" 앞뒤 공백 없이 붙어 보이던 문제 수정(2026-07-29)**: 원문은 「」인용부호 안에 "幼稚園の子供⇔大人"처럼 붙어있어 괄호가 시각적 구분을 해줬는데, `splitGlossAndExamples`가 「」를 벗겨내면서 그 구분이 사라져 "子供⇔大人"처럼 빽빽해 보였다 — 괄호를 벗긴 뒤에도 "⇔" 앞뒤에 공백을 넣도록 수정. → **오쿠리가나 괄호 생략형 표제어 404 대응 + 예문 "―" 복원(2026-07-29)**: (1) 「落（と）す」처럼 표기에 괄호 오쿠리가나가 있는 표제어는 kotobank URL 슬러그가 생략형(落す)이라 표준 표기("落とす")로는 404가 났음(직접 선택 모드에선 곧바로 "찾지 못했습니다") — 404일 때만 kotobank 검색(`/search?q=`)에서 오쿠리가나 생략 변형 링크를 찾아 재시도하고, 표제어 파싱도 괄호 벗긴 표준 표기(headword)/생략형 포함 matchForms로 분리해 관련성 확인이 정상 항목을 거르지 않게 수정. (2) 예문의 "―"(표제어 대용 기호, 활용어는 어간만 대신함)를 읽기의 "・" 어간 경계 기반으로 실제 표제어로 복원(君 예문 "―、一緒に行こう"→"君、…", 食べる 「生で―・べる」→"生で食べる"). 세부 실측은 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#daijisen) 함정 8·9번 참고.
  - [x] ~~ja 어댑터 — **JMdict**(2순위, daijisen 실패 시 폴백 및 품사 판정 1순위 소스)~~ → **구현 및 `dev` 병합 완료(2026-07-28)** — `question/dictionary/jmdict.ts`(로컬 번들 조회, 네트워크 호출 없음) + `scripts/build-jmdict-bundle.py`(jmdict-simplified `eng` 원본 117MB → `resources/jmdict/{words,index,tags}.json` 약 62MB로 트리밍). `registry.ts`가 파일 존재로 자동 감지하므로 별도 등록 작업 없음. 세부 스키마 매핑·misc 코드 register/convention 분류표는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#jmdict)의 "구현 완료" 절 참고. → **접미사/접두사(達/たち 등)가 화면에 번역 안 된 영어 "other"로 보이던 문제 수정(2026-07-29)**: JMdict `suf`/`pref` 코드가 `CanonicalPos<'ja'>`에 전용 카테고리가 없어 `'other'`로 매핑됐는데, `formatDictionaryAnswer`(`senseSelect.ts`)가 `pos` 값이 있으면(비어있지 않으면) `POS_KO`에 없어도 그 값 자체("other")를 그대로 보여줘서 한국어 문장 사이에 영단어가 그대로 샜다 — (1) `suffix`/`prefix`를 `CanonicalPosCommon`(en/ja/zh 공통, MW `fl`도 이 라벨을 쓰고 Wiktionary도 언어 불문 "Suffix"/"Prefix" 헤더를 쓰므로)에 정식 추가해 `POS_KO`에 접미사/접두사 번역을 채우고, (2) 화면 표시 로직을 `posDisplayLabel` 함수로 분리해 "번역된 라벨이 하나도 없으면 posRaw(원본 코드)로 대체"하도록 일반화(다른 소스의 남은 `'other'` 코드—JMdict `cop`/`aux`/`exp`/`unc`/`v-unspec`—가 앞으로도 영단어를 그대로 새게 두지 않는 안전망). 이 두 수정과 별개로 en(MW `fl`)/ja(JMdict)/zh(汉典·萌典)/공용(Wiktionary) 사전 어댑터 전부의 품사 매핑 표를 다시 훑어봤고, 이 suffix/prefix 외에는 스키마가 놓치는 품사가 없음을 확인.
  - [x] ~~ja 활용형 기본형 변환(`dictionary.ts` `japaneseWordCandidates`, 2026-07-28) 도입 후 발견한 사소한 중복 — 팝업 원문 선택(atom 병합, `TOKENIZE_JA` IPC)에서 이미 한 번 형태소 분석한 텍스트를, 사전 조회 시(`japaneseWordCandidates`) **또 한 번** 형태소 분석한다(엔진은 현재 sudachi 하나 — kuromoji는 2026-07-28에 이미 걷어냄).~~ → **재호출 제거(2026-07-30, `fix/ja-tokenize-dup`)**: `shared/nlp/ja.ts`/`ja-unidic.ts`의 병합 함수 `combine()`이 여러 원시 토큰을 하나의 atom 으로 합칠 때 첫 토큰의 `baseForm`을 버리고 있던 것을 그대로 들고 가도록 수정하고, `Word` 타입(`shared/types.ts`)에 `baseForm?` 필드를 추가해 팝업이 이미 계산해둔 값을 `SelectionContext.words`에 실어 보낸다. `dictionary.ts`의 `toJapaneseDictionaryBaseForm`이 이제 `ctx.words[0]`이 조회어의 접두와 일치하면 그 `baseForm`을 그대로 재사용하고, 일치하지 않는 예외 상황(선택 범위가 atom 경계와 어긋난 경우 등)에만 예전처럼 `word`를 다시 형태소 분석하는 폴백으로 남겨둠 — 표준 경로(단일 단어 클릭, 다중 단어 폴백의 단어별 조회)에서는 sudachi 재호출이 사라짐.
    - **(2026-07-28) jisho.org 라이브 API가 아니라 jmdict-simplified 로컬 JSON 번들("full"+"eng" 변형)로 확정.** 근거: jmdict-simplified 원본 인터페이스(`scriptin/jmdict-simplified`) 실측 확인 결과 `Sense.field`(전문분야)/`misc`(사용역)/`dialect`(방언)가 각각 별도 배열이고, `Kana.appliesToKanji`/`Sense.appliesToKanji`·`appliesToKana`(이표기·읽기 제약)까지 구조화돼 있는데, jisho.org API는 이걸 전부 `tags` 배열 하나로 뭉개거나(field+misc 혼합, 실측: "レジスター"의 "Computing"과 "しどい"의 "Slang"이 같은 자리에 옴) 아예 노출을 안 함(raw priority 코드 nf01~48, ke_inf/re_inf, xref의 sense-index, `languageSource.wasei` 전부 API 응답에 없음). "simplified"는 데이터 축소가 아니라 XML→JSON 포맷 정리일 뿐이라 "full" 변형은 원본 JMdict과 엔트리 수 동일(README 확인) — 로컬 번들에 크기·커버리지 손해가 없음. "eng" 변형으로 영어 외 gloss(불/독/러 등)를 걸러내 용량만 줄임.
    - 위 결정에 따라 `DictionaryEntry.headword: string` → `string[]`로 변경(실측: "さびしい/さみしい"→["寂しい","淋しい"], jmdict-simplified `Kanji`가 이표기별로 따로 옴), `DictionaryReading.appliesToHeadwords?: string[]` 신설(jmdict-simplified `Kana.appliesToKanji` 그대로 반영, 실측: "一人" 엔트리에서 独り는 ひとり에만 적용되고 いちにん엔 안 붙음). `DictionarySense.domain?: string[]`(JMdict `field`)·`seeAlso?: string[]`(JMdict `see_also`/`related`, 이후 2026-07-28에 `SeeAlsoRef[]`로 재구조화됨 — 위 125번 항목 참고)도 이때 함께 신설, `register`는 `usageTags`로 개명(JMdict `misc`까지 포괄하도록). 이표기별 개별 우선도·주석(`Kanji.tags`, ateji/구자체 등)과 `Sense.appliesToKanji`(뜻이 특정 한자표기에만 한정)는 스코프 제외 — 실사용 예가 아직 확인 안 됐고 이 앱에 표기별 우선순위 UI가 없어 당장 필요성 낮음(dialect 필드를 뺀 것과 동일 판단).
  - [x] ~~zh 어댑터 — **汉典**(zh-Hans 1순위 `/hans/` / zh-Hant 2순위 `/hant/`, 스크래핑): 세부 실측 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#汉典-zh-hans)의 汉典 노트(近反义词 정적 파싱 가능·同音词만 lazy 등) 참고.~~ → `feat/hanyu-adapter-v2`에 구현(`question/dictionary/hanyu.ts`). 표제어 페이지가 `#jbjs`(基本解释, 한자 전용)/`#xxjs`(词语解释/详细解释, 표제어 종류별 템플릿 2종)/`#gyjs`(国语辞典) 3개 섹션으로 갈리는데, `#gyjs`가 실제론 萌典과 같은 대만 교육부 계열 데이터라 zh-Hans 1순위 소스로 부적절하다고 판단해 **`#jbjs`→`#xxjs`→`#gyjs` 엔트리 단위 폴백**(뜻풀이가 하나라도 나오는 첫 섹션 채택, 병합 안 함)으로 확정 — 실측(躺平 등 신조어)으로 3단 폴백 전체 경로 검증 완료. 트레이드오프: `#jbjs`/`#xxjs`로 확정되는 대다수 엔트리는 synonyms/antonyms가 없음(그 데이터는 `#gyjs` 전용). **알려진 미수정 이슈**: `#xxjs` 한자 템플릿의 인라인 영어 대역어(`.encs` span)가 안 걸러져 gloss에 섞여 들어감 — 발생 빈도 낮다고 판단해 보류(DICTIONARY_SOURCES.md 참고). 세부 실측 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#汉典-zh-hans)의 汉典 노트 참고. **(2026-07-30 후속)** daijisen(kotobank.jp)과 완전히 동일한 브라우저 User-Agent 문자열을 각자 상수로 갖고 있던 것을 전수 점검으로 발견 — `dictionary/httpConfig.ts`의 `BROWSER_USER_AGENT`로 통합(`refactor/reduce-duplication`, dev 병합). Wiktionary(`WIKTIONARY_USER_AGENT`, Wikimedia User-Agent 정책 준수용)는 목적이 달라 그대로 별도 유지.
  - [x] ~~zh 어댑터 — **萌典**(zh-Hant 1순위, 공개 JSON API): 세부 실측 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#萌典-zh-hant)의 萌典 노트 참고.~~ → **구현 및 `dev` 병합 완료(2026-07-28)** — `question/dictionary/guoyuCidian.ts`(教育部重編國語辭典, moedict.tw/uni/{표제어} JSON API 호출, `heteronyms`→`DictionaryReading[]`·`definitions[].type`→`CanonicalPos`·`link[]`→`seeAlso`(kind: `related`) 매핑, `quote`/`radical`/`stroke_count`/`bopomofo`계열은 DICTIONARY_SOURCES.md 결정대로 미반영). **소스 식별자를 원전 기준 `guoyu-cidian`(教育部重編國語辭典)으로 리네임(2026-07-28)** — moedict.tw(萌典)는 경유 플랫폼일 뿐이라 파일명(`moedict.ts`→`guoyuCidian.ts`)·타입/클래스명(`Moedict*`→`GuoyuCidian*`)·`DictionarySourceId`(`'moedict'`→`'guoyu-cidian'`) 전부 정정. `FALLBACK_CHAINS`(`dictionary.ts`)의 zh-Hant 1순위로 실제 오케스트레이션에도 연결 완료.
  - [x] ~~zh 어댑터 — **CC-CEDICT**(zh-Hans/zh-Hant 공통 최종 로컬 폴백): `resources/cedict.u8` 로컬 데이터셋~~ → `feat/cccedict-adapter`에 구현 완료(`question/dictionary/cccedict.ts`) — 슬래시 세그먼트를 CL:/교차참조 접두어/`(Taiwan pr. [...])`/알려진 사용역·전문분야 라벨 순으로 분류해 `gloss`/`seeAlso`/`pronunciations[].variety`/`usageTags`/`domain`/`isIdiom`으로 라우팅, 번체·간체 표기 둘 다로 인덱싱해 zh-Hans/zh-Hant 어느 쪽 조회든 같은 어댑터가 응답한다. `dictionary.ts`의 정식 `FALLBACK_CHAINS`(zh-Hans/zh-Hant 둘 다 汉典/萌典 다음 공통 최종 로컬 폴백)에 연결 완료.
    - **2026-07-28 재설계 — 세그먼트=sense로 분리.** 초기 구현은 한 발음(pinyin) 줄의 슬래시 세그먼트 전부를 sense 하나의 `gloss[]`로 합쳤는데, CC-CEDICT가 라벨을 세그먼트마다 반복 붙이는 포맷이라(예: "行"→(bound form) x4) 라벨이 sense 하나에 중복 적재되고 어느 뜻풀이에 정확히 걸리는지도 사라지는 문제가 실측 확인됨(사용자 피드백). → 세그먼트(CL:/교차참조 제외) 하나당 sense 하나로 변경. **트레이드오프**: "確定"(13개 세그먼트가 사실상 2~3개 뜻의 근의어 나열)처럼 과분할되는 항목도 있지만, CC-CEDICT 원본에 세그먼트 묶음 판단 근거가 없어 라벨-뜻풀이 매칭 정확도를 우선하기로 결정(감수). `seeAlso`는 세그먼트가 아니라 줄(발음) 전체에 걸리는 포인터라 split된 모든 sense에 복제.
    - **병음 표기 버그 발견 및 수정(2026-07-29, 실사용 중 발견)** — CC-CEDICT 원본은 병음을 `[da3 suan4]`처럼 숫자 성조로 담고 있는데(汉典/萌典은 스크래핑 시점부터 이미 발음 구별 부호 형태라 문제없음, 이 소스만 예외) 변환 없이 그대로 노출되고 있었다. 표준 병음 성조 규칙을 재구현(`numberedPinyinToDiacritic`)해 변환 후 표시하도록 수정 — ü는 "v"가 아니라 CC-CEDICT 고유 표기인 "u:"로 나온다는 것도 실측 확인해 반영. 세부 근거(자체 발견한 변환 로직 버그 포함)는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#cc-cedict) CC-CEDICT 절 참고.
    - **`DictionarySense.classifiers` 필드 폐기.** CC-CEDICT의 `CL:`(양사) 세그먼트는 표제어 전체 중 명사 뜻 하나에만 해당하는데, 세그먼트=sense 분리 이후로는 여러 split sense 중 어디에 귀속시킬 근거가 없어졌다. 원래도 `senseSelect.ts`/LLM 프롬프트 어디서도 안 읽던 미사용 필드였고 사실상 CC-CEDICT 1개 소스 전용이었던 터라(`shared/types.ts`), `CL:` 세그먼트는 이제 그냥 버리고 필드 자체를 스키마에서 제거.
    - `(bird species of China)` 라벨은 문서에 필드 배정이 없어 원문 그대로 둔 채 손대지 않음. `usageTags`/`domain`을 재실측 빈도 기준으로 확장(fig./Tw/lit./onom./Internet slang/Cantonese/physics/botany/sports/geology/anatomy/music/military/linguistics/zoology 추가) — `(sb)`/`(sth)`는 라벨이 아니라 뜻풀이 안 문법적 자리표시자라 제외. `resources/cedict.u8`이 CRLF로 재배포되는 경우가 있어 파서가 `\r\n`도 지원하도록 수정.
    - 세부 실측 근거·결정 이력은 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#cc-cedict)의 CC-CEDICT 노트 참고.
  - [x] ~~**Wiktionary 어댑터**(en/ja/zh 공용 최종 폴백, 하나의 소스를 언어 태그로 필터링) — `question/dictionary/wiktionary.ts`. REST API(뜻풀이) + dictionaryapi.dev(en 전용 발음 보강) + raw wikitext(`action=parse`, ja `{{ja-pron}}`/zh `{{zh-pron|m=}}` 발음 값만 정규식으로 추출) 3경로 조합, 실API 스모크 테스트로 en(run/ate)·ja(走る/美しい/東京/猫/食べる)·zh(捷運/打算/你好) 검증 완료. **아직 안 한 것**: ja `====Conjugation====` 활용 분류 라벨(`conjugationClass`, 一段/五段(う) 등) 추출은 스코프 밖으로 남겨둠 — 이 어댑터는 커넥터 자체만 완성된 상태고, en: MW→OEWN→Wiktionary/ja: daijisen→JMdict→Wiktionary/zh: 汉典·萌典→CC-CEDICT→Wiktionary 정식 폴백 오케스트레이션(`dictionary.ts`, 파일 상단 TODO 주석 참고)엔 아직 안 엮여 있음. 세부 실측 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#wiktionary-공용--enjazh-최종-폴백) 참고.~~ 병합 후 사용자 피드백 반영(2026-07-28): (1) 연락처 없는 User-Agent로 개발 중 HTTP 429가 잦아 `WIKTIONARY_USER_AGENT` 상수(저장소 URL 포함, 이후 개인 이메일은 제거)로 REST API/action API/dictionaryapi.dev 요청 전부 통일, (2) 발음이 여러 개인 표제어(猫→ねこ/ねこま, 東京 3개 읽기, 的→de/dì 등)에서 첫 값만 쓰던 걸 배열 전체로 채우도록 수정, (3) Wiktionary는 CC BY-SA 4.0(+GFDL) 라이선스라 저작자 표시 의무가 있어 `senseSelect.ts` 출처 줄에 표제어 페이지 링크+라이선스명 표기 추가(이후 OEWN도 같은 이유로 동일 처리, 위 OEWN 항목 참고). **간체/번체 한쪽 표기로만 페이지가 있는 경우 수정(2026-07-29, 실사용 중 발견)**: "天线"(간체, antenna)은 en.wiktionary.org REST API가 404를 주는데 "天線"(번체)은 정상 응답 — 표제어 자체가 한쪽 스크립트에만 있는 경우를 기존 폴백(같은 표제어의 raw wikitext 재확인)이 못 잡고 있었음. CC-CEDICT 번들(이미 로드해 씀)에서 반대 표기를 찾는 `findOtherScriptVariant`를 신설해 조회 완전 실패 시(zh 한정) 한 번 더 시도하도록 수정 — 화면엔 실제 조회에 성공한 표기가 아니라 사용자가 선택한 원래 표기를 그대로 보여줌. 세부 근거는 [DICTIONARY_SOURCES.md](DICTIONARY_SOURCES.md#wiktionary-공용--enjazh-최종-폴백) 참고.
  - [x] **정식 폴백 오케스트레이션 구현 완료(2026-07-28)** — `question/dictionary.ts`의 `FALLBACK_CHAINS`: en `merriam-webster→wordnet→wiktionary`, ja `daijisen→jmdict→wiktionary`, zh-Hans `hanyu-dict→cc-cedict→wiktionary`, zh-Hant `guoyu-cidian→hanyu-dict→cc-cedict→wiktionary` 순으로 앞 소스가 못 찾거나(entries 없음) 실패(네트워크 에러 등)하면 조용히 다음으로 넘어간다(`lookupThroughFallbackChain`, 개별 에러는 `console.warn` 진단용으로만 남김 — 위 149번 항목의 "파싱 실패 → 다음 폴백 자동 전환"을 이걸로 해결). 소스별 조회 로직(`fetchSourceEntries`)을 폴백 체인과 기존 `forceSource` 디버깅 경로가 공유하도록 통합해 중복 제거(부수 효과로 JMdict/汉典/教育部重編國語辭典도 이제 forceSource 강제 호출 지원). 팝업 툴바에 "직접 선택" 체크박스 토글 추가(기본값은 이후 켜짐으로 변경됨, `PopupScreen.tsx`/`Toolbar.tsx`) — 켜면 드롭다운에서 고른 소스를 강제 호출, 꺼지면 정식 폴백을 탄다. 디버깅 강제 호출 기능(`registry.ts` + 팝업 토글/드롭다운) 자체는 나중에 걷어낼 것을 감안해 `dictionary.ts`에서 여전히 명확히 구분된 블록으로 분리해둠. → **forceSource 도 활용형 후보를 타도록 수정(2026-07-29)**: `lookupForcedSource`가 표면형 하나만 쿼리해서, "行って"(行く의 て형)처럼 활용형은 "직접 선택"으로 daijisen/JMdict/Wiktionary 어느 소스를 골라도 항상 "못 찾음"이었다(실사용 확인) — `lookupThroughFallbackChain`과 같은 `wordCandidatesFor`(japaneseWordCandidates/englishWordCandidates)를 공유해 후보 전부를 시도하고 entries를 합치도록 통일. 후보 중 하나가 그 자체로도 표제어일 수 있어도(실측: "食べられる"가 JMdict에 가능형으로 직접 등재돼 있음) 첫 성공에서 멈추지 않고 기본형("食べる") 후보까지 마저 시도해 원형 뜻도 LLM 후보 목록에 같이 들어가게 함(en "closed"/"close" 사례와 동일 원칙). → **"직접 선택" 정식 기능으로 격상(2026-07-30)**: 디버깅 임시 기능이라는 딱지를 떼고(사용자 결정) 정식 기능으로 문서/주석을 정리(`dictionary.ts`/`registry.ts`/`Toolbar.tsx`/`PopupScreen.tsx`의 "임시"/"디버깅용"/"제거 예정" 표현 제거). 토글 기본값도 켜짐 → **꺼짐**으로 되돌려(정식 폴백이 기본이고, 특정 소스로 직접 조회하고 싶을 때만 사용자가 켜는 형태) 실사용 중 다중 단어 폴백 버그(위 항목 2026-07-30 참고)를 우회로라도 덜 겪게 함.
  - [x] ~~다중 단어 선택 처리(en)~~ → **`feat/dictionary-en-multiword`에 구현 완료, `feat/merriam-webster-adapter` 경유해 `dev`에 병합 완료(2026-07-28)**. 최종 설계는 초기 계획(아래 취소선 4단계)과 달리 "atom 분해 결과 재사용"이 아니라 **단어 하나짜리 파이프라인(MW 조회→LLM 판정/번역→서식화)을 그대로 병렬 재사용**하는 쪽으로 갔다 — 단어별 뜻이 서로 독립적이라(관용구가 아닌 이상) 하나의 프롬프트/판정으로 합칠 필요가 없었기 때문:
    1. 선택된 텍스트 전체를 먼저 표제어로 조회(관용구는 통째로 사전에 있을 수 있음 — 예: "kick the bucket")
    2. 실패하면 공백/하이픈 기준으로 단어를 쪼개(en, `splitIntoWords`) 각 단어에 대해 같은 단일 단어 파이프라인을 `Promise.allSettled`로 병렬 호출, 성공한 것만 이어붙임. 기능어(관사 등) 필터링은 별도로 안 함 — MW가 그 단어에서 못 찾으면 자연히 결과에서 빠짐.
    3. **`MAX_FALLBACK_WORDS = 5`**로 상한(초과 시 폴백 자체를 안 하고 원문 통째 조회의 MW `suggestions`만 보여줌) — 실측 확인: 5단어 넘는 긴 속담("the early bird catches the worm" 등)은 사용자가 실제로 드래그하는 표면형으로는 MW 정확 매치가 거의 항상 실패하고(대명사 정규화 등, 예: "count one's chickens (before they hatch)"으로만 등재), 단어별로 쪼개도 관용적 의미가 안 살아나 폴백의 실익이 낮음 — 대신 이 경우 MW의 `suggestions`(정규화된 표제어 제안)가 더 유용해서 그것만 보여줌.
    4. 개별 단어 조회/LLM 호출이 실패해도(네트워크 순간 오류 등) 전체를 실패시키지 않고 그 단어만 건너뜀 — 통째 조회 단계에서 이미 네트워크·API 키 확인이 끝난 상태라 남은 실패는 그 단어 하나만의 문제로 취급.
    5. ~~ja는 단어별 조회 전에 활용형 전처리(基本形 치환)를 각각 적용~~ — ja/zh 어댑터가 아직 없어 이 부분은 en과 별개로 나중에 재설계 필요(en은 활용형이 원형과 교차 연결돼 있어 전처리 자체가 불필요, ja/zh는 `splitIntoWords`도 공백 기준이 아니라 형태소 분석/양사 인식 기반으로 다시 짜야 함). **`MAX_FALLBACK_WORDS=5`라는 상한값 자체는 ja/zh 도입 시에도 그대로 재사용할 계획**(2026-07-28 결정) — "무엇을 1단위로 셀지"만 언어별로 다르고(en: 공백/하이픈, ja/zh: `main/nlp/japanese.ts`·`main/nlp/chinese.ts`의 기존 형태소 분석/분절 엔진 결과 재사용), 상한 개수 정책 자체는 언어 공통으로 둔다. → **(2026-07-30) 계획대로 ja/zh로 확장 + forceSource 경로 누락 수정.** 사용자 제보("must have been exorbitant"의 뜻이 "직접 선택"(forceSource) 켠 채로는 안 찾아짐)를 조사하다 두 가지를 발견: (1) 다중 단어 분해가 여전히 `ctx.language !== 'en'`이면 통째로 스킵돼, 위 2026-07-28 결정과 달리 ja/zh는 실제로 구현이 안 돼 있었음 — 팝업이 항상 원자 단위로 선택해준다는 예전 가정이 틀렸다(드래그로 여러 단어/문장도 선택 가능). `splitIntoDictionaryWords(text, language)`를 신설해 en은 기존 `splitIntoWords`, ja/zh는 `segmentJapaneseWords`/`segmentChineseWords`(main/nlp)로 분해하도록 `dictionary.ts`에 적용. (2) 이 분해 로직이 애초에 정식 폴백 경로(`lookupDictionary`)에만 있고 강제 소스 선택 경로(`lookupForcedSource`)엔 전혀 없었음 — "직접 선택" 토글 기본값이 켜져 있어(Toolbar.tsx) 실사용에서 이 경로를 자주 타는데도 다중 단어 선택은 항상 "못 찾음"으로 끝났던 것. `lookupForcedSource`의 조회 로직을 `lookupForcedSourceOnce`로 뽑아내 통째 조회/단어별 폴백 양쪽에서 공유하도록 리팩터링 — "강제"는 소스 하나만 고정한다는 뜻이지 그 소스 내부의 분해 폴백까지 건너뛴다는 뜻이 아니므로, 이제 forceSource로 어떤 소스를 골라도(daijisen/JMdict/汉典/萌典/CC-CEDICT/Wiktionary/MW/OEWN 전부) 정식 폴백과 동일하게 다중 단어 분해가 적용된다.
  - [x] ~~한국어 설명 처리 — 사전 API는 원어 sense 목록만 제공, LLM이 문맥 판정 + 한국어 설명을 함께 생성하도록 프롬프트 구성~~ → `prompts/dictionary.txt` + `senseSelect.ts`(`buildSenseListText`/`parseJudgeReply`/`formatDictionaryAnswer`)로 구현 완료 — LLM이 번호 판정과 함께 뜻풀이·대응어·예문을 한국어로 번역해 "번호/대응어/번역/예문번역" 블록으로 답하면 `judgeAndFormat`(`dictionary.ts`)이 이를 파싱해 채팅창용 마크다운으로 서식화한다. → **TEMP DEBUG: LLM이 준 "대응어" 원본을 디버깅 줄에 표시(2026-07-29)**: `combineTranslation`(`senseSelect.ts`)이 대응어 길이가 번역문 대비 일정 비율을 넘으면 화면에서 조용히 빼버려서, 실제 화면에 대응어가 안 보일 때 "LLM이 애초에 안 줬는지" vs "줬는데 로컬에서 걸렀는지" 구분이 안 된다는 피드백 — `SelectedSense.rawCounterpart`(필터링 전 원본, 없으면 null)를 추가하고 `withDebugCountsLine`(`dictionary.ts`)의 `[DEBUG]` 줄에 번호별로 `"텍스트"` 또는 `없음`으로 표시하도록 확장. 프롬프트(`prompts/dictionary.txt`)의 대응어 요청은 원래도 언어(en/ja/zh) 불문 동일 템플릿.
  - [x] ~~(2026-07-28 재검토: 우선순위 상향 검토 필요) 스크래핑 소스(daijisen/汉典/萌典)의 안정성 대비 — 페이지 구조 변경/일시 차단 시 다음 폴백 단계로 자연스럽게 넘어가도록 에러 처리. 이 셋이 ja/zh 폴백 체인의 1순위(daijisen/萌典) 또는 1-2순위(汉典)라 "선택 사항"이 아니라 실제로는 ja/zh 사전 기능 전체의 가용성을 좌우함 — 최소한 "파싱 실패 감지 → 다음 폴백 자동 전환"만이라도 다른 구현보다 먼저 하는 게 안전.~~ → **정식 폴백 오케스트레이션 구현으로 함께 해결(2026-07-28)** — 아래 참고.
  - [ ] 상업화 시 재검토(별도 트리거 필요, 지금은 미착수) — en: MW 제외하고 OEWN+Wiktionary만 / ja: daijisen 제외하고 JMdict+日本語WordNet(NICT) 조합으로 교체 / zh: 汉典·萌典 제외하고 CC-CEDICT 중심 + 有道詞典 API(유료) 검토. 상세 근거는 PLAN.md §6 참고
- [x] ~~네이버 사전 바로가기 — 위 LLM 뜻 번호 판정과는 별개로, 툴바에 네이버 로고 + [사전] 버튼을 추가해 언어별(en/ja/zh) 네이버 사전 페이지를 외부 브라우저 새 창으로 연다(`question/naver.ts` `naverDictionaryUrl`, 언어별 서브도메인은 `shared/languages.ts` `naverDictSubdomain`). 새 창 열기 로직은 기존 `google.ts`에 있던 것을 `question/browser.ts`(`openUrlInNewWindow`)로 분리해 구글/네이버가 공유. 네이버는 공식 API가 없고 스크래핑은 ToS 위반 소지가 있어, LLM 뜻 번호 판정과 달리 "사용자가 직접 찾아보는" 바로가기 용도로만 제공~~
- [x] ~~통합 질문: 자유 프롬프트 입출력 — `askLlm` 전체 파이프라인 완성(`question/index.ts` `runQuestion` → `llm/adapter.ts`, 스트리밍 포함)~~
- [x] ~~자주 쓰는 질문: 등록 / 수정 / 삭제 + 영속화 — `popup/FrequentQuestions.tsx`. main 프로세스 `userData/frequent.json` 에 파일 저장(`main/frequentStore.ts` + IPC `FREQUENT_GET`/`SET`, 렌더러는 `popup/frequentStore.ts` 얇은 래퍼). localStorage 임시 저장에서 이전 완료 → 재시작·재설치 후 유지~~
- [x] ~~구글 검색 — 발음/이미지 탭을 **외부 브라우저로 여는 것까지 구현**(`question/google.ts`, `ipc.ts` `OPEN_GOOGLE`). 기본 브라우저 번들을 감지해 브라우저별로 분기: Chromium 계열은 `open -b <bundle> --args --new-window --window-position=... --window-size=... <url>`(mac), Firefox는 `--new-window`, Safari는 AppleScript로 새 문서+위치 지정 — `open -n`은 Chrome이 단일 인스턴스라 무시되고 새 탭으로 열려버려서 쓰지 않는다(코드 주석에 명시). Windows도 레지스트리로 기본 브라우저를 찾아 `--new-window`를 강제하는 별도 로직(`openWinNewWindow`)이 있고, 감지 실패 시(및 Linux)에만 `shell.openExternal`로 폴백. PLAN §4.2 "팝업 속 팝업"(임베드형 `BrowserWindow` child)은 채택하지 않기로 확정 — 현재의 외부 브라우저 새 창 방식이 최종 구현.~~
  - [x] ~~Safari로 새 창(발음/이미지/사전 검색 결과 등)을 열었다가 닫으면, 원래 열려 있던 다른 Safari 창들이 사용자가 하던 작업 위로 끌려나오는 문제(사용자 보고, 2026-07-28)~~ → **완화 완료(`browser.ts`)** — Safari는 단일 프로세스라 창을 여러 개 열어도 전부 같은 앱 인스턴스에 속해, 새 창을 활성화하면 Safari 앱 전체가 활성 앱이 되고 나중에 그 창을 닫으면 macOS가 같은 앱의 다음 창(원래 열려있던 다른 Safari 창)을 자동으로 앞에 올려버림 — Safari 창끼리는 OS 차원에서 완전히 독립될 수 없는 구조적 한계. 새 창을 활성화하기 직전에 "원래 활성 앱"을 기억해두고(`frontmostAppBundleId`), 새로 만든 창의 id를 받아서(`watchSafariWindowAndRestoreFocus`) 백그라운드(메인 흐름을 안 막는 detached 프로세스)에서 그 창이 사라질 때까지 1초 간격으로 확인 → 사라지면 원래 앱을 다시 활성화. 창 생성/id 캡처/frontmost 조회/재활성화 각 API 호출은 개별적으로 실제 동작 확인했으나, "닫았을 때 포커스가 정확히 되돌아오는지" 전체 흐름은 터미널 명령 실행 자체가 포커스에 영향을 줘 도구로는 확실히 관찰 못함 — 실사용 확인 필요.
  - [x] ~~채팅창 마크다운 안의 링크(사전 출처 등)를 구글/네이버 버튼과 같은 방식(기본 브라우저 새 창)으로 열기~~ → **완료** — react-markdown이 만드는 평범한 `<a>` 클릭을 그대로 두면 Electron 렌더러가 그 URL로 팝업 창 자체를 이동시켜버리는 문제 발견(`windows.ts`의 `setWindowOpenHandler`는 `target="_blank"`류의 "새 창 열기 시도"만 가로채고 평범한 클릭은 안 걸림). IPC 채널 `OPEN_EXTERNAL_LINK`를 신설해 구글/네이버와 같은 `openUrlInNewWindow` 경로를 타게 하고, `Chat.tsx`에서 `ReactMarkdown`의 `a` 컴포넌트를 오버라이드해 기본 이동을 막고 이 채널을 호출하도록 변경 — 사전 출처 링크뿐 아니라 채팅에 나오는 모든 마크다운 링크에 개별 배선 없이 일괄 적용됨.

<a id="b-ui"></a>

**UI · 설정**
- [x] ~~팝업 화면: 원문 + 툴바(발음·사전 버튼 / 입력 / 구글) + 채팅 + 자주쓰는질문 (`PopupScreen.tsx` + `popup/*`)~~
- [x] ~~팝업 UI 다듬기 — 창 크기를 설정 창과 통일(1200×800), 다크→라이트 모드 전환(설정 화면과 동일 팔레트), 선택 하이라이트가 atom·gap 조각마다 라운드가 있어 끊겨 보이던 것을 이어지게 수정, 원문 문맥 박스 내부 스크롤을 없애고 텍스트 양만큼 자동으로 높이 확장, 구글 로고를 발음/이미지 버튼 왼쪽에 배치, 자주 쓰는 질문 순서를 드래그(HTML5 DnD)로 변경(`popup/FrequentQuestions.tsx`)~~
- [x] ~~툴바 버튼 그룹 순서 반전 — 구글(발음/이미지) 그룹을 왼쪽, AI(발음/사전) 그룹을 오른쪽으로 배치 변경(`popup/Toolbar.tsx`). 구글 로고도 `AI` 배지와 동일한 `.llm-badge` 스타일(패딩·라운드)로 감싸 두 그룹의 배치감을 통일(`styles.css` `.llm-badge`에 `inline-flex` 정렬 추가)~~
- [x] ~~채팅 세션 상태 유지 — 팝업 = 1세션. `PopupScreen.tsx` 가 `messages` 상태를 누적하고 `question()` 호출 시 `history: ChatTurn[]`(에러 메시지 제외)를 함께 전달~~
- [x] ~~발음·사전 버튼 — 토글이 아니라 원샷 액션(`popup/Toolbar.tsx` `onPron`/`onDict` + `PopupScreen.tsx` `askPronunciation`/`askDictionary`). 누르면 즉시 채팅에 고정 라벨('발음 질문'/'사전 검색')로 질문이 남고 답변이 다른 채팅 메시지와 동일하게 스트리밍됨. 발음은 LLM 연동 완료, 사전은 아직 스텁이라 빈 답변~~
- [x] ~~스트리밍 렌더 (`QUESTION_STREAM` 수신) — `PopupScreen.tsx` `onQuestionStream` 구독 후 진행 중 말풍선에 델타 append, `popup/Chat.tsx` 가 커서 표시~~
- [x] ~~에러 배너/토스트 — `QuestionResult.error` 존재 시 `error.code`별 안내. `popup/Chat.tsx` 가 `errorTitle(code)` 로 배너 렌더(발음/사전도 채팅 메시지로 통합되면서 에러도 같은 경로로 렌더됨, 구 `InfoRow`는 제거됨). (재시도/설정 이동 버튼은 미구현)~~
- [x] ~~설정 화면 5개 섹션: LLM 선택 / API 키(입력·보기·수정·삭제) / 단축키 / 문맥 범위(Byte, 구 "AI 주변 범위") / 언어 (`SettingsScreen.tsx`). API 키는 사진과 동일하게 현재 선택된 LLM 1개에 대해서만 표시. 단축키는 실제 keydown 캡처로 accelerator 문자열 생성(수식키 필수·F1-F12 예외·Esc 취소), OS별 표시·Cmd/Ctrl 이중 지원·해제는 위 공동 항목 참고. Byte 범위는 **자유 지정**(연속 슬라이더 + 숫자 입력, 상한은 고정값이 아니라 설정 화면 미리보기 텍스트 길이로 동적 계산되는 `BYTE_MAX`, `clampByte`)이며 **앞/뒤 예산 분리**(`contextBytesBefore`/`After` + `contextBytesLinked` 로 동일값 잠금) + 미리보기(`@shared/context` `computeContextRange` 공유). API 키 섹션엔 **유효성 검사 상태 + 사용 모델 드롭다운**(위 B-llm 항목) 포함~~
  - **(2026-07-30 추가) 단축키/문맥 범위(Byte) 섹션에 "기본값으로 초기화" 버튼 추가** — `settingsStore.ts`에 `getDefaultSettings()`(DEFAULT_SETTINGS 사본 반환) 신규 export, IPC(`SETTINGS_GET_DEFAULTS`)로 렌더러에 노출. `SettingsScreen.tsx`가 마운트 시 1회 조회해두고, 각 섹션 제목 옆 버튼(`.reset-btn`)이 눌리면 그 섹션 필드들만 골라 `patch()`로 되돌린다(단축키 6개: modeShortcut/settingsShortcut/windowSelectShortcut/windowDeselectShortcut/manualRegionShortcut/forceOcrShortcut, 문맥 범위 3개: contextBytesBefore/After/Linked) — 코드에 이미 있던 기본값(`DEFAULT_SETTINGS`)을 그대로 되돌리는 것뿐이라 별도 기본값 정의는 추가하지 않음.
- [x] ~~API 키 `safeStorage` 암호화 저장·로드 영속화 — `keyStore.ts`, `userData/apikeys.json`(암호문 base64)~~
- [x] ~~앱 설정 영속화 (파일 저장) — `settingsStore.ts`, `userData/settings.json`~~
- [x] ~~아이콘/UI 통일 정리 — 이모지(⌨️/ℹ️/💸/⚙️) 를 SVG 아이콘으로 교체, 항목별로 제각각이던 수정/삭제 버튼을 공용 `EditDeleteGroup`(`screens/EditDeleteGroup.tsx`, 연필+휴지통 아이콘 쌍)으로 통일해 자주 쓰는 질문·API 키·단축키에 동일 적용. 연필/휴지통/설정 톱니 아이콘은 손으로 그린 SVG 대신 `lucide-react`(Pencil/Trash2/Settings)로 교체(`screens/icons.tsx`). 눈(EyeOff) 아이콘의 슬래시 방향과 눈 윤곽 틈이 어긋나 구멍처럼 보이던 것도 수정~~
- [x] ~~팝업 전체 글자 크기 확대(`styles.css` `.popup-screen` 이하 대부분의 `font-size`를 2px씩 상향: 기본 14→16px, 라벨류 12→14px, 채팅/입력 13→15px 등) + 툴바 구글 로고 왼쪽 여백 추가(`.toolbar`에 `padding-left: 8px`)~~
- [x] ~~(2026-07-30) 팝업/설정 UI 자잘한 정리 일괄~~ → 팝업 헤더 언어 표기를 원어(`getNativeLanguageName`, 예: 简体中文)에서 한국어 이름(`getLanguageName`, 예: 중국어(간체))으로 교체. "문자 단위 선택" 토글을 툴바에서 "범위 지정" 헤더(`.ctx-head`) 우측으로 이동 — 실제 적용 대상이 원문 문맥(ContextView)이라 그쪽에 붙는 게 자연스럽다는 판단(`Toolbar.tsx`의 showCharLevelToggle/charLevel prop 제거). 메인 화면 "창 선택" 버튼이 데모 미리보기 버튼 5개가 같은 블록 안에 쌓여있어 grid 중앙 정렬 기준이 아래로 밀리던 문제 — 데모 버튼들을 `.demo-panel`로 묶어 `position:absolute`로 버튼 오른쪽에 분리(나중에 제거 시 이 div + CSS 규칙 두 개만 지우면 됨). 팝업 원문 문맥(`.ctx-text`) 줄 간격/자간이 en(1.7/자간없음) vs ja·zh(2/0.05em)로 들쭉날쭉하던 걸 CJK 쪽 값으로 통일. macOS 트레이 아이콘이 32px로 리사이즈돼 가로로 길쭉해 보이던 문제 — macOS만 22pt(메뉴바 표준)로 분기. 설정 화면 언어 tier 안내 문구 다수 정리: "형태소분석기" 붙어쓰기 수정, tier1 OCR/발음을 "특화 OCR"/"특화된 발음"으로 tier2의 "범용 OCR"과 대비되게 명시(드롭다운 라벨도 동일하게 통일), Merriam-Webster 섹션에 LLM 섹션과 중복되던 암호화 안내 문구 제거, "그 언어" → "해당 언어".
- [x] ~~(2026-07-30) 한중일영 폰트 통일 — Noto Sans KR/JP/SC/TC 번들링~~ → 맥(San Francisco/히라기노/핑팡)과 윈도우(Segoe UI/메이리오·유고딕/야헤이·젠헤이)가 서로 다른 시스템 고딕을 쓰던 걸 그만두고, 앱이 직접 들고 다니는 Noto Sans 가변 폰트(wght 100~900 파일 하나)로 통일 — 한국어/영어는 `Noto Sans KR`(라틴 포함), 일본어는 `Noto Sans JP`, 중국어는 간체/번체 자형이 달라(한자통합) `Noto Sans SC`/`Noto Sans TC`로 분리(`PopupScreen.tsx`가 `lang-zh-hans`/`lang-zh-hant`로 분기, 기존엔 `lang-zh` 하나로 묶여있었음). tier2 언어(아랍어·러시아어·태국어 등 55개)는 대상 밖 — 지금처럼 OS 시스템 폰트 자동 폴백에 맡긴다(사용자 확인). 폰트 파일(총 ~50MB, `src/renderer/src/assets/fonts/`)은 `resources/`와 같은 원칙으로 커밋하지 않고 `scripts/fetch-fonts.sh`가 google/fonts 저장소에서 받아온다(`ensure-resources.sh`가 predev/prebuild에서 자동 호출). 앱이 폰트를 직접 통제하게 되면서, 예전엔 OS 폴백 폰트(특히 Windows Yu Gothic UI)의 빽빽함을 보정하려고 `.ctx-text`에 걸어뒀던 `line-height:2`/`letter-spacing:0.05em` 강제값(바로 위 항목에서 CJK 값으로 통일했던 그 값)도 더 이상 필요 없어져 제거 — Noto 자체 지표(디폴트값)에 맡긴다.

<a id="b-ext"></a>

**브라우저 확장 · 자막 추출 (MV3)**

원래 담당 A(팝업 전) 영역이었으나 **담당 B로 이관**(2026-07-28) — 자막 버퍼(앞뒤 문맥)가 팝업 표시·문맥 구성과 직결돼 B의 문맥 로직과 붙어 있는 게 자연스러워서. 확장은 크롬(MV3)만 대응. 통신은 native messaging 대신 **로컬 WebSocket**(Electron main이 localhost 서버, 확장 background가 클라이언트)으로 결정 — native messaging host의 OS별 매니페스트 등록(레지스트리/plist) 없이 개발·배포가 단순해서. **유튜브·넷플릭스 둘 다 구현 완료**(2026-07-29, `feat/browser-subtitles`에 `feat/netflix-subtitles` 통합, dev 병합됨) — 이후 유지보수는 이 브랜치가 두 사이트 모두 담당.

**핵심 설계(최종, 여러 번 갈아엎은 뒤 정착)**: 화면 자막 DOM(유튜브 `.ytp-caption-segment` / 넷플릭스 `.player-timedtext-text-container`, 공용 유틸 `extension/src/domWords.ts`)에서 단어별 뷰포트 좌표를 얻어 **hover 박스·클릭을 확장이 페이지 안에서 직접 그린다**(`extension/src/highlight.ts`) — 오버레이로 좌표를 앱에 릴레이하던 초기 설계는 크로스 프로세스 지연으로 hover가 어긋나 폐기. 전체 자막(앞뒤 문맥)은 **플레이어 자신의 실제 네트워크 요청을 MAIN world 스크립트로 가로채는 방식**으로 확보(유튜브는 `networkHook.ts`가 fetch/XHR 패치, 넷플릭스는 MSL 암호화라 `netflixNetworkHook.ts`가 `JSON.parse`/`JSON.stringify` 후킹) — 유튜브는 timedtext 직접 호출→InnerTube API→native TextTrack 순으로 시도했다가 전부 세션 검증/빈 응답으로 막혀 이 방식으로 정착(`extension/src/timedtext.ts` 상단 주석 참고). 자막은 언어 무관하게 CJK(한자/가나)는 글자 단위로 앵커링해 팝업이 열린 뒤 앱의 zh/ja 형태소 분석기가 실제 단어 경계로 재조합한다(`wordSegments` 프로토콜).

- [x] ~~확장 번들 파이프라인~~ → `scripts/build-extension.mjs`(esbuild), 엔트리 4개(`background`/`content`/`networkHook`/`netflixNetworkHook`) → `extension/dist`. 메인 워크트리는 이 `dist`를 그대로 복사해 로드(워크트리에서 빌드 후 수동 복사).
- [x] ~~확장↔앱 WebSocket 통신 계층~~ → `bridge.ts`(main)/`background.ts`(확장). keepalive ping, 지수 백오프 재접속, `chrome.alarms`로 서비스워커 강제 기상, 재접속 시 desired 상태(자막 캡처 on/off) 재동기화까지 포함.
- [x] ~~탭/URL 변화 감지 → 앱에 재판정 통지~~ → `activeTab.ts`(`activeTabTracker`), 분류(kind+isMedia)가 실제로 바뀔 때만 재판정/로그. → **자막 페이지 이탈 시 OCR 자동 전환 대신 선택 모드 종료(2026-07-29, 사용자 요청)**: 자막 모드(`isMedia=true`)로 있다가 재판정 결과 `isMedia=false`(유튜브 영상을 보다가 채널/홈 등으로 이동)가 되면, 예전엔 그대로 OCR 경로(영역 자동/수동 지정)로 자동 전환됐는데 이게 엉뚱한 페이지에 OCR 드래그 안내를 띄우는 부작용이 있었다 — `shortcut.ts`의 `applyExtractionDecision`에서 `isSubtitleModeActive()`가 true인 채로 `decision.mode !== 'subtitle'`이 되면 OCR로 넘기지 않고 `exitSelectMode()`(단축키 토글의 "끄기" 분기와 공유하는 헬퍼로 추출)로 선택 모드 자체를 끈다. 자막 모드였던 적이 없는 일반 진입(비-미디어 페이지에서 처음 선택 모드에 들어간 경우)은 기존처럼 OCR로 그대로 진입.
- [x] ~~유튜브 자막 DOM 추출 + 좌표~~ → `youtube.ts` + 공용 `domWords.ts`(CJK 글자 단위 토큰화, 후리가나 `<rt>`/`<rp>` 제외, `MutationObserver`+300ms 폴링 폴백).
- [x] ~~유튜브 timedtext 전체 자막 버퍼~~ → 네트워크 가로채기(`networkHook.ts`)로 확보, `subtitleSource.ts`가 문장 종결부호 기준(없으면 cue 단위) 줄바꿈으로 조립.
- [x] ~~좌표 보정 + 오버레이 연동~~ → 오버레이 릴레이 방식 폐기, 확장이 페이지 안에서 직접 hover 박스를 그리는 방식(`highlight.ts`)으로 대체돼 좌표 보정 자체가 불필요해짐.
- [x] ~~클릭 → `ExtractedSelection`(앞뒤 자막 포함) → 팝업~~ → `subtitleSource.ts`(`buildSelection`/`anchorInTranscript`), 팝업 열림/닫힘에 맞춰 영상 자동 일시정지/재생 + 팝업 닫힘 후 브라우저로 OS 포커스 복귀까지 구현. → **자막 문단 줄바꿈 판정 재수정(2026-07-29, 사용자 제보 — 유튜브 나레이션류 영상에서 문장이 안 끝났는데 cue 경계에서 강제 줄바꿈됨)**: `anchorInTranscript`가 "구두점 있는 자막"인지 판단할 때 "cue가 그 자리에서 끝나는가"의 **비율**로 봤는데, 자막 cue 컷은 보통 시간/화면폭 기준이라 문장 경계와 거의 안 맞아떨어진다 — 그래서 진짜로 구두점이 촘촘한 나레이션 자막도 비율이 낮게 나와 "구두점 없음"으로 오판하고 모든 cue 경계에서 강제 줄바꿈해버렸다. cue 끝인지 여부와 무관하게 전체 텍스트 안 어디에든 문장부호가 얼마나 자주 나오는지(밀도, cue 당 평균 개수)로 판단 기준을 바꿔서, 넷플릭스류 대사 자막(거의 무구두점, 가끔 "…"만 섞임)과 나레이션류(문장마다 하나씩)를 구분한다. 합성 placeholder 텍스트로 시뮬레이션해 수정 확인. → **되돌림(2026-07-29, 사용자 제보 — 중국어 넷플릭스 대사 자막 스크린샷)**: 밀도 기준으로 바꾸니 정작 넷플릭스 대사 자막처럼 cue(대사 한 마디)마다 "?"/"." 로 자주 끝나는 콘텐츠도 밀도가 높게 나와 "나레이션"으로 오판해, 원래 cue(대사)마다 줄바꿈되던 게 문장 단위로 죽 이어져버렸다. 원래 비율 기준으로 되돌리고, 나레이션 케이스는 진단 로그(구조적 통계만, 자막 원문은 안 남김)로 실제 수치를 다시 확인한 뒤 별도로 고치기로 함 — 아직 미해결. → **클릭한 단어와 다른 단어가 선택되는 버그 수정(2026-07-29, 사용자 제보 — 유튜브 자막에서 특정 단어를 클릭했는데 팝업엔 다음 자막 줄의 첫 단어가 선택된 채로 뜸)**: `anchorInTranscript`가 클릭된 cue를 재생 시각(`currentTime`)만으로 판단했는데, 클릭이 cue 전환 직전/직후 타이밍에 걸리면 `currentTime`이 이미 다음 cue로 넘어가 있어 엉뚱한 cue를 고를 수 있었다 — 그 cue 텍스트엔 클릭한 줄/단어가 당연히 없으니 결국 그 cue 맨 앞 글자로 폴백되면서 완전히 다른 단어가 선택됐다. 화면에 실제로 뜬 줄(`currentLine`)을 실제로 포함하는 cue를 먼저 직접 찾도록(시간이 가장 가까운 후보 채택) 바꾸고, 못 찾을 때만 기존 시간 기준으로 폴백한다.
- [x] ~~넷플릭스 자막 추출~~ → `feat/netflix-subtitles`를 병합 통합(2026-07-29). 매니페스트 `JSON.parse`/`JSON.stringify` 후킹(`netflixNetworkHook.ts`, Language Reactor류 도구와 동일 기법)으로 WebVTT 다운로드 URL 확보 → `parseWebVtt`로 파싱 → 유튜브와 같은 `transcript` 파이프라인 재사용. 실브라우저 검증 중 발견한 버그 다수 수정: 매니페스트 재시도 차단(첫 매니페스트 실패 시 영영 재시도 안 함), 전체화면 중 hover 박스가 top layer 밖이라 안 보임, 후리가나가 자막 본문에 섞임. **매니페스트 필드명 신포맷 대응(2026-07-29)**: 넷플릭스가 매니페스트 필드 이름을 `timedtexttracks`/`ttDownloadables`에서 `textTracks`/`downloadables`로 바꿔, 옛 이름만 보던 후킹이 매니페스트를 한 번도 못 잡아 넷플릭스 문맥 자막이 아예 안 떴었다(Worker 파싱 문제로 오판했으나 실제 원인은 필드명 변경 — Chrome에 설치된 Language Reactor 확장의 `pageScript_lln.min.js`를 실측 분석해 확정). 두 이름을 모두 받아 구포맷 모양으로 정규화하고, 요청(JSON.stringify)에 `supportsPartialHydration`/`showAllSubDubTracks`를 함께 켜(트랙에 실제 다운로드 URL이 hydrated로 담기게) 넷플릭스 자신의 객체는 사본에만 변형을 가하도록 수정. URL 추출도 `downloadUrls`(신)/`urls`(구) 양쪽 지원, `rawTrackType` 대소문자 무관 비교. `parseWebVtt`에 `MM:SS.mmm`(시간 생략) 허용 + TTML(dfxp) 폴백 파서 추가.
- [x] ~~선택 모드 단어 하이라이트 렌더~~ → `highlight.ts`(오버레이 대신 페이지 내부 직접 렌더로 확정). hover 시 커서를 pointer로 변경. → **모서리가 각져 보인다는 피드백으로 살짝 둥글게(2026-07-29)**: `shared/highlightStyle.ts`(Electron 오버레이·크롬 확장 공용 단일 소스)의 `WORD_BOX_STYLE`에 `borderRadius: 3`(px) 추가 — `Overlay.tsx`(단어 hover 박스)와 `extension/src/highlight.ts`(유튜브/넷플릭스 자막 hover 박스) 둘 다 이 값을 그대로 반영.
- [x] **확장 리로드 후 기존 탭 자동 복구(2026-07-29)** — 확장을 리로드하면 기존 탭의 content script(isolated world)가 무효화돼 `setCapture`를 받을 곳이 없어 탭을 새로고침하기 전까지 hover 박스가 아예 안 떴다(사용자 제보). `background.ts`: `onInstalled`(리로드 포함) 시 열려있는 유튜브/넷플릭스 탭에 `chrome.scripting.executeScript`로 content script + MAIN world 훅을 재주입하고, `sendCapture` 실패 시(캡처 켜는 경우)에도 재주입 후 1회 재시도. 이중 설치 방지: MAIN world 훅(`networkHook.ts`/`netflixNetworkHook.ts`)은 페이지 JS라 확장 리로드에도 살아남으므로 전역 플래그(`__nuanceYtHooked`/`__nuanceNfHooked`)로 재설치를 스킵(기존 훅은 postMessage 기반이라 새 content script와도 그대로 통신됨), content script는 주입 전 `sendMessage` ping으로 생존 확인해 리스너 중복을 방지.
- [x] ~~(후속) 웹페이지 DOM 텍스트 추출(태그 제외·문단 잇기) + 좌표 — 일반 웹페이지 대상, 유튜브·넷플릭스 이후.~~ → 위 "DOM 텍스트(일반 웹페이지) 직접 추출 — 담당: milleion" 항목으로 2026-07-30 구현 완료(중복 항목 정리).

<a id="공동"></a>

## 🤝 공동
- [x] ~~모드 전환 단축키 기본값 변경 — `Ctrl+1` → `Alt+Q`(macOS: Electron이 Option 키로 자동 매핑, Windows: Alt 그대로) 로 변경 완료(`selection/shortcut.ts` 34행 등 문구 갱신됨)~~
- [x] ~~단축키 macOS Cmd/Ctrl 둘 다 지원 + 해제 가능 — Electron 의 `CommandOrControl` 은 macOS 에서 Cmd 하나로만 고정 매핑돼 물리 Ctrl 키가 반응하지 않던 것을, `Control` 조합도 함께 등록해(`shortcut.ts` `expandAccelerator`) Cmd/Ctrl 둘 다 실제로 동작하게 함. 설정 화면 표시도 실제 동작에 맞춰 "Cmd/Ctrl"(그 외 OS는 "Ctrl")로 보여주고(`SettingsScreen.tsx` `formatAccelerator`), 방향키·Esc·Del 등도 흔한 약어/기호로 표시. 단축키 행이 `EditDeleteGroup`(연필=변경/휴지통=해제)으로 바뀌어 빈 문자열(해제 상태)로 설정 가능(`ipc.ts`/`updateModeShortcut` 이 빈 문자열도 처리하도록 수정)~~
- [x] ~~전역 "설정 화면 열기" 단축키(기본 `Cmd/Ctrl+,`) — `AppSettings.settingsShortcut`, `shortcut.ts` `registerSettingsShortcut`/`updateSettingsShortcut`. 모드 전환 단축키와 동일한 등록/해제/Cmd·Ctrl 이중 등록 패턴을 그대로 재사용.~~ → **동작 범위 제한 완료(2026-07-29)**: 원래는 `globalShortcut`이라 어느 앱에 포커스가 있든 무조건 반응했는데(다른 앱과 단축키가 우연히 겹치면 그 앱 사용 중 의도치 않게 설정 화면이 뜸), (1) Nuance 자신의 창(메인/설정/피커/팝업/오버레이 전부)이 포커싱돼 있을 때, (2) 선택된 대상 창이 포커싱돼 있을 때로만 실제 동작하도록 `isSettingsShortcutAllowed()`(`shortcut.ts`)로 한 번 더 거른다. (1)은 `BrowserWindow.getFocusedWindow()`로 판정 — 처음엔 `getMainWindow()?.isFocused()`만 썼는데 별도 창인 팝업이 포커싱된 동안엔 안 먹히는 버그가 있어 수정(2026-07-29 재수정). win32는 새로 추가한 `GetForegroundWindow` 바인딩(`win32Capture.ts` `isWin32WindowForeground`)으로 직접 비교하고, macOS는 창 단위 포커스 조회 API가 없어 `isMacTargetCovered`가 쓰던 z-순서 목록을 재사용해 "최상단 on-screen 창 = 사실상 포커싱된 창"으로 근사한다(`macWindow.ts` `isMacWindowFocused`). **트레이 메뉴가 떠 있는 동안 허용하는 것은 시도 후 포기(2026-07-29)** — macOS 네이티브 컨텍스트 메뉴(`Tray.popUpContextMenu`)가 떠 있는 동안은 그 메뉴의 트래킹 루프가 Electron 메인 프로세스의 JS 이벤트 루프 자체를 막아 `globalShortcut` 콜백이 그 순간엔 실행되지 않는다(실사용 확인) — OS 네이티브 드롭다운으로는 구조적으로 불가능해 요구 자체를 접었다(대안은 커스텀 BrowserWindow 드롭다운으로 통째로 바꾸는 것뿐이라 비용 대비 가치 낮다고 판단). 트레이 메뉴를 열어 "설정" 항목을 직접 클릭하는 것 자체는 이 제약과 무관하게 그대로 잘 동작한다.
- [x] ~~`npm run dev` 중 Ctrl+C 가 electron-vite 서버만 죽이고 이미 spawn된 Electron 본체(+GPU/네트워크/렌더러 헬퍼)는 못 죽여 좀비로 남는 문제(2026-07-29) — macOS 에서 Electron 이 launchd 에 재부모화(PPID=1)되면 터미널의 SIGINT가 그 프로세스 그룹에 애초에 전달되지 않는다(실측: dev 서버 종료 후에도 메인 Electron 프로세스 PPID 가 1로 남아있었음). `main/index.ts`의 `app.requestSingleInstanceLock()` 때문에 이 좀비가 남아있으면 다음 `npm run dev`가 락을 못 얻고 조용히 바로 종료돼버려("아무 창도 안 뜸"으로 보임) — `scripts/kill-nuance.sh`(프로젝트 경로로 뜬 Electron 프로세스만 매칭해 `kill -9`, VSCode/Claude Desktop 등 무관한 Electron 앱은 안 건드림)를 추가하고 `predev`에 연결해 매 `npm run dev` 시작 전 항상 정리되게 함. 필요시 `npm run kill`로 수동 실행도 가능.~~
- [x] ~~(향후) 단축키 항목 확장 — 현재 '모드 전환'/'설정 화면 열기' 단축키 2개를 지원(`AppSettings.modeShortcut`/`settingsShortcut`, `selection/shortcut.ts` 의 `currentAccelerators`/`currentSettingsAccelerators`, macOS 에선 Cmd/Ctrl 이중 등록으로 배열). 나중에 '선택창(picker) 전환'·'선택 해제' 등도 단축키로 지정하려면: (1) `AppSettings` 에 `pickerShortcut`/`deselectShortcut` 등 필드 추가, (2) `shortcut.ts` 등록 로직을 `Map<action, accelerator[]>` 로 일반화(액션별 register/unregister), (3) 각 액션 핸들러 연결(전환=picker 라우팅, 선택 해제=트레이 메뉴 동작 재사용), (4) 설정 화면 단축키 캡처 컴포넌트에 항목 추가. ⚠️ 전역 단축키(`globalShortcut`)라 상호/타앱 충돌 검증(`isRegistered()` 로 등록 실패 시 UI 안내) 필요. (A: 등록·핸들러 / B: `AppSettings` 확장·설정 UI)~~ → **구현 완료(2026-07-29, 사용자 요청)**: `AppSettings`에 `windowSelectShortcut`(창 선택/창 선택 전환 공용)·`windowDeselectShortcut`(창 선택 해제)·`manualRegionShortcut`(영역 수동 선택) 3개 추가, 트레이 메뉴 표시 순서(선택 해제/전환/수동 선택) 그대로 기본값 Opt+1/2/3(`Alt+1`/`Alt+2`/`Alt+3`, Alt는 macOS에서 Option으로 자동 매핑)을 배정(`settingsStore.ts`). 계획대로 `Map` 대신 `shortcut.ts`에 id 별 콜백 레지스트리(`registerNamedShortcut`/`updateNamedShortcut`, id: `windowSelect`/`windowDeselect`/`manualRegion`)로 일반화해 기존 `expandAccelerator`(Cmd/Ctrl 이중 등록) 로직을 그대로 재사용. 콜백(`deselectWindow`/`openWindowPicker`)이 `tray.ts`에 있어 `createTray()`에서 한 번 등록하고, `ipc.ts`의 `SETTINGS_SET` 패치 핸들러가 `updateModeShortcut`과 동일한 패턴으로 재등록. 설정 화면(`SettingsScreen.tsx`)에도 기존 두 단축키와 동일한 녹화 UI로 3개 행 추가. 추가로 트레이 메뉴에 **"모드 전환"(일반 ↔ 선택) 버튼**을 새로 노출(기존엔 단축키로만 가능, 트레이엔 없었음 — `toggleMode`를 `shortcut.ts`에서 export)하고 현재 모드를 라벨에 표시(`모드 전환 (현재: 선택 모드/일반 모드)`), 대상 창이 선택돼 있을 때만 노출(모드 개념 자체가 대상 창 기준이라). 각 메뉴 항목엔 Electron `MenuItem.accelerator`를 채워(getSettings() 값, 해제 상태면 undefined) OS가 항목 옆에 단축키를 자동으로 표시하게 함 — 트레이 컨텍스트 메뉴는 앱 메인 메뉴가 아니라 뜨는 동안만의 지역 표시라 별도로 등록해둔 전역 `globalShortcut`과 충돌하지 않음. ⚠️ 등록 실패(타 앱과 충돌) 시 UI 안내는 여전히 미구현. → **재수정(2026-07-29)**: 트레이 순서·단축키를 전환=Opt+1/해제=Opt+2로 서로 교체(원래 반대), 설정 화면 라벨도 "창 선택 / 창 선택 전환"→"창 선택 / 전환"으로 축약. → **2차 재수정(2026-07-29)**: "모드 전환" 라벨의 "(현재: 선택 모드/일반 모드)" 표시를 제거(그냥 "모드 전환")하고, 트레이·설정 화면 둘 다 순서를 모드 전환 → 창 선택 전환 → 창 선택 해제 → 영역 수동 선택 → 설정 열기로 통일.
- [x] ~~모든 창에 OS 수준 기본 우클릭 메뉴 — Electron 은 우클릭 메뉴를 자동으로 붙여주지 않아 지금까지 어느 창에서도 반응이 없던 것을, `electron-context-menu` 로 잘라내기/복사/붙여넣기·맞춤법 제안·macOS 찾아보기·서비스 메뉴까지 구성해 앱의 모든 창(메인/설정/팝업/오버레이)에 공통 적용(`main/contextMenu.ts`, `main/index.ts` `registerContextMenu()`)~~
- [x] ~~A→B 경계 재정의(팝업 기준) 반영 — A는 '팝업 직전 추출 결과(근방 텍스트 + 단어 좌표 + 클릭 기준점)'를 `ExtractedSelection` 으로 넘기고, B가 팝업에서 최종 `SelectionContext` 를 확정. 경계 인터페이스·IPC 채널 실연결 완료(`shared/types.ts`, `SELECTION_EXTRACTED`)~~
- [x] ~~`SelectionContext` / `QuestionResult` + IPC 채널 확정 (스텁 → 실연결) — `SELECTION_EXTRACTED`/`QUESTION_REQUEST`/`QUESTION_STREAM` 실동작~~
- [x] ~~메인/피커/설정 창 통합 — 세 화면이 동시에 보일 필요가 없어 별도 창(피커·설정) 대신 메인 창 하나를 리사이즈(`windows.ts: setMainWindowRoute`/`navigateMainWindow`)해 재사용하도록 변경. 전환 시 항상 창을 중앙 정렬하며, 애니메이션 없이 즉시 크기 변경(다른 창이 뜬 것처럼 보이지 않게). `ipc.ts`/`tray.ts`가 실제로 호출하고, `App.tsx`가 해시 라우트로 `SettingsScreen`/`WindowPickerScreen`을 같은 창 안에서 전환한다 — 별도 `BrowserWindow`는 더 이상 생성되지 않는다.~~
- [x] ~~IPC 허브 A→B 실연결 — 선택 파이프라인(A) → 팝업/질문(B) 이 `ipc.ts` 허브를 통해 실동작(오버레이 클릭 → 팝업 오픈 → 질문 스트리밍)~~
- [ ] 첫 관통 경로: PDF 직접추출 → 통합 질문 — **선택모드 OCR 관통은 Windows·macOS 둘 다 됨**(mac 은 아래 OCR 항목 참고). 남은 건: **직접추출 경로**(`readWindowText` 접근성 API 는 win32 전용, mac 미구현) + **PDF 직접추출 파서 자체 미구현** → 현재는 양 플랫폼 모두 OCR 로만 텍스트를 얻는다. PDF 직접추출을 붙이면 관통 완성.
- [x] ~~배포 패키징(electron-builder) — `electron-builder.yml`(appId `com.nuance`·productName `Nuance` 고정 → userData 경로 안정, 재설치/버전 업 후에도 설정·API 키·자주쓰는질문 유지). scripts: `pack:dir`(스모크) / `dist:mac`·`dist:win`·`dist:linux`. mac `--dir` 패키징 성공 확인(코드서명 없음: `identity: null`). 산출물은 `dist/`(gitignore)~~
  - [ ] 정식 배포 시 코드서명/공증 — mac: hardenedRuntime+notarize(Apple Developer 인증서), win: 서명 인증서. 현재는 사설 배포(미서명)라 Gatekeeper/SmartScreen 경고가 뜸
  - [ ] mac entitlements 파일 추가 — 화면 기록(`desktopCapturer`/`screencapture`) 권한을 쓰는데 `.entitlements` 파일 자체가 없음. 지금은 미서명 상태라 TCC가 서명 여부와 무관하게 권한 다이얼로그를 띄워줘서 우연히 동작하지만, hardened runtime 전환 시 선언 안 된 권한 호출이 조용히 거부/크래시할 수 있어 코드서명/공증과 함께 반드시 추가 필요
  - [ ] 앱 아이콘 교체 — 현재 `build/icon.png` 는 기존 256px 을 1024 로 업스케일한 임시본(정식 아이콘으로 교체 필요)
- [x] ~~`npm install` + `electron-vite dev` 빌드 정상화~~
  - [x] ~~koffi를 optionalDependencies로 이동 — 맥에서 네이티브 빌드 실패해도 install 유지 (`fix: 969d08f`)~~
- [ ] 크로스플랫폼(Win / Mac) 동작 점검
  - [x] ~~맥(arm64) 부팅 확인 — win32Capture(koffi)를 동적 import로 격리해 맥에선 미로드, `npm run dev` 정상 기동 (`fix: 969d08f`). 맥은 desktopCapturer 경로 사용(창 목록 실사용엔 화면 기록 권한 필요)~~
  - [ ] Windows 재확인 — win32Capture가 static→동적 import로 바뀌어 Windows 창 목록/캡처 정상 동작 재점검 필요
  - [x] ~~(담당 A) 선택 창 테두리 오버레이 macOS 대응 — `ipc.ts SELECT_WINDOW` 의 비-win32 분기가 `showMacSelectionOverlay(windowId)`(`windows.ts`)를 호출. CoreGraphics `CGWindowListCopyWindowInfo`(koffi, `selection/macWindow.ts`)로 bounds 를 얻어 테두리를 정렬하고 16ms 폴링(`MAC_TRACK_INTERVAL_MS`)으로 이동/리사이즈 추적, owner PID 로 `NSRunningApplication` activate(창 맨 앞으로). Accessibility/자동화 권한 불필요~~ → **(2026-07-29) "같은 PID 안 여러 창"(크롬 개발자도구 별도 창) 케이스 보완 시도 → 결국 되돌림(2026-07-30)**: `activateApp`(PID 전체 activate)만으로는 devtools 창이 앞으로 오는 문제가 있어 Accessibility(AX) API로 그 PID의 특정 창만 `AXRaise`+`AXFocusedWindow` 하는 `raiseSpecificWindow`를 추가하고, 권한 요청 다이얼로그 명시 트리거·프롬프트 발동 조건 축소(창 2개 이상일 때만)까지 이어 붙였으나, 손쉬운 사용 권한을 요청해도 시스템 설정 목록에 앱 자체가 추가되지 않아 사용자가 권한을 부여할 방법이 없었다(실사용 확인) — 근본적으로 동작하지 않는 접근이라 AX 관련 코드 전체(`ensureAX`/`raiseSpecificWindow`/`countOnScreenWindowsOfPid` 등)를 삭제하고 `activateApp`(PID 전체 activate)만 쓰는 원래 방식으로 되돌림. devtools 분리창일 때 어느 창이 앞으로 올지는 다시 macOS 판단에 맡긴다(미해결로 남김).
    - [ ] mac 오버레이 정밀화(선택) — win32 는 즉시 반응 훅(WinEventHook)까지 있는 반면 mac 은 순수 폴링(16ms)뿐이라 이론상 프레임 사이 지연이 있을 수 있음(폴링 주기 자체는 짧음). 멀티 디스플레이/스케일 좌표 정확도, 대상 창이 다른 창에 가려질 때 z-order 처리(현재 `setAlwaysOnTop(true, 'floating')`)는 실사용 점검 필요.
- [x] ~~언어 확장성 — 현재 영/일/중만 지원(PLAN.md §1), 추후 언어 추가를 대비한 구조.~~
  - [x] ~~`@shared/languages.ts`에 언어별 정적 데이터(이름, 구글 검색 접미어 등) 단일 레지스트리 도입. `Language` 유니온에 언어를 추가하면 `Record<Language, ...>` 사용처가 컴파일 에러로 누락을 알려줌(`question/llm/adapter.ts`, `question/google.ts` 적용 완료)~~
  - [x] ~~(담당 A) OCR 언어 감지/언어팩을 이 레지스트리와 연동하거나 별도 레지스트리로 통일 (`selection/langDetect.ts`, `selection/ocr.ts`)~~
  - [x] ~~(담당 B) 설정 화면 언어 선택지, 언어별 사전 API 소스, 발음 표기 체계(IPA/히라가나/병음 등 — 언어마다 표기 체계 자체가 다름, 데이터 하나로 단순 확장 안 됨) 설계 시 레지스트리 패턴 반영~~
  - **3단계 언어 등급제로 실제 구현 완료(2026-07-30, `feat/language-tiers`)** — "OCR/사전은 en/ja/zh만, 나머지는 감지+구글/네이버 연결만"으로 스코프를 정하고 진행:
    - **tier 정의**: tier1(완전지원, 기존 4개, `Language` 타입 유지) / tier2-A(구글+네이버+OCR+AI발음, eld 감지 ∩ 네이버 실지원 30개) / tier2-B(구글+OCR+AI발음만, 네이버 없음 26개) / tier3(그 외 전부, 아무 기능 없음 — eld가 아예 못 잡는 언어 + "발음"에 대응하는 신뢰 가능한 번역을 못 찾은 요루바어). tier2 전체 목록·근거는 `shared/types.ts`의 `LinkLanguage` 유니온 주석 참고.
    - **네이버 사전 URL 패턴 재조사**: 기존 tier1이 쓰던 `{code}.dict.naver.com` 서브도메인 방식은 en/ja/zh/ko 등 소수 언어 전용 레거시였고, 실제 다국어 사전은 `dict.naver.com/{code}kodict/#/search?query=` 경로 방식이라는 걸 네이버 실제 프로덕션 JS 라우터 번들(`en.dict.naver.com`이 로드하는 main.js)을 직접 받아 라우트 목록을 추출해 확인(2026-07-30) — 서드파티 워크플로우 소스로 추정했던 목록보다 실제 지원 언어가 더 많았음(덴마크어/히브리어/라오어/노르웨이어/타갈로그어/우르두어 등 6개 추가 확인).
    - **tier2 정적 데이터 소스**: 구글 발음 검색 접미어("발음"에 해당하는 단어)는 전부 Wiktionary "pronunciation" 번역 테이블 + 개별 언어 사전(Cambridge 등)으로 언어별 검증 — 단순히 영어 "pronunciation"을 그대로 안 쓴 이유는, 검색어 전체가 영어처럼 보이면 라틴 문자 겹치는 단어(철자가 우연히 영어와 같은 경우)에서 구글이 언어를 잘못 추정할 위험이 있어서(사용자 지적, 2026-07-30) — ja/zh가 애초에 読み方/拼音을 쓰는 것과 같은 이유.
    - **발음 표기 체계**: tier2는 전부 IPA로 통일(러시아어 강세표시 키릴, 태국어 RTGS+성조 같은 언어별 자연스러운 대안이 있지만 검증 비용 때문에 보류 — 실사용 후 필요해지면 언어별로 교체 가능하도록 `LinkLanguageInfo.pronunciationNotation`에 필드는 이미 있음). "AI 발음"은 LLM 프롬프트만으로 되는 기능이라 tier1/tier2 구분 없이 항상 지원 — "AI 사전"(LLM+사전 API 폴백체인)만 tier1 전용으로 남음.
    - **OCR 확장**: Tesseract 언어팩 용량 실측(jsdelivr `@tesseract.js-data` CDN, `_best_int` 양자화 모델) — tier2 56개 언어 합계 약 96MB, 언어당 평균 1.7MB(온디맨드 로딩이라 실제 디스크 사용량은 사용자가 실제로 보는 언어 1~2개뿐). 타갈로그어(tl)만 양자화 팩이 없어서(`tgl` 404) 사실상 표준 타갈로그어인 `fil`(필리핀어) 팩으로 대체.
    - **OCR 스크립트 모호성 해소**: Tesseract OSD(Orientation & Script Detection)는 "문자 체계"만 알려주고 "어느 언어"인지는 모른다 — 라틴/키릴/아랍/데바나가리 문자는 tier2에서 여러 언어가 공유(예: 라틴 문자 20개 이상). `langDetect.ts`의 `resolveAmbiguousScript()`가 그 스크립트의 대표 언어팩으로 1차 러프 OCR을 돌린 뒤, 자막 판별과 같은 eld 텍스트 판별기(`detectRawLanguage`)로 실제 언어를 재판정하는 2패스 방식 도입 — 기존 zh-Hans/zh-Hant를 표본 문자로 가르던 패턴("1차 대충 인식 → 재판별")을 일반화.
    - **타입 설계**: `Language`(tier1, 4개, 닫힌 유니온, LLM 사전 등 tier1 전용 기능이 계속 씀) + `LinkLanguage`(tier2, 56개) + `AnyLanguage = Language | LinkLanguage`(감지/선택 결과를 실어 나르는 자리 전부, `ExtractedSelection.language`/`SelectionContext.language` 등) 세 단계로 분리 — tier1 전용 로직(`question/dictionary.ts`)은 진입점에서 `isFullLanguage()`로 한 번 좁혀 `DictSelectionContext`(`language: Language`)로 캐스팅한 뒤 그 아래 모든 헬퍼가 좁은 타입만 다루게 해서, 사전 없는 언어가 실수로 사전 로직을 타는 걸 컴파일 타임에 막는다. tier2를 tier1로 승격하려면 `LinkLanguage`에서 빼서 `Language`로 옮기고 `LINK_LANGUAGES` 항목을 `LANGUAGES`로 그대로 옮기면 되도록(필드 구조 동일) 설계해둠.
    - **판별 결과 폴백 제거**: 예전 `detectLanguage()`가 tier1 외 언어를 전부 'en'으로 조용히 폴백하던 것(2026-07-29 넷플릭스 간체/번체 버그와 같은 유형의 함정)을 `detectSupportedLanguage()`로 교체해 tier3면 `null`을 반환하도록 명시화 — 자막 클릭 경로(`subtitleSource.ts`)는 null이면 팝업 대신 Electron 기본 `Notification`으로 "이 언어는 아직 지원하지 않습니다" 토스트만 띄운다(사용자 결정 — "짧은 토스트만").
    - **UI 분기**: 팝업 툴바(`Toolbar.tsx`)에 `showNaverDict`/`showAiDictionary` prop 추가 — tier1은 전부 노출, tier2-A는 네이버는 보이고 AI 사전만 숨김, tier2-B는 둘 다 숨김(AI 발음/구글은 항상 노출). 팝업 헤더 언어 라벨은 `PopupScreen.tsx`가 자체 관리하던 `LANGUAGE_LABEL`(tier1 4개만 하드코딩된 중복 레지스트리)을 지우고 `shared/languages.ts`의 `getNativeLanguageName()`(원어 표기, 신규 필드) 하나로 통합.
    - **태국어/라오어(공백 없는 스크립트) 특별 처리**: eld 60개 언어 중 CJK를 빼면 이 둘만 단어 사이 공백이 없다 — 형태소 분석기가 없는 tier2라 정확한 단어 경계를 낼 방법이 없으므로, 확장 hover 박스(`extension/src/domWords.ts`)는 줄 전체를 한 덩어리로 보여주고(`THAI_LAO_CHAR_RE`), 팝업이 열린 뒤 내부 선택(`popup/selection.ts` `NO_WORD_BOUNDARY_LANGUAGES`)은 토글 없이 항상 글자 단위로 강제 — "호버는 줄 단위, 팝업 내부는 글자 단위"라는 두 레이어가 서로 다른 이유(사용자 결정, 2026-07-30).
    - **`extractDirect.ts`(접근성 텍스트 직접 추출, 아직 다른 곳에서 호출 안 되는 경로) 부수 수정**: `language`가 항상 `'en'`으로 하드코딩돼 있던 걸 `detectSupportedLanguage()`로 실제 판별하도록 고침 — 같은 유형의 폴백 버그 재발 방지 차원.
  - **머지 후 셀프 리뷰(2026-07-30)로 발견한 미흡점 4가지, `feat/language-followups`에서 후속 수정**:
    - **RTL(아랍어/페르시아어/히브리어/우르두어) 팝업 레이아웃**: `ContextView.tsx`/`styles.css` 어디에도 `dir` 처리가 없어 팝업 본문이 항상 LTR 기준으로 렌더링되던 문제. `shared/languages.ts`에 `isRtlLanguage()` 추가하고 `PopupScreen.tsx`가 `ContextView`에 `dir="rtl"|"ltr"`을 넘기도록 배선 — `.ctx-text`는 원래도 `text-align: justify`라 우측정렬이 아니라 양쪽정렬로 자연히 보인다(추가 CSS 불요). 드래그 선택(`atomIndexAtPoint`가 `document.caretRangeFromPoint` 기반, 브라우저 네이티브 bidi 인식)과 LLM 문맥 전달(atom index/오프셋이 전부 논리 순서 기준, JS 문자열은 원래 논리 순서 보존)은 조사 결과 이미 구조적으로 안전해서 별도 수정이 필요 없었음 — 순수 레이아웃(문단 시작 방향) 문제였음.
    - **설정의 수동 언어 선택이 판별 로직에 미연동**: `AppSettings.language`가 있었지만 OCR(`langDetect.ts`)·자막(`bridge.ts`/`subtitleSource.ts`)·웹페이지(`webSource.ts`) 판별 어디서도 참조되지 않아 항상 자동판별만 하던 문제. `settingsStore.ts`에 `getLanguageOverride()`(`'auto'`가 아니면 그 값, 아니면 `null`)를 추가하고 네 지점 전부에서 자동판별보다 우선 적용하도록 배선 — `langDetect.ts`는 override가 있으면 OSD/2차 인식 자체를 생략해 비용도 아낀다. 라틴/키릴/아랍/데바나가리처럼 여러 tier2 언어가 스크립트를 공유해 자동판별이 흔들릴 수 있는 경우, 사용자가 이걸로 확정할 수 있게 됨.
    - **설정 화면 언어 선택 UI가 tier1 4개만 노출**: `LANGUAGE_ORDER`(tier1) 기반 pill 버튼을 `<select>`+`<optgroup>` 드롭다운으로 교체해 tier1/tier2-A/tier2-B 60개 전체를 노출(그룹 안은 한국어 이름 가나다순 정렬). optgroup 라벨이 브라우저 기본 렌더링으로 굵게+들여쓰기 구분되어 별도 구분선 CSS 없이 티어 경계가 드러남. "자동"과 "언어 선택" 각각이 실제로 뭘 다르게 하는지(자동판별을 건너뛰는지 여부)와 tier별 지원 범위(1단계=OCR+사전+언어별 발음표기, 2단계=OCR+IPA발음+구글, 그중 일부만 네이버)를 설명하는 문구 추가.
    - **태국어/라오어 hover/OCR이 "줄" 아닌 "문단"·"단어" 단위였던 문제**: 처음 구현한 `domWords.ts`의 "텍스트 노드 전체=한 단어" 규칙이 자막(원래 한 노드=한 줄)에는 맞았지만, 이후 병합된 일반 웹페이지 본문 추출(`webSource.ts`/`webArticle.ts`, 한 `<p>` 노드가 여러 줄로 줄바꿈됨)에도 재사용되면서 문단 전체가 한 덩어리로 뭉치는 문제가 있었음 — `splitTextNodeIntoVisualLines()`로 실제 화면상 줄바꿈 지점(글자별 세로좌표 변화)을 찾아 진짜 줄 단위로 쪼개도록 수정. OCR 경로(`ocr.ts`)의 태국어/라오어도 원래 Tesseract 단어 단위 그대로였던 걸, 세로쓰기 일본어(NDLOCR)에 쓰던 것과 같은 `lineId` 메커니즘(`wordMapping.ts` `findLineWordsAtPoint`가 자동으로 lineId 기준 그룹핑)을 재사용해 줄 단위 hover로 통일.
  - **OCR 자동판별 ↔ eld 경로 불일치 전수조사 및 수정(2026-07-30, `fix/lang-detect-is-ku`)** — "OCR 자동판별이 eld랑 다른 경로 아니냐, eld가 지원하는 tier2 언어를 OCR도 전부 감지하냐"는 질문을 계기로 확인:
    - **경로 차이 확인**: 자막/DOM 텍스트 경로는 `detectSupportedLanguage()`로 eld를 항상 직접 호출하지만, OCR 경로(`langDetect.ts`)는 Tesseract OSD로 스크립트(문자 체계)를 먼저 판별하고, 그 스크립트를 언어 하나가 전유하면 eld 호출 없이 끝난다 — 라틴/키릴/아랍/데바나가리처럼 여러 언어가 스크립트를 공유할 때만 `resolveAmbiguousScript()`가 대표 언어팩으로 초벌 OCR한 텍스트를 eld에 2차로 넘긴다.
    - **전수조사 방법**: eld README(`node_modules/eld/README.md`)가 스크립트를 명시한 4개 언어(Azerbaijani=Latin, Kurdish=Arabic, Malay=Latin, Serbian=Cyrillic)와, 나머지 tier2 56개 언어를 `ocrLang`(Tesseract 팩 스크립트) 기준으로 `SCRIPT_TO_LANGUAGE`의 Latin/Cyrillic/Arabic/Devanagari 후보 배열·1:1 스크립트 매핑과 전부 대조.
    - **발견 1 — `is`(아이슬란드어) 단순 누락 버그**: eld 모델도 Tesseract 팩(`isl`)도 둘 다 라틴 문자인데 `SCRIPT_TO_LANGUAGE.Latin` 배열에서만 빠져 있어 OCR 경로가 항상 `en`으로 오판 — 배열에 추가해 수정(`langDetect.ts`).
    - **발견 2 — `ku`(쿠르드어) 구조적 불일치, tier3로 되돌림**: eld의 `ku` 모델은 소라니 방언(아랍 문자)인데 이 앱이 등록해둔 Tesseract 팩은 쿠르만지 방언(`kmr`, 라틴 문자) — 같은 코드가 OCR과 eld에서 서로 다른 방언/문자체계를 가리켜, 후보 배열에 추가해도(라틴이든 아랍이든) eld가 그 스크립트에서 `ku`를 반환할 일이 없어 원리적으로 해결이 안 됨. 자막 경로(eld 직접 호출)는 원리상 소라니 아랍 문자 텍스트를 감지할 수 있지만, OCR은 그 문자용 팩 자체가 없어 두 경로 결과가 갈리는 비일관성이 생기므로 `LANGUAGES`/`LINK_LANGUAGES`/`LinkLanguage` 유니온에서 통째로 제거해 tier3(미지원)로 통일. tier2 새 카테고리(예: "2-C") 신설은 검토했으나 영향 언어가 1개뿐이고 축(사전 지원 범위 vs 자동판별 가능 여부)이 달라 기각 — 나중에 같은 유형 사례가 더 쌓이면 재검토.
    - **부수 정정**: tier2-B 26→25개, tier2 합계 56→55개, 총 지원 언어 60→59개로 문서·주석 갱신(`PLAN.md`, `styles.css`, `ocr.ts`, `types.ts`). 설정 화면(`SettingsScreen.tsx`)의 tier2 드롭다운은 `LINK_LANGUAGES` 레지스트리를 동적으로 순회해 그리므로 코드 수정 없이 자동 반영됨.
- [x] ~~담당 A/B 각자 구현이 같은 로직을 중복해 들고 있던 사례 전수조사(2026-07-29) 후 4건 공유 모듈로 통합~~ — (1) 팝업 원문 atom 분해(`popup/selection.ts`)와 확장 hover 박스 단어 추출(`extension/src/domWords.ts`)이 각자 다른 정규식으로 "단어=문장부호 제외" 규칙을 구현하던 것을 `shared/wordTokenize.ts`(`WORD_ATOM_PATTERN`/`HOVER_WORD_ATOM_PATTERN` — 하이픈 처리만 팝업/hover 박스가 의도적으로 다르므로 패턴 두 개로 분리, 모듈 주석 참고)로 통합. (2) 화면 자막 텍스트로 ja/zh-Hans/zh-Hant 를 추정하는 휴리스틱이 `bridge.ts`(`detectCjkLine`, 순환참조 우려로 일부러 따로 두고 있었음)와 `subtitleSource.ts`(`detectSubtitleLanguage`)에 간체자 판별 문자 세트까지 바이트 단위로 동일하게 있던 것을 `shared/cjkDetect.ts`(`detectCjkLanguage`)로 통합 — 공유 모듈로 뽑으니 우려하던 순환참조 자체가 안 생김. (3) `HAS_HAN_CHAR_RE`(한자 포함 여부 판정)가 `nlp/engines/chineseTokenizer.ts`/`intl-zh.ts`/`jieba.ts` 세 파일에 완전히 동일하게 있던 것도 같은 `cjkDetect.ts`의 `HAN_CHAR_RE`로 통합. (4) 사각형 여러 개의 합집합(bbox union) 계산이 `wordMapping.ts`(`unionBbox`, `Word[]` 대상)·확장 `highlight.ts`(`unionRects`, `RectPx[]` 대상)·`domWords.ts`(`rangeRect` 내부, `DOMRect[]` 대상) 세 곳에 입력 타입만 다르고 로직은 동일하게 중복돼 있던 것을, `DOMRect`/`RectPx`/`Word.bbox` 모두 `{x,y,width,height}` 셰이프를 만족한다는 점을 이용해 `wordMapping.ts`의 저수준 `unionRects({x,y,width,height}[])` 하나로 통합(`unionBbox`는 이제 이 위에 얹은 얇은 래퍼).
- [x] ~~넷플릭스 자막을 간체로 설정했는데 팝업엔 번체(`中文(繁體)`)로 뜨는 버그 — 실사용 제보(2026-07-29)~~ → 원인은 위 (2)에서 통합한 `cjkDetect.ts`의 `SIMPLIFIED_ONLY_CHAR_RE`가 간체 전용 글자 **17자**만 검사하고, 없으면 무조건 zh-Hant로 폴백하는 편향 구조였음(간체 자막이어도 그 17자가 한 줄에 안 걸리면 번체로 오판). 조사 과정에서 이 판별이 세 곳(`cjkDetect.ts` 17자 / `content.ts` `detectDomLanguagePrefix` zh 세분화 없음 / `langDetect.ts` OCR 경로 90자)에 서로 다른 표본으로 파편화돼 있던 것도 함께 확인됨. **`feat/language-expansion` 브랜치에서 근본 수정**:
  - **1단계(대분류 언어 판별)**: franc/eld/tinyld 실측 비교(자체 작성한 다국어 샘플, 짧은 자막 한 줄 vs 전체 트랜스크립트 두 길이로 측정) — franc는 짧은 텍스트에서 38.1%(최소 신뢰 길이 미달 시 `und` 응답 다발)로 탈락, eld/tinyld는 길이 무관 100%였고 그중 eld가 속도상 근소 우위(large DB 기준 0.033ms/short)라 채택. 번들 크기 위해 `eld/extrasmall`(~950KB) 사용 — 정확도 손실 없음을 실측 확인.
  - **2단계(zh-Hans/zh-Hant 세분화)**: 기존 수작업 17자/90자 표본을 OpenCC(BYVoid/OpenCC, MIT)의 STCharacters/TSCharacters 단일 문자 매핑 데이터에서 뽑은 간체전용 3875자/번체전용 3222자 표본으로 교체(`shared/hanziVariants.ts`, `scripts/gen-hanzi-variants.mjs`로 재생성 가능 — opencc-js는 런타임 의존성으로 안 두고 데이터 추출에만 씀).
  - 판별 로직을 `shared/cjkDetect.ts` → `shared/languageDetect.ts`로 대체 통합(`detectLanguage`/`detectRawLanguage`/`detectCjkVariant`/`detectHanziVariant`, `HAN_CHAR_RE`도 여기로 이전) — `bridge.ts`/`subtitleSource.ts`/`nlp/engines/*`(main) 와 `extension/src/content.ts`(확장, `@shared` esbuild alias로 동일 소스 재사용) 전부 이걸로 교체. `langDetect.ts`(OCR 경로)의 `detectChineseScript`도 자체 문자셋 대신 `detectHanziVariant`를 호출하도록 정리.
  - **판별 텍스트 범위 확대 + 캐시**: 기존엔 클릭 시점의 화면 자막 "한 줄"(`hit.lineText`)만 판별에 썼는데, 실제로는 `bridge.ts`의 `transcripts` Map에 영상 전체 자막이 이미 캐시돼 있었음에도 안 쓰고 있었음(2026-07-29 지적) — 이제 `transcript` 메시지 수신 시 전체 cue를 이어붙인 텍스트로 1회만 판별해 `Transcript.language`에 캐시하고, `subtitleSource.ts`는 클릭마다 재판별하지 않고 이 캐시값을 그대로 쓴다(표본이 커져 정확도도 오르고, 반복 판별 비용도 없어짐).
  - **트랙 선택 단계도 함께 수정**: `content.ts`의 `pickNetflixTrack`이 넷플릭스 후보 트랙 중 `zh` 접두사만 보고 아무거나 첫 매치를 고르던 것을, `detectDomLanguagePrefix`가 이제 zh-Hans/zh-Hant까지 세분화해 반환하므로 그 값으로 먼저 매칭하고 (넷플릭스가 트랙을 zh 하나로만 내려주는 경우에 대비해) zh 통짜 매칭으로 폴백하도록 변경 — 표시 라벨만이 아니라 애초에 받아오는 트랙 자체가 틀릴 수 있던 근본 원인.

<a id="미해결-문제"></a>

<a id="hidden-ui"></a>

## 🙈 코드는 살려두고 UI에서만 감춘 것

개발 중에 쓰던 보조 기능들 — **지우지 않고 화면에만 안 뜨게** 해뒀다(2026-07-31 사용자 요청).
나중에 다시 필요하면 아래 적힌 한 곳만 되돌리면 그대로 살아난다.

- [ ] **팝업 미리보기(데모) 버튼** — 메인 화면 개편(2026-07-31) 때 UI에서만 뺐다. 목업 데이터
  (`src/renderer/src/screens/popup/mockSelection.ts`, 영어/영어 동음이의어/일본어/중국어 간체·번체
  5종)와 이를 소비하는 경로(`IPC.OPEN_POPUP`의 `demo` 인자, preload `openPopup(demo?)`,
  `PopupScreen.tsx`의 demo 쿼리 분기)는 **전부 그대로 살아 있다**. 되살리려면
  `MainScreen.tsx`에 `window.nuance.openPopup('hobbit')` 같은 버튼만 다시 넣으면 된다
  (`#/popup?demo=hobbit` 형태로도 직접 열 수 있다).
- [ ] **사전 답변 맨 윗줄 `[DEBUG] entries/readings/senses/대응어` 표시** — 채팅창에 노출되던
  진단 줄을 껐다. `src/main/question/dictionary.ts`의 `SHOW_DEBUG_COUNTS_LINE = false` 상수
  하나로만 제어하며, 줄을 조립하는 `withDebugCountsLine()`과 그 호출부들은 그대로 둔 상태다
  — 다시 보려면 이 상수를 `true`로 바꾸기만 하면 된다. (원래 목적: LLM이 "대응어:" 줄을 아예
  안 준 건지, 줬는데 로컬 길이 비율 필터가 걸러낸 건지 구분하기 위함 — senseSelect.ts 참고.)

## ⚠️ 미해결 문제

- [ ] (담당 B) LLM 비용 고려사항 — 현재 `streamLlm()`이 `history` 전체를 매 요청마다 잘라내기/요약 없이 재전송함(`llm/adapter.ts`). 팝업 세션이 길어질수록 요청당 input 토큰이 선형 증가.
  - [ ] 대화 history 길이 제한 정책 결정 (예: 최근 N턴만 유지, 또는 초과 시 요약으로 압축)
  - [ ] 프롬프트 캐싱을 `cacheableContext`(선택 근방 문맥) 외에 대화 history에도 적용할지 검토 — 현재는 Claude의 `cache_control: ephemeral`도 문맥 블록에만 적용, history 메시지 배열은 캐싱 대상 아님
  - [ ] GPT/Gemini에도 캐싱 연동 검토 — 현재 캐시 제어는 Claude 클라이언트에만 구현됨. GPT는 prefix 자동 캐싱(별도 API 불필요, prefix 안정성 필요), Gemini는 명시적 context caching API(`cachedContents`) 필요
  - [ ] provider별 요청당 비용 추정치 산정 + 설정 화면에 예상 비용/사용량 노출 여부 결정
  - [ ] 팝업 내 선택 범위 재수정 시 문맥 캐시 재사용 — 현재는 선택 근방 문맥(`cacheableContext`)을 한 번에 넘겨 캐싱하는데, 같은 팝업에서 선택 범위를 나중에 바꾸면 LLM에 필요한 근방 문맥이 살짝 달라짐(대부분 겹치고 경계만 약간 이동). 매번 전체를 새로 보내면 낭비가 크므로, 겹치는 prefix 재사용/증분 전송 등으로 캐시 적중을 유지하는 방안 검토(예: 문맥 블록을 넉넉히 한 번 캐싱해두고 선택 마커만 갱신)
- [ ] (담당 B) 문맥 범위 '바이트 예산'의 언어별 형평성 검토 — 같은 의미의 글도 언어마다 바이트가 다름(실측: 세계인권선언 1조 = 영 170B / 일 252B / 중 129B). CJK는 1자=3바이트지만 훨씬 압축적(영 170자 vs 중 43자)이라 두 효과가 부분 상쇄되나 정확히 같진 않음 → 같은 예산이면 중국어가 가장 많이·일본어가 가장 적게 담김. 진짜 정밀한 문맥/비용 통제가 필요하면 바이트 대신 LLM 토큰 기준 예산으로 전환 검토. (설정: `AppSettings.contextBytesBefore/After`, `@shared/context.ts`)
- [ ] (담당 A) 선택 창 테두리 오버레이 — 대상 창과의 실시간 동기화 지연. 대상 창을 드래그로 이동/리사이즈할 때 오버레이가 못 따라가는 프레임이 간헐적으로 있음(어떤 경우엔 창 크기 변화가 먼저 반영되고 테두리가 뒤따라옴, 어떤 경우엔 반대). `WinEventHook`(`EVENT_OBJECT_LOCATIONCHANGE`, `win32Capture.ts`)으로 즉시 반응 + 150ms 폴링 안전망까지 적용했지만, 오버레이 창과 대상 창이 서로 다른 프로세스의 독립된 최상위 창이라 DWM 이 두 창을 같은 컴포지터 프레임에 맞춰준다는 보장이 없어 완전한 동기화는 구조적으로 어려움. 더 근본적인 해결(예: 대상 창을 자식 창으로 편입 등)이 필요하면 위험도·부작용을 먼저 검토할 것.
- [ ] (담당 A) 창 선택 목록 — 최소화된 창 썸네일 캡처 시 화면 구석에 짧은 깜빡임. `captureMinimizedWin32Window`(`win32Capture.ts`)가 DWM Thumbnail 합성 결과를 읽으려고 화면 우하단에 실제로(약 0.1~0.2초) 작은 캡처용 창을 띄웠다가 지운다 — 그 순간이 눈에 보임. `PrintWindow`/화면 밖 위치로는 DWM 합성 결과를 못 읽어서(이 시스템에서 재현·검증됨) 어쩔 수 없이 화면 안에 실제로 띄워야 했음. 완전히 없애려면 접근 방식 자체를 바꿔야 함(예: 최소화된 창은 실시간 썸네일 대신 아이콘+라벨로 표시, 또는 가상 데스크톱 활용 등) — 사용자와 논의 후 보류 중.
- [x] **(2026-07-29) 창 선택 화면(`WindowPickerScreen.tsx`)에서 hover 테두리와 이미 선택된 창 테두리 색 구분** — 둘 다 `--theme`(보라)를 써서 새로 고르려는 창에 커서만 올려도 이미 선택된 창처럼 보였다(사용자 피드백). hover는 `--blue`(오버레이 일반 모드와 같은 파랑)로 바꾸고 `.selected`(보라)는 그대로 유지 — `.selected`가 CSS 소스 순서상 `:hover`보다 뒤에 있어 명세가 같아도 이겨서, 이미 선택된 타일에 커서를 올려도 보라색 그대로 유지된다.
- [ ] (담당 A) Windows — 다른 가상 데스크탑(Virtual Desktop)의 창 선택 지원 여부 미확인. `listWin32Windows`(`win32Capture.ts`)의 `EnumWindows` 자체는 가상 데스크탑과 무관하게 모든 최상위 창을 순회하지만, 열거 시 쓰는 `isCloaked()`(`DWMWA_CLOAKED`) 필터가 다른 가상 데스크탑에 있는 창까지 걸러내고 있는지 실기(Windows, 다중 가상 데스크탑 환경)로 확인 안 됨. 걸러진다면 `IsWindowOnCurrentVirtualDesktop`을 함께 확인해 걸러내는 대신 목록에 포함시키고, 캡처(`PrintWindow`/DWM Thumbnail)가 다른 가상 데스크탑 창에서도 실제로 되는지도 별도 검증 필요(COM `IVirtualDesktopManager` 바인딩 추가가 필요할 수 있음). macOS는 다른 Space 창까지 보여주는 시도를 했다가 UX 문제로 원복했음(위 "앱·윈도우·모드" 항목 참고) — 다시 시도할 경우 Space별 탭 구분(비공개 SkyLight `CGSCopySpacesForWindows`, 신뢰도 낮아 보류했었음)까지 함께 재검토할 것.
- [ ] (담당 A) NDLOCR-Lite 세로쓰기 인식 — 縦中横 숫자·"……" 등이 스페이스 마커도 없이 조용히 빠지는 "완전 침묵" 케이스를 여러 방향으로 시도했으나 못 잡음(2026-07-29, 페이지 전체 실측으로 확정) — 상세 근거는 위 "OCR 엔진 연동 → 언어별 특화 엔진" 항목 참고. 시도했다가 효과 없어서 되돌린 것들: (1) 줄 자신의 bbox 안 잉크 픽셀 합산을 기대치와 비교(미검출/과다인식 양방향) — 신호가 40자 안에 묻혀 소수 글자 차이는 감지 안 됨. (2) 줄 끝 반복 문자(예: "………")의 마지막 칸 국소 잉크 검사 — 효과 제한적이고 판단 근거 자체가 case-by-case. (3) PaddleOCR 검출 전용 API로 NDLOCR bbox 밖에 내용이 더 있는지 확인 — 페이지 17줄 전부에서 PaddleOCR 독자 경계가 NDLOCR bbox 를 넘어선 줄이 하나도 없어서(NDLOCR bbox 가 이미 정확/넉넉), 이 접근 자체가 원리적으로 이 종류의 누락엔 신호를 못 준다는 게 확인됨. 남은 선택지는 의심 여부와 무관하게 다른 엔진(Yomitoku 등)으로 상시 재대조하는 것 정도인데 속도 트레이드오프가 있어 보류 중.
- [x] ~~(담당 B) 새로고침 없이 선택 모드에 들어가거나 유튜브↔넷플릭스 탭을 오가면 팝업에 앞뒤 문맥 없이 현재 줄만 뜸(2026-07-29 사용자 제보, 여러 차례 재조사 끝에 해결)~~ — **최종 원인은 세 가지가 겹쳐 있었다**: (1) 넷플릭스 매니페스트 필드명이 `timedtexttracks`/`ttDownloadables`→`textTracks`/`downloadables`로 바뀌어 옛 이름만 보던 `JSON.parse` 후킹(`netflixNetworkHook.ts`)이 매니페스트를 한 번도 못 잡음 — Chrome에 설치된 Language Reactor 확장(`pageScript_lln.min.js`)을 디컴파일해 신·구 필드명을 모두 받는 정규화 로직으로 확정 수정. (2) 유튜브 훅(`networkHook.ts`)은 넷플릭스 훅과 달리 마지막 캡션 응답을 캐시해두지 않아, 영상 로드 극초반(선택 모드 진입보다 훨씬 전)에 한 번만 오는 네트워크 응답을 리스너가 준비되기 전에 놓치면 영영 유실됐다 — `lastCaption` 캐시 + `requestLastCaption` 재전송(넷플릭스와 동일 패턴), 캐시조차 없으면 자막 트랙을 껐다 켜 새로고침과 같은 효과로 timedtext 요청을 강제 재발생시키는 `forceReloadCaptions()`까지 추가. (3) **가장 근본적인 원인**: 앱의 전체 자막 캐시(`bridge.ts`)가 전역 슬롯 하나(`lastTranscript`)뿐이라 유튜브↔넷플릭스 탭을 오가면 서로 자막을 덮어썼는데, 확장 쪽 dedup 캐시("이미 앱에 보냈으니 재전송 스킵")는 이 덮어쓰기를 모른 채 재전송을 계속 거부해 탭을 되돌아오면 구조적으로 복구가 불가능했다 — videoId별 `Map<string, Transcript>`(`transcripts`)로 전환해 덮어쓰기 자체를 제거. 그 외 보완: 확장을 리로드하면 기존 content script의 dedup 표시(`lastInterceptedSig`/`lastNetflixKey`)가 앱 쪽 빈 캐시와 안 맞을 수 있어 `setCapture(true)` 수신 시 항상 리셋 후 재확보, 클릭 시 일치하는 transcript 도착을 최대 2.5초(`TRANSCRIPT_WAIT_MS`) 기다리되 한 번 타임아웃된 videoId는 `timedOutVideoIds`로 표시해 이후 클릭은 대기 없이 열리게 함(자막 자체가 없는 영상에서 매번 2.5초씩 느려지는 부작용 방지), 넷플릭스는 캡처 여부와 무관하게 상시 도는 1초 폴링으로 선택 모드 진입 전부터 미리 자막을 받아둠(단, 이미 확보된 영화는 스킵 — 안 그러면 매초 DOM 스캔을 반복해 랙 유발).
- [x] ~~(담당 B) 확장 hover 박스 위에서 커서가 pointer 로 안 바뀜(유튜브는 드래그 손 모양, 넷플릭스는 기본 화살표로 고정, 2026-07-29 사용자 제보)~~ → **원인·수정**: 유튜브/넷플릭스가 자막 컨테이너 주변 요소에 이미 자체 인라인 `cursor`(진행바 드래그용 grab 등)를 지정해뒀는데, CSS 상속은 "그 요소 자신에게 지정된 값이 하나도 없을 때만" 일어나서 `document.documentElement`에 인라인 `!important`를 줘봐야 자손이 자기 값을 이미 갖고 있으면 상속 자체가 발생하지 않아 못 이겼다. `*` 전체 선택자에 `!important`를 건 `<style>` 시트를 주입해(자손에 직접 매치되므로 상속이 아니라 우선순위 승부) 해결(`extension/src/highlight.ts`).
- [x] ~~(2026-07-30) 창 선택/설정 화면 전환·포커스·단축키 관련 자잘한 수정 일괄~~ → 모드 전환 기본 단축키를 `Alt+Q`→`` Alt+` ``(1 왼쪽 backtick)로 변경 — mac/Windows 공통 accelerator 문자열이라 양쪽 다 자동 대응(`settingsStore.ts`/`selection/shortcut.ts`). 창 선택 화면에서 이미 선택된 창이 있는 상태로 Esc를 누르면 메인 화면으로 안 돌아가고 그냥 창을 숨기도록 수정(`WindowPickerScreen.tsx`, 새 IPC `WINDOW_HIDE`). main/picker/settings가 창 하나를 재사용하는 구조라 숨겨진 창을 `show()`할 때 이전 화면이 잠깐 보이는 깜빡임이 있었던 것을, 렌더러가 목표 라우트로 전환·렌더를 마쳤다는 응답(`NAVIGATE_READY`)을 받은 뒤에야 `show()`하도록 순서를 뒤집어 제거(`windows.ts` `showMainWindowAtRoute`, 팝업 창의 `POPUP_CONTENT_READY`와 동일 패턴). 메인 창이 이미 떠 있는데 다른 창에 가려졌거나 다른 앱이 활성 상태일 때 트레이의 "창 선택"/"설정"을 눌러도 앞으로 안 나오던 문제 — 팝업에 쓰던 `forceWindowToFront`(app.focus steal + moveTop + focus)를 메인 창에도 적용. 재추출 중 자동 탐지 블록(`debugBlocks`) 잔상 제거 — 화면 변화로 재추출이 시작될 때 단어 박스(`sendOverlayWords([])`)만 비우고 블록은 안 비워서 새 화면 위에 이전 회차 표시가 남아있던 것을 `changeWatcher.ts`/`extractionCache.ts` 여러 지점에서 같이 비우도록 수정. Windows 트레이 메뉴 accelerator 표시("Ctrl + 콤마"처럼 OS 로케일로 키 이름이 렌더링됨) 문제 — 한때 라벨에 직접 텍스트를 붙이는 방식으로 바꿨다가, 그러면 트레이 메뉴가 열려 있는 동안 그 키로 항목을 바로 선택하는 네이티브 동작도 같이 사라진다는 걸 확인(accelerator 프로퍼티가 "표시"와 "메뉴 열려있을 때 키로 선택" 두 기능을 겸함, 분리 불가) — 후자 기능을 지키기로 하고 native accelerator로 원복(`tray.ts`).
- [x] ~~(2026-07-30) 단축키 중복 등록 방지 + 설정 화면 단축키 섹션 문구 정리~~ → 같은 키 조합을 다른 단축키에 이미 쓰고 있으면 저장을 막고 어느 기능과 겹치는지 경고 표시(`SettingsScreen.tsx` `SHORTCUT_FIELDS`/`shortcutError`) — `globalShortcut`은 같은 accelerator를 두 번 등록하면 나중 것이 앞의 콜백을 조용히 덮어써서, 겹쳐도 UI엔 둘 다 "지정됨"으로 보이지만 실제로는 하나가 죽은 단축키가 되는 문제가 있었음. 단축키 섹션 설명 문구를 "연필 아이콘으로 수정 가능" 대신 실제 등록 가능한 키 조합 규칙(수식키 최소 1개 + 아무 키, 또는 F1~F12 단독)을 mac/Windows 키 이름에 맞춰 안내하도록 변경.
- [x] ~~(2026-07-30) 폰트 통일(292번 항목) 후속 — 적용 누락분 전수조사~~ → button/input/select/textarea가 브라우저 기본값상 body의 `font-family`를 상속하지 않고 OS 네이티브 컨트롤 폰트를 써서 Noto Sans KR이 폼 컨트롤 텍스트엔 안 먹히던 것을 전역 `font-family: inherit` 규칙으로 수정(`styles.css`). 단축키 조합 표시(`.shortcut-keys`)도 모노스페이스 대신 Noto Sans KR로 통일. 채팅 답변 마크다운 인라인 코드(`` `code` ``, `.bubble .md code`)는 mac(SF Mono)/Windows(Consolas 계열)에서 서로 다른 서체로 그려지는 걸 확인했으나, 코드를 본문과 시각적으로 구분하는 통상적 관례로 보고 의도적으로 유지하기로 결정(사용자 확인) — OS 무관 통일이 필요해지면 body와 같은 Noto Sans KR로 바꾸면 됨.

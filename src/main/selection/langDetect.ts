import type { Language } from '@shared/types'

// 담당 A — 언어 자동 감지 (PLAN.md §4.1 / §6)
// 자동 감지 + OCR 필요 시, 언어 특화 OCR 전에 경량 분류/범용 OCR 로 언어 특정.
//
// ── zh-Hans/zh-Hant 판별 가이드 (PLAN.md §5) ──────────────────────────────────
// `Language` 는 `'zh'` 하나가 아니라 `'zh-Hans'`(간체/대륙식) / `'zh-Hant'`(번체/대만식)로
// 나뉜다. 사전 API가 스크립트별로 다른 소스를 쓰기 때문에(汉典·CC-CEDICT는 간체 우선,
// 萌典은 번체 전용) 여기서 zh 로 판정되면 반드시 스크립트까지 같이 정해줘야 한다.
//
// 이건 "변환"이 아니라 "판별"이라 상대적으로 쉽다 — 변환(OpenCC 등)은 간체 한 글자가
// 여러 번체 후보로 갈리는 一簡對多繁 같은 진짜 모호한 문제지만, 판별은 반대로 대부분의
// 상용한자가 스크립트별 고유 형태를 갖고 있어서(国/國, 汉/漢, 电/電, 头/頭 …) 표본
// 텍스트에 있는 글자 몇 개만 세어봐도 신뢰도 높게 판정된다.
//
// 권장 구현: OpenCC(https://github.com/BYVoid/OpenCC) 프로젝트가 공개해둔
// 간체 전용 한자 집합(simp only)과 번체 전용 한자 집합(trad only) 매핑 데이터를 그대로
// 가져와, sample 문자열에서 각 집합에 속하는 글자 수를 세어 다수인 쪽으로 판정하면 된다
// (새로 조사해서 만들 필요 없이 기존 오픈소스 매핑 테이블을 재사용). 둘 다 안 나오거나
// 동률이면(예: 짧은 표본, 한자 자체가 적은 문장) 기본값(예: zh-Hans)으로 폴백.
//
// 참고: 위 판정은 en/ja/zh 자체를 가르는 유니코드 블록 분류(한글/가나/한자 비율 등)와는
// 별개 단계다 — 먼저 en/ja/zh 중 무엇인지 정하고, zh 로 판정된 경우에만 위 스크립트
// 판별을 한 번 더 거친다.

export async function detectLanguage(_sample?: string): Promise<Language> {
  // TODO(담당 A): 유니코드 블록 비율 기반 경량 분류 → en/ja/zh, zh 인 경우 위 가이드대로
  // zh-Hans/zh-Hant 스크립트 판별까지 마저 수행.
  return 'en'
}

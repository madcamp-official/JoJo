// 오버레이 (PLAN.md §4.1) — 담당 A
// 투명·클릭스루 창 위에서 단어 hover 시 커서/사각형 하이라이트, 클릭 시 선택 확정.
// TODO(담당 A): 단어 bbox 를 받아 hover/select 렌더, setIgnoreMouseEvents 토글.
export function Overlay() {
  return <div className="overlay-root" />
}
